# DISABLED: Duplicate of claude.yml — use that workflow for @claude mentions
# This file is kept for git history (per CLAUDE.md "NO deletions, refactor only")
name: Claude Code Action Official (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: See claude.yml instead
    runs-on: ubuntu-latest
    steps:
      - name: Redirect notice
        run: |
          echo "This workflow is deprecated. Use claude.yml for @claude interactions."
          echo "See: .github/workflows/claude.yml"
