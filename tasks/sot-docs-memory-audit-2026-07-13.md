# SOT + Docs + Memory Sync Audit — 2026-07-13

Repo-wide scan (4 parallel scouts) triggered by the brand-font-canon drift. Root cause: **brand canon was denormalized into ~40 restatements** across skills, docs, evals, agent prompts, memory, and `brand.yaml`; only the generator chain (`typography.json → design-tokens.css`) stayed correct. Fix pattern: **point each copy at the SOT instead of restating it.**

Current font canon (ground truth = `typography.json` + shipped `assets/fonts/*.woff2`):
Archivo (display) · Hanken Grotesk (body/UI) · Anton (UI caps/accent) · Cinzel (engraved-caps accent) · Inter (fallback) + bespoke name-scripts BR Script / LH Graffiti / Pinyon (SIG) / Grand Hotel (KC).
**CUT 2026-07-10 — never reintroduce:** Playfair Display · Cormorant Garamond · Bebas Neue · Yellowtail. (Pacifico/Kaushan = old scripts, replaced by BR/LH bespoke.)

---

## 1. SOT registrations — DONE this session (branch `chore/skill-font-canon-sync`)

- [x] `typography.json` — root symlink + Data SOT row (commit `9a64d96cf`)
- [x] `data/collections/<slug>/identity.json` (×4) — Data SOT row (SOURCE, collection-metadata canon)
- [x] `data/collections/<slug>/sot.json` (×4) — Data SOT row (GENERATED view)
- [x] `assets/brand/brand.yaml` — Brand canon row (fonts excluded → typography.json)
- [x] `llm/model_ids.py` + `knowledge-base/prompts/INDEX.yaml` — added to Domain config map (MCP/agents)

## 2. SOT — candidates NOT yet registered (lower priority, review)

| Path | Owns | Note |
|---|---|---|
| `config/collections.py` | pipeline collection registry (aesthetic/price/copy_voice) | **downstream mirror** of identity.json — slug drift (`kids` vs `kids-capsule`, only 3 collections). Demote-and-note, don't bless as canon. |
| `assets/products/manifest.json` | per-SKU render-source asset map (+sha, `catalog_sha`) | GENERATED by `scripts/build_asset_manifest.py`; Imagery row candidate |
| `config/asset_taxonomy.yaml` + `asset_tagging.yaml` | asset style/mood/color vocabulary | low-value SOT |
| `config/skyyrose_3d_config.yaml` | 3D gen config | low-value SOT |
| schemas: `identity.schema.json`, `typography.schema.json` | validators for the above | note on parent rows (not separate) |

## 3. Duplications / fragmentation to reconcile (HOLD — touches declared SOTs)

- **`brand.yaml` `typography:` block (lines ~148-173)** still lists Playfair/Cormorant/Bebas as canonical — a **stale second font authority** duplicating registered `typography.json`. Dead (fonts flow via typography.json→gen-design-tokens.py) but misleading. → **strip block, point to typography.json.** Verify `scripts/sync_brand_to_php.py` doesn't read it first (theme CLAUDE.md says `brand.generated.php` supplies tagline + collection helpers, not fonts).
- **`brand.yaml` `catalog.yaml` ref (lines 20, 226, 234)** → `assets/product-masters/catalog.yaml` **does not exist**. Repoint to `skyyrose-catalog.csv` or delete refs.
- **Collection metadata fragmented** across identity.json (canon) · brand.yaml `collections:` · config/collections.py. → identity.json is canon (now in SOT.md); demote the other two to labeled downstream.
- **Color-token authority fragmented** across brand.yaml `colors:` · design-tokens.css · `inc/brand-colors.php` (self-labels "SSOT" but admits it mirrors design-tokens.css). → design-tokens.css chain is canon; brand-colors.php is a subordinate mirror.

## 4. Docs to sync — font canon (deterministic swap, same as skills)

**HIGH — would re-introduce cut fonts / actively mislead a fresh session:**
- [ ] `eval/brand.md:35`, `eval/brand-story.md:124-126,129` — eval rubrics list cut fonts as current → would FAIL correct code
- [ ] `docs/brand/collection-design-proposals.md` (~30 refs), `collection-stories.md:44,111,185,255`, `collection-ux-architecture.md:105` (stale `design-tokens.css:302-348` claim — file is 339 lines, 0 cut fonts)
- [ ] `agents/elite_web_builder/prd.md:38-39` + `knowledge/wordpress.md:92-94` + `knowledge/wordpress_deployment.md:162-164` — **LIVE agent scaffolding** mandating "Inter + Playfair, Google Fonts import ONLY" → generates wrong CDN output
- [ ] `.claude/skills/luxury-design-taste/SKILL.md:21,43` — names cut set as "locked brand fonts"
- [ ] `.design-sync/NOTES.md:40-41,71` — blesses Yellowtail(BR)/Kaushan(LH); wired in settings.json
- [ ] `wordpress-theme/skyyrose-flagship/data/collections/README.md:67` — Pacifico/Kaushan as current
- [ ] `skyyrose/ARCHITECTURE_V2.md` + `skyyrose/README.md` — cut fonts + Google-Fonts CDN (part of dead prototype §6 — fix or drop with it)

**MED — stale but scoped/dated:**
- [ ] `.ralph/ralph-directive.md:207,830` — "Google Fonts preconnect" (active loop; contradicts zero-CDN)
- [ ] `.claude/learn-wordpress-theme-progress.md` — 8-12 families incl cut set (self-flags drift)
- [ ] `docs/superpowers/**` plans/specs (2026-05-25, 06-14, 06-22) — Yellowtail/Kaushan/Playfair — add superseded banner
- [ ] `frontend/DASHBOARD_STATUS.md:55,125-126`, `frontend/docs/CONTRIB.md:500-501` — dashboard fonts.ts (known open item, `tasks/todo.md:127`)
- [ ] `docs/engineering-learnings.md:52` — "1.0.0 for commercial release" contradicts :168 (1.11.0) — fix version line
- [ ] `tasks/todo.md:458-476` — P1 Fonts still frames Yellowtail/Kaushan + dead `assets/fonts/collections/*.woff2`; cites nonexistent `skyyrose-tokens.css`
- [ ] the "9 font families / zero CDN" sentence restated verbatim in 7+ docs → make them reference SOT, not restate

**LOW:** ~30 doc `**Version**: 1.0.0` footers (doc-version, not theme); `.claude/skills/*/references/*` Bebas fallback literals → Anton; `archive/**` historical (add banner, no content change).

## 5. Memory to sync (HOLD — LOCKED founder rules; get nod before editing)

- [ ] **`~/.claude/.../memory/feedback_collection_canon_attribution.md:33,51`** — **LOCKED** rule asserts LH="Yellowtail + Playfair italic", SIG="Italiana + Pinyon" as CURRENT display fonts. Read as canon every session. Keep the copy-attribution rule; fix the font lines.
- [ ] **`feedback_collection_lockup_assets.md:3,31`** — **LOCKED**; lists Yellowtail/Italiana/Unifraktur as current interior fonts. Keep "hero titles = lockup images" rule; fix font list.
- [ ] `google-fonts-selfhost.md`, `project_structural_remediation_2026_07_05.md` — historical → add "SUPERSEDED 2026-07-10" banner (no content change)
- [ ] `MEMORY.md` — no typography entry → add `project_typography_canon.md` mirroring cerebrum's 2026-07-13 entry (font overhaul only propagated to cerebrum + claude-mem, not auto-memory or buglog)
- [ ] `.wolf/anatomy.md` — 7 deleted-dir headers + misses `data/brand/` + `assets/hub/` → **regenerate via `openwolf scan`** (auto-generated, don't hand-edit)
- [ ] `.wolf/cerebrum.md:19` — "30 products" → 33
- [ ] SOT.md doesn't mention the auto-memory store (`~/.claude/projects/.../memory/`) — add a pointer note (lives outside repo, can't symlink)
- VERIFIED CLEAN: cerebrum's 2026-07-13 font Do-Not-Repeat entry is correct; `.wolf` carries only historical font strings; claude-mem #24324 mis-title already flagged in cerebrum.

## 6. Dead prototype `skyyrose/` — disposition (Founder approved delete-after-census; HELD on new evidence)

Census (Scout D) — the tree is **mixed**, not a clean prototype:
- **LIVE — keep:** `elite_studio/` (188 consumers), `core/` (150), `character_pipeline/` (Love Hurts Girl mascot), `integrations/`, `multi_agent/` (runnable), `cli_harnesses/` (vendored, retain).
- **PAID — STOP-AND-SHOW, never autonomous-delete:** `skyyrose/assets/scenes/**/*-v2.png` (3 founder-locked Stage-1 scene renders).
- **LIVE-wired parallel data (reconcile, NOT delete):** `skyyrose/assets/data/{product-content,garment-analysis,product-embeddings,alt-text,woocommerce-import}` — read by `agents/social_media_agent.py`, commerce/imagery domain agents, 5 scripts. Genuine second product-data layer → SOT reconciliation task.
- **DEAD static-prototype cluster (~59 files, census-clean, safe delete-as-tracked-code):** `skyyrose/*.html` (index/collections/explore/preorder/product-*), `collections/*.html`, `explore/`, `assets/css/`, `assets/js/`, `sw.js`, `api/assistant.js`, `build/`, `package.json`, `package-lock.json`, `ARCHITECTURE_V2.md`, `DUAL_PAGE_ARCHITECTURE.md`, `README.md`, top-level `skyyrose_content_agent.py` + `skyyrose_production_studio.py`. Zero live serve/deploy/CI/test refs (root `vercel.json`→`frontend/.next`; prototype `package.json` = local `http-server` preview only). Superseded by skyyrose.co theme + devskyy.app dashboard.
- **DEAD duplicate:** `skyyrose/assets/data/prompts/` (8 files) — second prompt-template store, zero live consumers → delete with cluster.

Recommended: delete the ~59-file dead cluster + prompts/ as ONE repoint-first commit (STOP-AND-SHOW the exact list); leave paid scenes + live data store; open a separate reconcile task for `assets/data/`.

## 7. Version drift (out of font scope, flagged)

- `llm/model_ids.py` pins `claude-opus-4-7` + `INDEX.yaml` refs `claude-opus-4-7` — project runs **Opus 4.8**. Bump after verifying no downstream assumes 4-7.
- Residual `pacifico-latin.woff2` + `kaushan-script-latin.woff2` still on disk in theme `assets/fonts/` (replaced by bespoke BR/LH) — census-gated cleanup candidate.

---

### Execution order (recommended)
1. ✅ SOT registrations (done) — commit.
2. Font-canon doc sync (§4 HIGH+MED) — deterministic, fan-out subagents (same pattern as skills). **Excludes LOCKED memory.**
3. `brand.yaml` typography reconcile (§3) — after verifying sync_brand_to_php.py.
4. Memory sync (§5) — **needs founder nod** (LOCKED files).
5. Dead-prototype delete (§6) — **STOP-AND-SHOW** the file list.
6. Data-store + fragmentation reconcile (§3, §6) — separate design task.

---

## 8. Discovered DURING the sync (2026-07-14) — code drift + follow-ups

**Done in the sync batch:** ~57 docs + eval rubrics (eval/brand.md, brand-story.md, banned-elements.md, premium-feel.md, design-system.json) + agents/elite_web_builder (prd/run.py/knowledge/*) + agents/core/creative/brand_creative.py + .claude/skills/luxury-design-taste + .ralph + .design-sync + collections/README + brand.yaml typography block (was live-loaded by `skyyrose/elite_studio/brand.py`) + 2 LOCKED auto-memory files + cerebrum count + new `project_typography_canon.md`. 24 dated docs bannered SUPERSEDED.

**CODE drift still live (NOT docs — separate change each, flagged by the sync agents):**
- `orchestration/brand_context.py:78-79` — ships Playfair Display / Montserrat (a doc quotes it verbatim; fix code then doc).
- `frontend/tailwind.config.ts:13-14` — `--font-playfair` / `--font-cormorant` tokens (dashboard, devskyy.app — separate surface).
- `design-system/skyyrose-storefront/scripts/sync-theme-assets.mjs:31-43,97-101` — `CANON_WOFF2`/`CURATED` still pin retired woff2s → **re-syncs cut fonts into the DS bundle on every `npm run sync`.** Highest-risk code item.
- `agents/llm_roundtable/ui/LLMRoundtable.tsx:271` — Google Fonts `@import` (internal dashboard UI).
- Residual `pacifico-latin.woff2` + `kaushan-script-latin.woff2` on disk in theme `assets/fonts/` (unreferenced by identity.json) — census-gated binary cleanup.

**Doc long-tail not yet swept:**
- `docs/brand/design-mockups/collection-designs.html` + `v2-visual-fixes.md` — dated mockup artifacts with Google-CDN + cut fonts. Banner or leave as historical mockups.

**Memory maintenance follow-ups:**
- `MEMORY.md` auto-memory index is 19.8KB (near 24.4KB read limit) → compact (one line/entry, move detail to topic files).
- `.wolf/anatomy.md` stale (7 deleted-dir headers; misses `data/brand/`, `assets/hub/`) → regenerate via `openwolf scan` (auto-generated — do not hand-edit).
- The auto-memory store (`~/.claude/projects/-Users-theceo-DevSkyy/memory/`) is not referenced in SOT.md (lives outside repo; add a pointer note).
