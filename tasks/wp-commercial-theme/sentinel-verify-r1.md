# Sentinel — Independent Live Verification, Round 1 (v1.12.0)

Date: 2026-07-19 · Method: cache-busted curls (`?cb=$(date +%s)`) against live skyyrose.co, fresh fetches only, no WebFetch. Version stamp confirmed live: `style.css?ver=1.12.0`.
Pages fetched: `/`, `/about/`, `/product/the-fannie/`, `/collections/black-rose/`, `/collections/love-hurts/`, `/cart/`, one guaranteed-404 URL.

**Score: 13 VERIFIED / 0 FAILED / 0 INCONCLUSIVE** (one informational sub-note on item 3).

| # | Item | Verdict | Evidence (fresh, this session) |
|---|------|---------|-------------------------------|
| 1 | Double mobile-nav gone | **VERIFIED** | Exactly 1 `<nav class="mobile-nav" aria-label="Mobile navigation">` on `/` and on `/about/` (grep count = 1 each). |
| 2 | style.css hero-offset + homepage-v2 hide block | **VERIFIED** | Live style.css:520 `.page-template-template-immersive-signature .site-content {` → `padding-top: 0;`; :530-536 `.homepage-v2 .site-header, .site-footer, .sr-scroll-progress, .sr-back-to-top { display: none; }` intact. |
| 3 | Render-blocking CSS reduced + deferred-sheet pattern | **VERIFIED** | Homepage: 27 render-blocking stylesheet links (noscript excluded) vs ~40 pre-train. Deferred pattern (`media='print' onload` + `<noscript>` twin) live for size-guide, luxury-cursor, cookie-consent on `/`, and view-transitions on both collection pages. *Note:* smart-showcase, personalization, brand-atmosphere appear on NO page in the 6-URL sample — CSS **and** JS both absent (no style/script mismatch), so they are enqueue-gated off these pages entirely; their deferred markup could not be exercised. Not a defect — absence beats deferral for perf. |
| 4 | Header video fallback → static webp | **VERIFIED** | The single header `<video>`: poster AND fallback `<img>` both = `assets/branding/tsrc-lockup-static@2x.webp` (HEAD: HTTP 200, `content-length: 5854`, image/webp). `tsrc-lockup-rotating@2x.webm` source present. Zero HTML references to the rotating .webp (the one "rotating" match is the .webm source). Informational: the 636,270B rotating webp still exists on the server (200) but is unreferenced — expected, it's a gitignored rider and hot-swap is additive. |
| 5 | Font preloads + token aliases | **VERIFIED** | `/`: `<link rel="preload" ... hanken-grotesk-latin.woff2>` present; inter-latin preload count = 0. 404 URL (confirmed `error404` template): inter-latin preload count = 1. Live `design-tokens.min.css?ver=1.12.0` contains `--font-body:var(--skyyrose-font-body); --font-display:var(--skyyrose-font-display); --font-ui:var(--skyyrose-font-ui)` in the §1.12 alias block. |
| 6 | Consent banner hidden + reveal gate | **VERIFIED** | `/` HTML: `<div id="skyyrose-cookie-consent" class="cookie-consent cookie-consent--hidden" hidden role="dialog" ...>`. Inline script contains `function sheetLive() { return 'fixed' === window.getComputedStyle( banner ).position; } if ( sheetLive() ) { reveal(); } else { ... }` (sheetLive ×3). |
| 7 | Size-guide pre-modal guard | **VERIFIED** | `/product/the-fannie/`: `<style>.sg-overlay[aria-hidden="true"]{opacity:0;visibility:hidden}</style>` immediately precedes `<div id="size-guide-modal" class="sg-overlay" ... aria-hidden="true" inert>` (guard pos 88637 < modal pos 88706). |
| 8 | Collection hero lockups | **VERIFIED** | BR + LH: one `img.col-hero__logo` each; srcset = `i0.wp.com/...lockup.webp?w=480&v=1.12.0 480w, ...w=640... , ...` (i0.wp.com ✓, `&v=1.12.0` ✓); no `rv-clip-up`; `fetchpriority=high loading=eager` on both. |
| 9 | Cart empty shell + PDP dossier seal | **VERIFIED** | `/cart/`: `.skyy-cart__empty` shell rendered (6 refs), `<h2 class="skyy-cart__empty-title">` present, no rv-clip-up on any `.skyy-cart__empty*` element. `/product/the-fannie/`: 0 hits for `NOT a backpack`, 0 for `garment_type_lock`, 0 for `sr-ed__garment-lock`. |
| 10 | Data-exposure seal | **VERIFIED** | Sealed 7/7 → HTTP 404: dossiers/black-rose-bomber-sherpa.md, product-embeddings.json, logo-registry.json, collections/_orphans.json, collections/identity.schema.json, scripts/build-editorial-index.js, data/build-collection-sot.py. Allowed 5/5 → HTTP 200: skyyrose-catalog.csv (21,771B), v7-cards.json (10,283B), site-guide.json (6,292B), editorial-index.json (1,677B), collections/black-rose/sot.json (21,567B). |
| 11 | Permissions-Policy header | **VERIFIED** | `/` response header: `permissions-policy: ... accelerometer=(self), gyroscope=(self), ...`. |
| 12 | Bolt's dequeue risk spots | **VERIFIED** | (a) Global-styles dequeue on `/`, BR, LH, about, PDP: all 29 `var(--wp--preset-...)` usages per page live inside Jetpack's OWN inline CSS and every one carries an inline fallback value (e.g. `var(--wp--preset--spacing--40,1.5rem)`); zero `has-*-color` classes; zero preset definitions needed. Cart correctly KEEPS global-styles (127 preset definitions, 242 usages). (b) Elementor: homepage body carries `elementor-page elementor-page-9822` but renders ZERO Elementor markup (`elementor-element`/`data-elementor`/`elementor-section` all 0) — the elementor-frontend dequeue is safe; no page in the sample renders Elementor content. (c) Cart footer-cro: `<link ... footer-cro ...>` in `<head>` (pos 79,222 < `</head>`) before the `ft-cro` markup (pos 97,199). |
| 13 | PDP editorial survives fail-closed gate | **VERIFIED** | `/product/the-fannie/`: `sr-ed__encounter` present (4 refs) — editorial chapter structure renders for the eligible SKU. |

## Residuals / notes for the train
- Item 3 note: smart-showcase / personalization / brand-atmosphere sheets unseen on all 6 sampled URLs (CSS+JS both absent — consistent, no unstyled-UI risk). If those features are expected on any of these pages, that's an enqueue-gating question for Bolt, not an async-CSS defect.
- Rotating 636KB webp still physically on the server (unreferenced). Harmless; only relevant if anything ever re-references it.
- Lighthouse round-2 numbers are the concurrent sweep's deliverable — this report is HTML/header/asset truth only, per tasking.
