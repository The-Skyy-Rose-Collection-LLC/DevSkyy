# DevSkyy Backend - Optimized for Fast Deployment
# =================================================
# Core dependencies only (~2-5 min build time)
# For ML/3D features, see requirements-full.txt

# Core Framework
fastapi>=0.104
uvicorn[standard]>=0.24
pydantic>=2.5
pydantic-settings>=2.1
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0
asyncpg>=0.29
aiosqlite>=0.19
alembic>=1.13

# Security
PyJWT>=2.8
email-validator>=2.1
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
argon2-cffi>=23.1
cryptography>=41.0

# HTTP Client
httpx>=0.25
aiohttp>=3.9
certifi>=2023.0
requests>=2.31
woocommerce>=3.0.0
gradio-client>=0.8

# AI/LLM Providers (Required)
openai>=1.3
anthropic>=0.7
google-generativeai>=0.3.0
groq>=0.4
mistralai>=1.0
cohere>=5.0

# Utilities
structlog>=23.2
python-dotenv>=1.0
tenacity>=8.2
orjson>=3.9
aiofiles>=23.2
anyio>=4.0
PyYAML>=6.0
toml>=0.10.2

# Monitoring
prometheus-client>=0.19

# Minimal AI Support
numpy>=1.24.0
scipy>=1.14.1
tiktoken>=0.8.0

# Message Queue
celery>=5.3.0
redis>=5.0.0

# WordPress Integration
python-wordpress-xmlrpc>=2.3

# Agent SDK
claude-agent-sdk==0.1.18

# LlamaIndex (multimodal capabilities - Anthropic + OpenAI only, no torch)
llama-index>=0.10.0
llama-index-llms-openai>=0.1.0
llama-index-multi-modal-llms-anthropic>=0.1.0
# HuggingFace multimodal requires torch - see requirements-full.txt

# ============================================
# OPTIONAL: Uncomment for advanced features
# ============================================

# RAG / Embeddings (adds ~500MB, +5min build time)
# sentence-transformers>=3.3.1
# chromadb>=0.5.23
# scipy>=1.14.1

# Transformers (adds ~2GB, +10min build time)
# transformers>=4.36.0
# accelerate>=0.25.0
# safetensors>=0.4.0

# Image Generation - SDXL/LoRA (adds ~5GB, +15min build time)
# Pillow>=10.0
# diffusers>=0.25.0
# peft>=0.7.0
# datasets>=2.16.0
# xformers>=0.0.23
# bitsandbytes>=0.42.0

# Image Processing (adds ~200MB, +3min build time)
# scikit-image>=0.22.0
# imagehash>=4.3.1
# opencv-python-headless>=4.8
# gradio-client>=0.8

# 3D Rendering (adds ~300MB, +5min build time)
# trimesh>=4.0
# pyrender>=0.1.45
# pyglet>=2.0

# PyTorch (install separately, adds ~3GB)
# torch>=2.1.0
# torchvision>=0.16.0
sentry-sdk>=2.49.0
