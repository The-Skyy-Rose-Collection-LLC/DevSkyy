# DevSkyy Platform Dependencies
#
# This file is for backward compatibility with pip-only environments.
# The canonical dependency source is pyproject.toml
#
# Recommended installation:
#   pip install -e ".[dev]"           # Development (includes test tools)
#   pip install -e ".[all]"           # Everything
#   pip install -e .                  # Core only
#
# Or with uv (faster):
#   uv pip install -e ".[dev]"
#
# For reproducible production builds, use:
#   pip-compile pyproject.toml -o requirements-lock.txt
#   pip install -r requirements-lock.txt

# Core Framework
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6
pydantic>=2.5
pydantic-settings>=2.1

# Database
sqlalchemy>=2.0
alembic>=1.13
asyncpg>=0.29
aiosqlite>=0.19

# Security
PyJWT>=2.8
passlib[bcrypt]>=1.7
argon2-cffi>=23.1
cryptography>=41
bcrypt>=4.1

# HTTP
httpx>=0.25
aiohttp>=3.9
requests>=2.31

# AI/ML & MCP Server
openai>=1.6
mcp>=0.9.0
# anthropic>=0.8
# google-generativeai>=0.3

# Caching (optional)
# redis>=5.0
# celery>=5.3

# Monitoring (optional)
# prometheus-client>=0.19
# structlog>=23.2

# WordPress (optional)
# wordpress-xmlrpc>=2.3
# woocommerce>=3.0

# Utilities
python-dotenv>=1.0
tenacity>=8.2
pyyaml>=6.0
orjson>=3.9
python-dateutil>=2.8
email-validator>=2.1
