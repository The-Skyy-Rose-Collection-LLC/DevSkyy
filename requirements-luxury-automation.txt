# DevSkyy Luxury Fashion Automation - Production Dependencies
# Python 3.11+ Required (per CLAUDE.md Truth Protocol)
# All versions pinned for reproducibility
# Updated: 2025-11-12 to match main requirements.txt versions

# ============================================================================
# Core Framework (Pinned exact versions per CLAUDE.md)
# ============================================================================
fastapi==0.119.0  # Web framework - https://fastapi.tiangolo.com/
pydantic==2.10.4  # Data validation - https://docs.pydantic.dev/
pydantic-settings==2.7.0  # Settings management (updated)
uvicorn[standard]==0.38.0  # ASGI server (updated to match main)
python-multipart==0.0.18  # File upload support (updated to match main)

# ============================================================================
# Security (CLAUDE.md: Cite RFC 7519 for JWT, NIST SP 800-38D for encryption)
# ============================================================================
# JWT Authentication per RFC 7519: https://tools.ietf.org/html/rfc7519
PyJWT==2.10.1  # JWT implementation (updated to match main)
cryptography==46.0.3  # NIST-compliant encryption (AES-256-GCM per NIST SP 800-38D)
passlib[bcrypt]==1.7.4  # Password hashing (Argon2/PBKDF2)
bcrypt==4.2.1  # bcrypt for passwords (updated to match main)

# ============================================================================
# AI & ML Libraries (Exact versions required)
# ============================================================================
# PyTorch & CUDA (updated to match main requirements.txt)
torch==2.9.0  # PyTorch framework (updated from 2.1.1)
torchvision==0.20.0  # Vision utilities (updated to match torch 2.9.0)
torchaudio==2.9.0  # Audio utilities (updated to match torch 2.9.0)

# HuggingFace Ecosystem (updated to match main requirements.txt)
transformers==4.57.1  # Transformers library (updated)
diffusers==0.35.2  # Diffusion models (SDXL, ControlNet, etc.) (updated)
accelerate==1.3.0  # Training acceleration (updated)
safetensors==0.6.2  # Safe tensor storage (updated)

# Image Processing (updated to match main requirements.txt)
pillow==12.0.0  # PIL/Pillow for images (updated)
opencv-python==4.11.0.86  # OpenCV for computer vision (updated)
imageio==2.36.1  # Image I/O (updated)
imageio-ffmpeg==0.5.1  # Video support (updated)

# Scientific Computing (updated to match main requirements.txt)
numpy==2.3.4  # Arrays and numerical computing (updated)
scipy==1.14.1  # Scientific algorithms (updated)

# ============================================================================
# Database & Caching (updated to match main requirements.txt)
# ============================================================================
sqlalchemy==2.0.36  # ORM for database (updated)
alembic==1.14.0  # Database migrations (updated)
redis==5.2.1  # Redis client for caching (updated)
asyncpg==0.30.0  # PostgreSQL async driver (updated)
psycopg2-binary==2.9.9  # PostgreSQL driver

# ============================================================================
# Monitoring & Observability (CLAUDE.md: Prometheus/Grafana integration)
# ============================================================================
prometheus-client==0.22.0  # Prometheus metrics (updated)
prometheus-fastapi-instrumentator==7.0.0  # FastAPI integration (updated)
python-json-logger==3.2.0  # Structured logging (updated)

# ============================================================================
# API Clients & Integrations (updated to match main requirements.txt)
# ============================================================================
# AI Model APIs
openai==2.7.2  # OpenAI API (GPT-4, DALL-E) (updated)
anthropic==0.69.0  # Anthropic API (Claude) (updated)
aiohttp==3.11.11  # Async HTTP client (updated)
httpx==0.28.1  # Modern HTTP client (updated)

# Cloud Storage (updated)
boto3==1.36.7  # AWS SDK (updated)
google-cloud-storage==2.19.0  # Google Cloud Storage (updated)

# ============================================================================
# Task Queue & Background Jobs (updated to match main requirements.txt)
# ============================================================================
celery==5.4.0  # Distributed task queue (updated)
redis==5.2.1  # Celery broker (updated)

# ============================================================================
# Testing (CLAUDE.md: Unit and integration tests required) (updated)
# ============================================================================
pytest==8.4.2  # Testing framework (updated)
pytest-asyncio==0.24.0  # Async test support (updated)
pytest-cov==6.0.0  # Coverage reporting (updated)
pytest-mock==3.14.0  # Mocking utilities (updated)
faker==33.1.0  # Test data generation (updated)
factory-boy==3.3.1  # Test fixtures (updated)

# ============================================================================
# Development & Code Quality (updated to match main requirements.txt)
# ============================================================================
black==24.10.0  # Code formatting (updated)
flake8==7.1.1  # Linting (updated)
mypy==1.13.0  # Type checking (updated)
isort==5.13.2  # Import sorting
pre-commit==3.8.0  # Git hooks (updated)

# ============================================================================
# Documentation (updated)
# ============================================================================
mkdocs==1.6.1  # Documentation generator (updated)
mkdocs-material==9.5.47  # Material theme (updated)
pdoc3==0.11.1  # API documentation (updated)

# ============================================================================
# Utilities (updated to match main requirements.txt)
# ============================================================================
python-dotenv==1.0.1  # Environment variables (updated)
pyyaml==6.0.2  # YAML parsing (updated)
ujson==5.10.0  # Fast JSON (updated)
orjson==3.10.13  # Even faster JSON (updated)
python-dateutil==2.9.0.post0  # Date utilities (updated)
pytz==2024.2  # Timezone support (updated)

# ============================================================================
# Additional ML/AI (for complete HuggingFace integration) (updated)
# ============================================================================
# Note: Some models require specific versions
sentence-transformers==3.4.1  # Sentence embeddings (updated)
tokenizers==0.22.1  # Fast tokenization (updated)
datasets==3.2.0  # HuggingFace datasets (updated)
huggingface-hub==0.36.0  # Model hub client (updated)

# ============================================================================
# Notes & Requirements
# ============================================================================
# Python Version: >=3.11.0,<3.12.0
# CUDA: 11.8+ (for GPU acceleration)
# System RAM: 16GB minimum, 32GB recommended
# GPU RAM: 12GB+ (for Stable Diffusion XL)
# Storage: 100GB+ (for model weights)

# Installation:
# pip install -r requirements-luxury-automation.txt

# For GPU support:
# pip install torch==2.1.1 torchvision==0.16.1 --index-url https://download.pytorch.org/whl/cu118

# External API Keys Required (set in .env):
# - OPENAI_API_KEY (for DALL-E, GPT-4)
# - ANTHROPIC_API_KEY (for Claude)
# - HUGGINGFACE_TOKEN (for private models)
# - JWT_SECRET_KEY (generate: python -c "import secrets; print(secrets.token_urlsafe(32))")
