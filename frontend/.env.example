# DevSkyy Dashboard Environment Variables
# =========================================
# Copy this file to .env.local and configure for your environment.
#
#   cp .env.example .env.local
#
# For development, the defaults should work out of the box.

# Backend API URL (for rewrites in next.config.mjs)
# ----------------------------------------------------
# Used by Next.js rewrites to proxy /api/* requests to the backend.
# Development: http://localhost:8000 (local FastAPI server)
# Production: Configured via Vercel environment variables (not needed in this file)
#
# Fallback chain: BACKEND_URL → NEXT_PUBLIC_API_URL → http://localhost:8000
BACKEND_URL=http://localhost:8000

# Public API base path (exposed to client-side code)
# ---------------------------------------------------
# This is the base path for API calls from the browser.
# Should always be /api (proxied via rewrites in next.config.mjs)
#
# In development: /api → rewrites to BACKEND_URL
# In production (Vercel): /api → rewrites to https://devskyy-backend.onrender.com (via vercel.json)
NEXT_PUBLIC_API_URL=/api

# Bundle Analyzer (optional, for bundle size analysis)
# -----------------------------------------------------
# Uncomment to enable bundle analysis on next build:
#   ANALYZE=true npm run build
# ANALYZE=true

# Authentication (optional, for protected routes)
# -----------------------------------------------
# NEXTAUTH_SECRET=your-secret-key-generate-with-openssl-rand-base64-32
# NEXTAUTH_URL=http://localhost:3000

# Analytics (optional)
# --------------------
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# Feature Flags (optional)
# ------------------------
# NEXT_PUBLIC_ENABLE_3D_PIPELINE=true
# NEXT_PUBLIC_ENABLE_ROUND_TABLE=true
