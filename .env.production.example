# DevSkyy Production Environment Configuration - TEMPLATE
# Generated: 2025-11-18
#
# ⚠️ CRITICAL SECURITY WARNING ⚠️
# - NEVER commit actual production secrets to version control
# - Copy this file to .env.production and replace with real values
# - If .env.production was previously committed, ALL secrets MUST be rotated immediately
# - Use strong, randomly generated values for all keys
#
# REQUIRED SECRET ROTATION:
# 1. Generate new SECRET_KEY (use: python -c "import secrets; print(secrets.token_urlsafe(32))")
# 2. Generate new JWT_SECRET_KEY (use different value than SECRET_KEY)
# 3. Update all API keys
# 4. Update database credentials
# 5. Verify .env.production is in .gitignore before committing

# Security - REQUIRED: Generate strong random values
SECRET_KEY=REPLACE_WITH_STRONG_RANDOM_SECRET_KEY_MINIMUM_32_CHARS
JWT_SECRET_KEY=REPLACE_WITH_DIFFERENT_STRONG_RANDOM_JWT_SECRET_KEY

# API Configuration
DEVSKYY_API_URL=http://localhost:8000
DEVSKYY_API_KEY=REPLACE_WITH_PRODUCTION_API_KEY

# Database - PostgreSQL Configuration
# Use strong credentials in production
DATABASE_URL=postgresql://PROD_USER:STRONG_PASSWORD@production-db-host:5432/devskyy_prod

# Redis - Caching Configuration
REDIS_URL=redis://production-redis-host:6379

# Logfire - Observability (optional)
# Uncomment and set if using Logfire
# LOGFIRE_TOKEN=your-production-logfire-token-here

# Server Configuration
HOST=0.0.0.0
PORT=8000
WORKERS=4
RELOAD=false

# Logging
LOG_LEVEL=INFO

# CORS - Restrict to specific origins in production
# NEVER use * in production
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Feature Flags
ENABLE_SWAGGER=false  # Disable in production for security
ENABLE_REDOC=false    # Disable in production for security
ENABLE_METRICS=true

# Truth Protocol Compliance Notes:
# - All secrets must be AES-256-GCM encrypted at rest
# - JWT tokens use HS256 with 15-minute expiry
# - Passwords hashed with Argon2id (primary) / Bcrypt (fallback)
# - Rate limiting: 5 req/min for auth endpoints
# - Audit logging enabled for all security events
