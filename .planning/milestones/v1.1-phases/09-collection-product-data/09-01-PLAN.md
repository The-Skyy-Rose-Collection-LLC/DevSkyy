---
phase: 09-collection-product-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wordpress-theme/skyyrose-flagship/template-parts/collection-page-v4.php
  - wordpress-theme/skyyrose-flagship/inc/product-catalog.php
  - wordpress-theme/skyyrose-flagship/data/products.csv
autonomous: true
requirements: [DATA-02, DATA-03]

must_haves:
  truths:
    - "Pre-order products do not appear in the catalog grid on any collection page"
    - "The PHP catalog is_preorder flags match the authoritative pre-order list from project memory"
    - "The CSV products.csv matches the PHP catalog (all 31 products present, deleted SKUs removed, pre-order flags correct)"
    - "Non-pre-order published products still appear in the catalog grid"
  artifacts:
    - path: "wordpress-theme/skyyrose-flagship/template-parts/collection-page-v4.php"
      provides: "Pre-order filtering in both WooCommerce and fallback catalog paths"
      contains: "is_preorder"
    - path: "wordpress-theme/skyyrose-flagship/inc/product-catalog.php"
      provides: "Corrected is_preorder flags for all 31 products"
      contains: "is_preorder"
    - path: "wordpress-theme/skyyrose-flagship/data/products.csv"
      provides: "Synced CSV with all products including br-d01-d04, sg-d01-d04, no sg-007/sg-008"
  key_links:
    - from: "wordpress-theme/skyyrose-flagship/template-parts/collection-page-v4.php"
      to: "wordpress-theme/skyyrose-flagship/inc/product-catalog.php"
      via: "skyyrose_get_collection_products() call and is_preorder field"
      pattern: "is_preorder"
---

<objective>
Filter pre-order products out of live collection catalog grids and fix product data integrity across the PHP catalog and CSV export.

Purpose: Pre-order products (br-004, br-005, br-006, br-d01-d04, lh-001, sg-001, sg-d01) should only appear on the pre-order gateway page, not in the regular collection catalog grids. The CSV export file is also out of sync with the authoritative PHP catalog.
Output: Updated collection-page-v4.php with pre-order filtering, corrected product-catalog.php, and synced products.csv.
</objective>

<execution_context>
@/Users/theceo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theceo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@wordpress-theme/skyyrose-flagship/template-parts/collection-page-v4.php
@wordpress-theme/skyyrose-flagship/inc/product-catalog.php
@wordpress-theme/skyyrose-flagship/data/products.csv
@wordpress-theme/skyyrose-flagship/inc/interactive-grid.php

<interfaces>
<!-- Key functions and data structures the executor needs -->

From inc/product-catalog.php:
```php
function skyyrose_get_collection_products( $collection ) // Returns array of product arrays
function skyyrose_get_product_catalog()                  // Returns full catalog keyed by SKU
function skyyrose_get_preorder_products()                // Returns preorder products by collection
```

From template-parts/collection-page-v4.php:
```php
// WooCommerce path (line 53-93): wc_get_products() with category filter
// Fallback catalog path (line 96-129): skyyrose_get_collection_products()
// Both paths build $all_products array used by the catalog grid
// $all_products[] items have 'is_preorder' key (bool)
```

Authoritative pre-order SKUs (from project memory -- NON-NEGOTIABLE):
- BR: br-004, br-005, br-006, br-d01, br-d02, br-d03, br-d04
- LH: lh-001 ONLY
- SG: sg-001, sg-d01
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix product data and filter pre-orders from catalog grid</name>
  <files>
    wordpress-theme/skyyrose-flagship/template-parts/collection-page-v4.php
    wordpress-theme/skyyrose-flagship/inc/product-catalog.php
  </files>
  <action>
**Step 1 — Fix is_preorder flags in product-catalog.php:**

The following SKUs have INCORRECT is_preorder values that conflict with the authoritative pre-order list. Fix them:

- br-002 (BLACK Rose Joggers): Currently `is_preorder => false` -- CORRECT, leave as-is.
  NOTE: The CSV has `_is_preorder=1` for br-002 but the PHP catalog says false. Per project memory rules "user verbal override > CSV", the PHP catalog is authoritative. br-002 is NOT a pre-order.
- br-007 (Basketball Shorts): Currently `is_preorder => false` -- CORRECT, leave as-is.
  NOTE: CSV has `_is_preorder=1` but PHP says false. br-007 is NOT a pre-order per memory.
- lh-002 (Love Hurts Joggers): Currently `is_preorder => false` -- CORRECT, leave as-is.
- lh-003 (Love Hurts Basketball Shorts): Currently `is_preorder => false` -- CORRECT, leave as-is.

All PHP catalog is_preorder flags are already correct per the authoritative list. No changes needed to product-catalog.php is_preorder values.

**Step 2 — Filter pre-orders from the catalog grid in collection-page-v4.php:**

After the `$all_products` array is fully built (after line 129, after both the WooCommerce and fallback paths), add a filter to separate pre-order products from catalog-display products:

```php
/* Separate pre-order products from the catalog grid.
 * Pre-orders are shown on the pre-order gateway page, not here. */
$catalog_products_display = array();
$preorder_products_list   = array();

foreach ( $all_products as $p ) {
    if ( ! empty( $p['is_preorder'] ) ) {
        $preorder_products_list[] = $p;
    } else {
        $catalog_products_display[] = $p;
    }
}
```

Then update the dynamic meta section (around line 132) to use `$catalog_products_display` for the count and price range instead of `$all_products`. Recalculate `$product_count`, `$min_price`, `$max_price` from `$catalog_products_display` only.

Update the featured product lookup (around line 138) -- the featured product can still come from `$all_products` (pre-order featured products like br-006 should still show in the featured section). Leave this as-is.

Update the catalog grid rendering: Where `skyyrose_render_interactive_grid( $all_products, $col )` is called (line 342), change to `skyyrose_render_interactive_grid( $catalog_products_display, $col )`. Similarly update the fallback legacy grid loop (line 347) to iterate over `$catalog_products_display` instead of `$all_products`.

Update the `wp_localize_script` call at the end (line 470-474) to pass `$catalog_products_display` instead of `$all_products` for the quick-view modal data (pre-orders should not be in the modal data for the catalog page).

Keep the interactive cards localization (line 477-486) also using `$catalog_products_display`.

IMPORTANT: Do NOT change the featured product section -- if the featured SKU is a pre-order (e.g., br-006 for Black Rose), it should still display as a featured product. Only the catalog GRID should exclude pre-orders.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && php -l wordpress-theme/skyyrose-flagship/template-parts/collection-page-v4.php && php -l wordpress-theme/skyyrose-flagship/inc/product-catalog.php</automated>
  </verify>
  <done>
    - Pre-order products are filtered out of $catalog_products_display before reaching the catalog grid
    - The catalog grid (both interactive and fallback) renders only non-pre-order products
    - Featured product section still works with pre-order products (e.g., br-006)
    - Product count and price range in the meta section reflect only catalog-display products
    - PHP syntax is valid for both modified files
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync products.csv with authoritative PHP catalog</name>
  <files>wordpress-theme/skyyrose-flagship/data/products.csv</files>
  <action>
Rewrite products.csv to match the PHP product-catalog.php exactly. The CSV is used for WooCommerce product import. Current issues:

1. **Missing products:** br-d01, br-d02, br-d03, br-d04, sg-d01, sg-d02, sg-d03, sg-d04, kids-001, kids-002 are in the PHP catalog but absent from the CSV. Add them.

2. **Deleted products still present:** sg-007 (The Signature Beanie) and sg-008 (Signature Crop Hoodie) are DELETED per project memory but still in the CSV. Remove them. Also remove lh-005 (Love Hurts Windbreaker) which is DELETED per project memory.

3. **Incorrect pre-order flags:** The CSV has `_is_preorder=1` for products that are NOT pre-orders in the authoritative list:
   - br-002: CSV says 1, should be 0 (not a pre-order)
   - br-007: CSV says 1, should be 0
   - lh-002: CSV says 1, should be 0
   - lh-003: CSV says 1, should be 0
   - sg-002: CSV says 1, should be 0
   - sg-003: CSV says 1, should be 0
   - sg-005: CSV says 1, should be 0
   - sg-006: CSV says 1, should be 0
   - sg-009: CSV says 1, should be 0
   - sg-010: CSV says 1, should be 0
   - sg-011: CSV says 1, should be 0
   - sg-012: CSV says 1, should be 0

4. **Incorrect published flags:** Match the PHP catalog `published` field:
   - br-001: Published=0 (draft)
   - br-003: Published=0 (draft)
   - br-008: Published=0 (draft)
   - lh-004: Published=0 (draft)
   - sg-004: Published=0 (draft)
   - All others with `published => true` should be Published=1

5. **Correct badge values:** Match PHP catalog badge field. Products with `is_preorder => true` get badge "Pre-Order". Products with `published => false` and no preorder get badge "Draft". Products with badge "NEW" (sg-d03, sg-d04) get badge "NEW".

Keep the same CSV column format: Type,SKU,Name,Published,Short description,Regular price,Categories,Attribute 1 name,Attribute 1 value(s),Attribute 1 visible,Attribute 2 name,Attribute 2 value(s),Attribute 2 visible,Meta: _is_preorder,Meta: _preorder_edition_size,Meta: _collection_badge

Order products by collection (black-rose, love-hurts, signature, kids-capsule), then by SKU within each collection.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && wc -l wordpress-theme/skyyrose-flagship/data/products.csv && head -1 wordpress-theme/skyyrose-flagship/data/products.csv && echo "---" && awk -F',' '{print $2}' wordpress-theme/skyyrose-flagship/data/products.csv | tail -n +2 | sort</automated>
  </verify>
  <done>
    - CSV contains exactly 31 product rows (+ 1 header = 32 lines)
    - All SKUs from PHP catalog are present: br-001 through br-008, br-d01 through br-d04, lh-001 through lh-004, sg-001 through sg-006, sg-009 through sg-012, sg-d01 through sg-d04, kids-001, kids-002
    - sg-007, sg-008, lh-005 are NOT present (deleted)
    - Pre-order flags match: only br-004, br-005, br-006, br-d01-d04, lh-001, sg-001, sg-d01 have _is_preorder=1
    - Published flags match PHP catalog
  </done>
</task>

</tasks>

<verification>
1. PHP syntax validation passes for all modified PHP files
2. products.csv has exactly 31 product data rows matching the PHP catalog
3. The pre-order filter logic in collection-page-v4.php correctly separates pre-order from non-pre-order products
4. Featured product section is unaffected by the pre-order filter
</verification>

<success_criteria>
- Pre-order products (br-004, br-005, br-006, br-d01-d04, lh-001, sg-001, sg-d01) are excluded from catalog grids
- Non-pre-order published products still render in the catalog grid
- products.csv is in sync with the PHP catalog (31 products, correct flags)
- No PHP syntax errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-collection-product-data/09-01-SUMMARY.md`
</output>
