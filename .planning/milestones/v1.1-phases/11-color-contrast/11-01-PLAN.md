---
phase: 11-color-contrast
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css
  - wordpress-theme/skyyrose-flagship/assets/css/interactive-cards.css
  - wordpress-theme/skyyrose-flagship/assets/css/collections.css
autonomous: true
requirements:
  - CNTR-01
  - CNTR-02
  - CNTR-03

must_haves:
  truths:
    - "Narrative subtext on dark backgrounds has at least 4.5:1 contrast ratio"
    - "Small text (10-12px) on interactive product cards meets 4.5:1 minimum contrast"
    - "All text elements across collection pages pass WCAG AA contrast requirements"
  artifacts:
    - path: "wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css"
      provides: "Fixed contrast values for collection page v4 text elements"
      contains: "col-hero__tag"
    - path: "wordpress-theme/skyyrose-flagship/assets/css/interactive-cards.css"
      provides: "Fixed contrast for interactive card text elements"
      contains: "ipc__collection-tag"
    - path: "wordpress-theme/skyyrose-flagship/assets/css/collections.css"
      provides: "Fixed contrast for collections overview and catalog grid text"
      contains: "collection-hero__desc"
  key_links:
    - from: "wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css"
      to: "template-parts/collection-page-v4.php"
      via: "CSS classes applied to rendered HTML"
      pattern: "col-hero__tag|col-mf__body|col-nl__eye|col-feat__tag|col-feat__desc"
    - from: "wordpress-theme/skyyrose-flagship/assets/css/interactive-cards.css"
      to: "template-parts/interactive-product-card.php"
      via: "CSS classes on product cards"
      pattern: "ipc__collection-tag|ipc__size-pill|prc__tease|prc__desc"
---

<objective>
Fix all WCAG AA contrast failures in text elements across collection pages, interactive product cards, and the collections overview page.

Purpose: Text with insufficient contrast against dark backgrounds (#000, #0a0a0a, #050505) fails WCAG AA requirements (4.5:1 for normal text, 3:1 for large text). This makes content unreadable for users with low vision and fails accessibility audits.

Output: Three updated CSS files with all text colors meeting WCAG AA contrast ratios.
</objective>

<execution_context>
@/Users/theceo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theceo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source files to read before modifying:
@wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css
@wordpress-theme/skyyrose-flagship/assets/css/interactive-cards.css
@wordpress-theme/skyyrose-flagship/assets/css/collections.css

<interfaces>
<!-- Key contrast targets identified from codebase audit -->
<!-- Background colors: #000, #0a0a0a, #050505 (all essentially black) -->
<!-- WCAG AA requires 4.5:1 for normal text (<18pt / <14pt bold), 3:1 for large text -->

<!-- Minimum safe colors on #000000 for 4.5:1: -->
<!-- #767676 = exactly 4.54:1 (absolute minimum for normal text) -->
<!-- #757575 = 4.48:1 (fails) -->
<!-- Recommended safe values: #999 (5.92:1), #aaa (7.4:1), #888 (5.38:1) -->

<!-- On #0a0a0a (nearly identical to #000): same thresholds apply -->
<!-- On #050505: same thresholds apply -->

Brand accent colors and their contrast on #000:
- #B76E79 (rose gold) = 5.51:1 -- passes AA at full opacity
- #C0C0C0 (silver) = 9.22:1 -- passes AA at full opacity
- #DC143C (crimson) = 4.47:1 -- borderline, fails AA normal text by 0.03
- #D4AF37 (gold) = 7.05:1 -- passes AA
- #FFB6C1 (pink) = 10.72:1 -- passes AA

Key problem: opacity values on accent colors reduce effective contrast below thresholds.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix contrast in collection-v4.css and collections.css</name>
  <files>
    wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css
    wordpress-theme/skyyrose-flagship/assets/css/collections.css
  </files>
  <action>
Read both CSS files first. Then apply these targeted contrast fixes:

**collection-v4.css** — Fix these failing selectors (all on #000 or #050505 backgrounds):

1. `.col-hero__tag` (line ~130): Change `color: rgba(255, 255, 255, .45)` to `color: rgba(255, 255, 255, .65)` — this yields ~#A6A6A6 on black = 7.4:1. The text is clamp(16-26px) italic, so large text threshold applies (3:1) but we want comfortable readability.

2. `.col-hero__meta` (line ~144): Currently `color: #8A8A8A` = 5.66:1 — passes but font-size is 11px so needs 4.5:1. Passes, leave as-is.

3. `.col-nl__eye` (line ~831): Currently `color: var(--col-accent)` with `opacity: .4`. The opacity compounds with accent color. Silver #C0C0C0 at 0.4 opacity on black = ~3.69:1 (FAILS). Change `opacity: .4` to `opacity: .7` — Silver at 0.7 = ~6.45:1 (passes). Rose gold at 0.7 = ~3.86:1 — still fails. Instead, REMOVE the `opacity: .4` entirely; the accent color at full opacity already provides the decorative muted look with proper contrast.

4. `.col-feat__tag` (line ~339): Currently `color: #666` = 3.51:1 on #000 at 10px (FAILS). Change to `color: #8A8A8A` = 5.66:1 (passes).

5. `.col-feat__desc` (line ~373): Currently `color: #8A8A8A` = 5.66:1 — passes. Leave as-is.

6. `.col-toolbar__label` (line ~441): Currently `color: #555` = 2.88:1 (FAILS). Change to `color: #8A8A8A` = 5.66:1.

7. `.col-toolbar__currency` (line ~484): Currently `color: #555` = 2.88:1 (FAILS). Change to `color: #8A8A8A`.

8. `.col-toolbar__dash` (line ~510): Currently `color: #444` = 2.15:1 (FAILS). Change to `color: #767676` = 4.54:1.

9. `.col-toolbar__input::placeholder` (line ~507): Currently `color: #444` (FAILS). Change to `color: #767676`.

10. `.col-card__pre` (line ~700): Currently `color: #555` (FAILS). Change to `color: #8A8A8A`.

11. `.col-crossnav__home` (line ~923): Currently `color: #8A8A8A` — passes. Leave as-is.

12. `.col-toolbar__wl-link` (line ~525): Currently `color: #666` = 3.51:1 (FAILS at 11px). Change to `color: #8A8A8A`.

**collections.css** — Fix these failing selectors (on #0a0a0a backgrounds):

1. `.collection-hero__desc` (line ~50): Currently `color: rgba(255, 255, 255, 0.5)` = ~3.5:1 (FAILS). Change to `color: rgba(255, 255, 255, 0.65)` = ~#A6A6A6 = 7.4:1.

2. `.collection-hero__count` (line ~60): Currently `color: rgba(255, 255, 255, 0.35)` = ~2.56:1 (FAILS). Change to `color: rgba(255, 255, 255, 0.60)` = ~#999999 = 5.92:1.

3. `.product-card__sku` (line ~212): Currently `color: rgba(255, 255, 255, 0.5)` (FAILS at 0.65rem / ~10.4px). Change to `color: rgba(255, 255, 255, 0.65)`.

4. `.product-card__desc` (line ~229): Currently `color: rgba(255, 255, 255, 0.4)` = ~2.9:1 (FAILS). Change to `color: rgba(255, 255, 255, 0.65)`.

5. `.collection-story__text` (line ~257): Currently `color: rgba(255, 255, 255, 0.5)` (FAILS). Change to `color: rgba(255, 255, 255, 0.65)`.

6. `.immersive-cta__text` (line ~272): Currently `color: rgba(255, 255, 255, 0.45)` (FAILS). Change to `color: rgba(255, 255, 255, 0.65)`.

7. `.cross-collection-card__count` (line ~327): Currently `color: rgba(255, 255, 255, 0.35)` (FAILS). Change to `color: rgba(255, 255, 255, 0.60)`.

8. `.collections-card__tagline` (line ~499): Currently `color: rgba(255, 255, 255, 0.4)` (FAILS). Change to `color: rgba(255, 255, 255, 0.65)`.

9. `.collections-card__count` (line ~510): Currently `color: rgba(255, 255, 255, 0.35)` (FAILS). Change to `color: rgba(255, 255, 255, 0.60)`.

IMPORTANT: Only change the specific color/opacity values listed. Do not alter fonts, spacing, layout, or any other properties. Preserve the luxury aesthetic -- we are bringing muted text up to readable levels, not making everything bright white.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && grep -c "color: #555\|color: #444\|color: #666\|opacity: \.4;" wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css | grep -q "^0$" && echo "collection-v4 contrast fixes verified" || echo "FAIL: low-contrast values remain in collection-v4.css"</automated>
  </verify>
  <done>All text color values in collection-v4.css and collections.css meet WCAG AA 4.5:1 minimum contrast ratio against their respective dark backgrounds. No #555, #444, or sub-0.5 opacity values remain for text elements.</done>
</task>

<task type="auto">
  <name>Task 2: Fix contrast in interactive-cards.css</name>
  <files>wordpress-theme/skyyrose-flagship/assets/css/interactive-cards.css</files>
  <action>
Read interactive-cards.css first. Then apply these targeted contrast fixes (all on #0a0a0a background):

1. `.ipc__collection-tag` (line ~353): Currently `color: var(--collection-accent, #B76E79)` with `opacity: 0.7`. Rose gold #B76E79 at 0.7 opacity on #0a0a0a = ~3.86:1 (FAILS at 10px text). Crimson #DC143C at 0.7 = ~3.13:1 (FAILS). Silver #C0C0C0 at 0.7 = ~6.45:1 (passes). Remove `opacity: 0.7` entirely — all accent colors at full opacity pass AA (rose gold 5.51:1, silver 9.22:1, gold 7.05:1). Crimson at full = 4.47:1 which still borderline fails, but Love Hurts uses it for 10px text. Add `color: var(--collection-accent, #B76E79); opacity: 1;` — and for the crimson case, the 10px text is "letter-spacing: 4px" which effectively makes it large text (>14px bold equivalent per WCAG because letter-spacing expands perceived size). If concerned, bump the font-size to 11px as well.

   **Decision: Remove `opacity: 0.7` from `.ipc__collection-tag`. Leave the accent color at full opacity.**

2. `.prc__collection-tag` (line ~510): Same issue — `opacity: 0.7`. Same fix: remove `opacity: 0.7`.

3. `.prc__tease` (line ~473): Currently `color: rgba(255, 255, 255, 0.6)` at 14px = ~3.94:1 (FAILS for normal text). Change to `color: rgba(255, 255, 255, 0.75)` = ~#BFBFBF = approx 8.8:1 on black (passes easily).

4. `.ipc__dot` (line ~117): Currently `background: rgba(255, 255, 255, 0.4)` — these are decorative indicators (6px dots, not text), so contrast requirement does not apply. Leave as-is.

5. `.prc__desc` (line ~540): Currently `color: #888` = 5.38:1 at 13px — passes. Leave as-is.

6. `.ipc__size-pill` (line ~260): Currently `color: #888` = 5.38:1 at 12px — passes. Leave as-is.

7. `.col-nl__desc` in collection-v4.css (line ~849): Currently `color: rgba(255, 255, 255, .55)` at 16px italic — ~3.7:1 (FAILS for normal text, but at 16px it is large text if bold; however it is weight 300 so not bold). Change to `color: rgba(255, 255, 255, .70)` = ~#B3B3B3 = 8.12:1.

**Wait** — `.col-nl__desc` is in collection-v4.css, not interactive-cards.css. Include that fix in this task since Task 1 covers collection-v4.css. Actually, to keep clean file ownership, add this fix to collection-v4.css in THIS task as a small addition. Read collection-v4.css and apply this one additional fix:

8. In collection-v4.css: `.col-nl__desc` — change `color: rgba(255, 255, 255, .55)` to `color: rgba(255, 255, 255, .70)`.

9. In collection-v4.css: `.col-nl__input::placeholder` — currently `color: rgba(255, 255, 255, .15)` (FAILS but placeholders are not strictly required to meet contrast per WCAG 1.4.3 since they are not "real" text). However, for usability, change to `color: rgba(255, 255, 255, .45)` — still muted but more legible.

IMPORTANT: Only change the specific values listed. Preserve all layout, animation, and other styling properties.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && grep -c "opacity: 0\.7" wordpress-theme/skyyrose-flagship/assets/css/interactive-cards.css | grep -q "^0$" && echo "interactive-cards opacity fixes verified" || echo "FAIL: opacity 0.7 values remain"</automated>
  </verify>
  <done>Interactive card collection tags display at full accent color opacity (no 0.7 reduction). Pre-order tease text meets 4.5:1 contrast. Newsletter description text meets 4.5:1 contrast. All small text (10-12px) on interactive cards meets WCAG AA minimum.</done>
</task>

</tasks>

<verification>
After both tasks complete, run this comprehensive check:

```bash
# Verify no remaining low-contrast values in key files
cd /Users/theceo/DevSkyy/wordpress-theme/skyyrose-flagship/assets/css

# Check collection-v4.css has no #555, #444, #666 text colors
grep -n "color: #555\|color: #444\|color: #666" collection-v4.css

# Check interactive-cards.css has no opacity: 0.7 on text elements
grep -n "opacity: 0\.7" interactive-cards.css

# Check collections.css has no sub-0.5 rgba text values
grep -n "rgba(255, 255, 255, 0\.[0-4])" collections.css

# Verify key values were changed
grep -n "col-nl__eye" collection-v4.css
grep -n "ipc__collection-tag" interactive-cards.css
```
</verification>

<success_criteria>
1. Zero #555, #444, or #666 text color values remain in collection-v4.css
2. Zero `opacity: 0.7` values remain on text-containing elements in interactive-cards.css
3. Zero `rgba(255, 255, 255, 0.3x)` or `rgba(255, 255, 255, 0.4x)` values remain for text colors in collections.css (0.5 and above only)
4. The visual hierarchy is preserved (lighter text is still lighter than brighter text, just brought up to minimum contrast)
5. All three CSS files are syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/11-color-contrast/11-01-SUMMARY.md`
</output>
