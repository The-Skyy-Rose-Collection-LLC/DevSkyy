---
phase: 06-wordpress-ci-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - scripts/setup-branch-protection.sh
autonomous: true
requirements:
  - CI-03
  - CI-04
  - CI-05

must_haves:
  truths:
    - "A PHP syntax error in any theme file causes CI to fail"
    - "CI runs npm run build for the WordPress theme and the build step passes"
    - "If a developer edits a source CSS/JS file without rebuilding, CI fails with a minification drift error"
    - "Branch protection requires the new WordPress Theme check to pass before merge"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "wordpress-theme CI job with PHP lint, build, and drift detection"
      contains: "wordpress-theme"
    - path: "scripts/setup-branch-protection.sh"
      provides: "Branch protection with 5 required status checks"
      contains: "WordPress Theme"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "wordpress-theme/skyyrose-flagship/package.json"
      via: "npm install && npm run build"
      pattern: "npm run build"
    - from: ".github/workflows/ci.yml"
      to: "wordpress-theme/skyyrose-flagship/scripts/verify-build.sh"
      via: "bash scripts/verify-build.sh"
      pattern: "verify-build.sh"
    - from: "scripts/setup-branch-protection.sh"
      to: "GitHub API branch protection"
      via: "gh api PUT with 5 required checks"
      pattern: "WordPress Theme"
---

<objective>
Add a WordPress Theme CI job that validates PHP syntax, runs the build pipeline, and detects minification drift -- then update branch protection to require this new check.

Purpose: CI catches PHP errors and stale minified files in the WordPress theme before they can be merged to main.
Output: Updated ci.yml with wordpress-theme job, updated branch protection script requiring 5 checks.
</objective>

<execution_context>
@/Users/theceo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theceo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-wordpress-ci-integration/06-RESEARCH.md

<interfaces>
<!-- Key files the executor needs to modify. Extracted from codebase. -->

From .github/workflows/ci.yml (structure):
- 6 existing parallel jobs: lint, python-tests, security, frontend-tests, threejs-tests, e2e-tests (conditional)
- 3 downstream jobs: api-integration, docker-build, deploy-staging, deploy-production
- 1 summary job: needs [lint, python-tests, security, frontend-tests, threejs-tests]
- env.NODE_VERSION: "22"
- All jobs use: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 (v4)
- All jobs use: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 (v6.2.0)

From scripts/setup-branch-protection.sh:
- OWNER="The-Skyy-Rose-Collection-LLC", REPO="DevSkyy", BRANCH="main"
- apply_protection(): writes JSON payload to temp file, PUT via gh api
- verify(): checks count = "4", strict, enforce_admins, force_pushes, auto_merge
- 4 current required checks: Lint & Static Analysis, Python Tests, Security Scan, Frontend Tests
- Uses temp file for JSON payload (not heredoc stdin) to guarantee emoji encoding

From wordpress-theme/skyyrose-flagship/package.json:
- "build": "npm run build:js && npm run build:css"
- package-lock.json is gitignored (use `npm install` not `npm ci`)

From wordpress-theme/skyyrose-flagship/scripts/verify-build.sh:
- 7 checks: JS count, CSS count, source maps, JS map pairing, CSS map pairing, theme header, size spot check
- Exit 0 on all pass, exit 1 on any failure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wordpress-theme CI job to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add a new job `wordpress-theme` to `.github/workflows/ci.yml` between the existing `threejs-tests` job and the `e2e-tests` job. The job structure:

```yaml
wordpress-theme:
  name: "WordPress Theme"
  runs-on: ubuntu-latest
  timeout-minutes: 10
  steps:
```

Steps (7 total):

1. **Checkout code** -- `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683` (v4, same pin as all other jobs)

2. **Set up Node.js** -- `actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238` (v6.2.0) with `node-version: ${{ env.NODE_VERSION }}`. Do NOT set `cache-dependency-path` since the theme's `package-lock.json` is gitignored.

3. **PHP syntax check (CI-03)** -- Run from repo root (`working-directory` not set or set to `.`). Loop over PHP files found by `find wordpress-theme/skyyrose-flagship -name "*.php" -not -path "*/node_modules/*" -not -path "*/vendor/*"`. For each file, run `php -l "$file"` and check output contains "No syntax errors". On failure, emit `::error file=$file::PHP syntax error`. After loop, if error_count > 0, print count and `exit 1`. Use process substitution `< <(find ...)` for the loop (not pipe, which would lose variable scope).

4. **Install theme dependencies** -- `working-directory: wordpress-theme/skyyrose-flagship`, run `npm install` (NOT `npm ci` -- the theme's `package-lock.json` is gitignored per Phase 5 decision).

5. **Build theme assets (CI-04)** -- `working-directory: wordpress-theme/skyyrose-flagship`, run `npm run build`.

6. **Verify build output (CI-04)** -- `working-directory: wordpress-theme/skyyrose-flagship`, run `bash scripts/verify-build.sh`. This script already exists from Phase 5 with 7 checks.

7. **Check for minification drift (CI-05)** -- Run from repo root. Execute `git diff --exit-code -- 'wordpress-theme/skyyrose-flagship/**/*.min.js' 'wordpress-theme/skyyrose-flagship/**/*.min.css'`. If non-zero exit: print `::error::Minification drift detected!`, print fix instructions (`cd wordpress-theme/skyyrose-flagship && npm run build`), and `exit 1`. If zero exit: print "No minification drift".

Also update the `summary` job:
- Add `wordpress-theme` to the `needs` array: `needs: [lint, python-tests, security, frontend-tests, threejs-tests, wordpress-theme]`
- Add a summary table row: `echo "| WordPress Theme | ${{ needs.wordpress-theme.result }} |" >> $GITHUB_STEP_SUMMARY`

IMPORTANT anti-patterns to avoid:
- Do NOT use `continue-on-error: true` on any step (Phase 1 removed all 17 instances)
- Do NOT use `setup-php` action (PHP 8.3.6 is pre-installed on ubuntu-latest)
- Do NOT use `npm ci` for the theme (package-lock.json is gitignored)
- Do NOT set `defaults.run.working-directory` at the job level (PHP lint needs repo root, build needs theme dir)
- Use emoji-free job name `"WordPress Theme"` to avoid potential encoding issues with branch protection matching. The existing jobs use emoji BUT Phase 4 used a temp-file approach specifically to handle emoji. Since this is a new job, use a clean ASCII name for reliability. Wait -- the research recommends using emoji for consistency. Use the emoji name from research: the job `name:` field should be exactly the string that branch protection will match. Set `name: "WordPress Theme"` (no emoji) to keep it simple and avoid the encoding pitfall documented in the research. The research example shows emoji but also warns about Pitfall 5 (exact string matching). Since the executor will update branch protection in Task 2, using ASCII is safer.

Actually -- to stay consistent with the existing 4 jobs that ALL use emoji, use the emoji name. The Phase 4 temp-file approach handles this. Set `name: "WordPress Theme"` with the construction-worker emoji as shown in the research.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python3 -c "
import yaml, sys
with open('.github/workflows/ci.yml') as f:
    ci = yaml.safe_load(f)
jobs = ci.get('jobs', {})
# Check wordpress-theme job exists
assert 'wordpress-theme' in jobs, 'wordpress-theme job missing'
wt = jobs['wordpress-theme']
# Check it has steps
steps = wt.get('steps', [])
step_names = [s.get('name', '') for s in steps]
assert any('PHP' in n for n in step_names), 'PHP syntax check step missing'
assert any('Build' in n or 'build' in n for n in step_names), 'Build step missing'
assert any('drift' in n.lower() or 'minif' in n.lower() for n in step_names), 'Drift check step missing'
assert any('Verify' in n or 'verify' in n for n in step_names), 'Verify build step missing'
# Check summary job includes wordpress-theme
summary = jobs.get('summary', {})
needs = summary.get('needs', [])
assert 'wordpress-theme' in needs, f'summary.needs missing wordpress-theme: {needs}'
print('All ci.yml checks passed')
" 2>&1 || echo "Validation script requires PyYAML -- fallback:" && grep -c 'wordpress-theme' .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml contains a wordpress-theme job with PHP lint (CI-03), build + verify (CI-04), and drift detection (CI-05) steps. Summary job includes the new job in its needs array.</done>
</task>

<task type="auto">
  <name>Task 2: Update branch protection to require 5 checks</name>
  <files>scripts/setup-branch-protection.sh</files>
  <action>
Update `scripts/setup-branch-protection.sh` in two places:

1. **apply_protection() function -- Add 5th check to the JSON payload:**
   Add `{"context": "WordPress Theme", "app_id": -1}` to the `checks` array (use the exact same string as the CI job's `name:` field). The payload should now have 5 entries in the `checks` array.

   IMPORTANT: Keep using the temp-file approach for the JSON payload (do not switch to heredoc stdin). This was a Phase 4 decision to guarantee emoji encoding. Even though the new check name may be ASCII, maintain the pattern.

2. **verify() function -- Update expected check count from 4 to 5:**
   Change `if [ "$check_count" = "4" ]` to `if [ "$check_count" = "5" ]`.
   Change the pass message from `"$check_count/4 configured"` to `"$check_count/5 configured"`.
   Change the fail message from `"expected 4"` to `"expected 5"`.
   Update the final success message from `"All 5 checks PASSED"` -- it already says "All 5 checks" if there are 5 verification checks. Actually, the verify function has 5 verification checks (status count, strict, enforce_admins, force_pushes, auto_merge). The "All 5 checks PASSED" refers to the 5 verification checks, not the 5 required status checks. So only update Check 1's expected count from 4 to 5.

Do NOT run the script -- it requires `gh` CLI auth and modifies the live GitHub repo. The executor should only edit the file.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && grep -c '"WordPress Theme"' scripts/setup-branch-protection.sh | grep -q '1' && grep -q '"5"' scripts/setup-branch-protection.sh && grep -q '5/5' scripts/setup-branch-protection.sh && echo "Branch protection script updated correctly" || echo "FAIL: script not updated"</automated>
  </verify>
  <done>Branch protection script requires 5 status checks (the original 4 + WordPress Theme). Verify function expects count of 5.</done>
</task>

</tasks>

<verification>
1. `ci.yml` YAML is valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`
2. `wordpress-theme` job exists with PHP lint, build, verify-build, and drift detection steps
3. Summary job `needs` array includes `wordpress-theme`
4. Branch protection script payload has 5 checks
5. Branch protection verify function expects count "5"
6. Local PHP lint passes: `find wordpress-theme/skyyrose-flagship -name "*.php" -not -path "*/node_modules/*" -not -path "*/vendor/*" -exec php -l {} + 2>&1 | tail -1`
7. Local build passes: `cd wordpress-theme/skyyrose-flagship && npm install && npm run build && bash scripts/verify-build.sh`
</verification>

<success_criteria>
- ci.yml contains a `wordpress-theme` job that runs PHP syntax checking on all theme PHP files (CI-03)
- ci.yml wordpress-theme job runs `npm run build` and `verify-build.sh` (CI-04)
- ci.yml wordpress-theme job runs `git diff --exit-code` on minified files after build (CI-05)
- Branch protection script requires 5 status checks including "WordPress Theme"
- No `continue-on-error: true` introduced in any step
</success_criteria>

<output>
After completion, create `.planning/phases/06-wordpress-ci-integration/06-01-SUMMARY.md`
</output>
