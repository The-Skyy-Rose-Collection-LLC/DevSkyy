---
phase: 15-ghost-mannequin-agent-qa
plan: "06"
type: execute
wave: 3
depends_on:
  - "15-03"
  - "15-04"
  - "15-05"
files_modified:
  - skyyrose/elite_studio/agents/ghost_mannequin_agent.py
  - skyyrose/elite_studio/graph/builder.py
  - skyyrose/elite_studio/graph/edges.py
autonomous: true
requirements:
  - GM-01
  - GM-02
  - GM-04
  - QA-02

must_haves:
  truths:
    - "ghost_mannequin_node LangGraph node is registered in builder.py behind enable_ghost_mannequin_agent flag"
    - "RunBudget is checked before every paid API dispatch; agent halts with clear message if projected total exceeds cap"
    - "dry-run mode produces a full cost manifest with zero API calls"
    - "Every generation attempt (success or fail) writes one line to the JSONL audit log"
    - "failures.json receives an appended entry for every veto-fail that exhausts all cascade options"
    - "API keys (TRIPO3D_API_KEY, GOOGLE_API_KEY, OPENAI_API_KEY, IDEOGRAM_API_KEY) never appear in JSONL or failures.json output"
    - "Retryable errors (429, 5xx) use exponential backoff; non-retryable errors (safety block, content filter) write to failures.json immediately"
  artifacts:
    - path: "skyyrose/elite_studio/agents/ghost_mannequin_agent.py"
      provides: "GhostMannequinAgent class + ghost_mannequin_node LangGraph node function"
      contains: "ghost_mannequin_node"
    - path: "skyyrose/elite_studio/graph/builder.py"
      provides: "enable_ghost_mannequin_agent flag in GraphConfig + node registration"
      contains: "enable_ghost_mannequin_agent"
    - path: "skyyrose/elite_studio/graph/edges.py"
      provides: "conditional edge wiring for ghost_mannequin_node"
      contains: "ghost_mannequin"
  key_links:
    - from: "skyyrose/elite_studio/agents/ghost_mannequin_agent.py"
      to: "skyyrose/elite_studio/graph/state.py"
      via: "EliteStudioState fields ghost_mannequin_agent_result, gm_audit_log_path"
      pattern: "ghost_mannequin_agent_result"
    - from: "skyyrose/elite_studio/agents/ghost_mannequin_agent.py"
      to: "skyyrose/elite_studio/synthesis/ghost_mannequin_synthesizer.py"
      via: "GhostMannequinSynthesizer.synthesize_ensemble()"
      pattern: "GhostMannequinSynthesizer"
    - from: "skyyrose/elite_studio/agents/ghost_mannequin_agent.py"
      to: "skyyrose/elite_studio/agents/quality_agent.py"
      via: "QualityAgent.score_ghost_mannequin()"
      pattern: "score_ghost_mannequin"
    - from: "skyyrose/elite_studio/graph/builder.py"
      to: "skyyrose/elite_studio/agents/ghost_mannequin_agent.py"
      via: "ghost_mannequin_node registered via builder.add_node"
      pattern: "ghost_mannequin_node"
---

<objective>
Create the core LangGraph node implementation: GhostMannequinAgent class, ghost_mannequin_node function, RunBudget integration, dry-run mode, JSONL audit emitter, failures.json writer, retry/cascade orchestration, and graph wiring in builder.py and edges.py.

Purpose: This is the central orchestration plan — it connects every prior plan's output (data contracts from 15-01, SKU resolver from 15-02, .glb cache from 15-03, synthesis pipeline from 15-04, QA vetoes from 15-05) into a single functioning LangGraph node. GM-01, GM-02, GM-04, and QA-02 are all finalized here.
Output: ghost_mannequin_agent.py (new), builder.py and edges.py (extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-CONTEXT.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-01-PLAN.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-03-PLAN.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-04-PLAN.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-05-PLAN.md

<interfaces>
<!-- From 15-01 (models.py):
  GhostMannequinAgentResult: sku, output_path, passed_qa, tier_used, total_cost_usd, score_breakdown, audit_log_path
  FailureEntry: sku, attempt_number, timestamp, stage, failure_class, error_message, score_breakdown
  AuditEntry: run_id, sku, timestamp, tier, cost_usd, passed_qa, score_breakdown, failure_class
-->
<!-- From 15-01 (graph/state.py):
  EliteStudioState (total=False TypedDict):
    ghost_mannequin_agent_result: GhostMannequinAgentResult
    gm_audit_log_path: str
    (plus existing fields: preflight_result, budget, run_summary_path, ghost_mannequin_front_path, etc.)
-->
<!-- From 15-02 (sku_resolver.py):
  sanitize_sku(raw: str) -> str
  resolve_sku(raw_sku: str) -> ResolvedSKU | None
  verify_tripo_region() -> bool
  ResolvedSKU: sku, name, garment_type, bundle_dir, manifest_path, branding_spec, is_jersey
-->
<!-- From 15-03 (glb_cache.py):
  GLBCacheManager: exists, get, put, invalidate
  TripoDigitizer.digitize(sku, flatlay_path, tmp_dir) -> Path
  _GHOST_MANNEQUIN_EST_COST_USD = 0.15 (from graph/nodes.py)
-->
<!-- From 15-04:
  PromptRegistry.get(garment_type, garment_name, branding_spec, seed, temperature) -> str
  GhostMannequinSynthesizer.synthesize_ensemble(...) -> list[CandidateImage]
  GhostMannequinSynthesizer.jersey_cascade(...) -> CandidateImage | None
  GhostMannequinSynthesizer.non_jersey_retry(...) -> CandidateImage | None
  CandidateImage: image_bytes, tier, seed, temperature, cost_usd
-->
<!-- From 15-05:
  QualityAgent.score_ghost_mannequin(candidate_bytes, flatlay_bytes, techflat_bytes, sku, branding_spec) -> VetoResult
  VetoResult: passed, silhouette_iou, color_delta_e, clip_score, logo_drift_pct, corner_purity, corner_passed, pattern_passed, vetoes_failed
-->
<!-- From existing graph/builder.py (per prior session reads):
  GraphConfig frozen dataclass with existing flags:
    enable_ghost_mannequin_preflight: bool = True
    enable_ghost_mannequin_composite: bool = True
    enable_3d: bool = False
  builder uses StateGraph from langgraph
-->
<!-- From existing budget.py (per CONTEXT.md):
  RunBudget dataclass with cap_usd field and spent_usd tracking
  Reuse — do NOT reimplement
-->
<!-- Manifest.json schema (per CONTEXT.md canonical_refs):
  {sku, name, dirname, collection, files: {techflat-front, techflat-back, source-photo, logo-ref, spec}}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ghost_mannequin_agent.py with GhostMannequinAgent and ghost_mannequin_node</name>
  <files>skyyrose/elite_studio/agents/ghost_mannequin_agent.py</files>
  <read_first>
    Read skyyrose/elite_studio/agents/three_d_agent.py lines 1-60 — confirm CreativeAgent import path, ADK SuperAgent pattern, and __init__ signature used in this codebase.
    Read skyyrose/elite_studio/budget.py in full — confirm RunBudget class, its fields (cap_usd, spent_usd), and any check_budget() or add_cost() methods.
    Read skyyrose/elite_studio/graph/builder.py lines 1-80 — confirm GraphConfig dataclass fields and how existing nodes are registered.
    Read skyyrose/elite_studio/graph/state.py lines 1-50 — confirm create_initial_state() signature.
    Check if skyyrose/elite_studio/agents/ghost_mannequin_agent.py already exists.
  </read_first>
  <action>
    Create skyyrose/elite_studio/agents/ghost_mannequin_agent.py as a new file.

    MODULE LAYOUT (in this order):

    1. Module docstring: "Ghost Mannequin Agent — LangGraph node for single-SKU ghost-mannequin front shot generation."

    2. Imports (module level):
       asyncio, contextlib, dataclasses, datetime, io, json, logging, os, pathlib, re, tempfile, time, typing
       PIL.Image (from PIL import Image)
       From project modules (lazy-imported inside methods where marked):
         - from skyyrose.elite_studio.models import GhostMannequinAgentResult, FailureEntry, AuditEntry
         - from skyyrose.elite_studio.graph.state import EliteStudioState
         - from skyyrose.elite_studio.sku_resolver import sanitize_sku, resolve_sku, verify_tripo_region, ResolvedSKU
         - from skyyrose.elite_studio.glb_cache import GLBCacheManager, TripoDigitizer
         - from skyyrose.elite_studio.prompts.ghost_mannequin import PromptRegistry
         - from skyyrose.elite_studio.synthesis.ghost_mannequin_synthesizer import GhostMannequinSynthesizer, CandidateImage
         - from skyyrose.elite_studio.agents.quality_agent import QualityAgent, VetoResult
         - from skyyrose.elite_studio.graph.nodes import _GHOST_MANNEQUIN_EST_COST_USD
         - from skyyrose.elite_studio.budget import RunBudget
       ADK / CreativeAgent (lazy import to match ThreeDAgent pattern — verify exact path in read_first):
         from skyyrose.elite_studio.agents.three_d_agent import CreativeAgent  (or whatever base class ThreeDAgent uses)

    3. Module constants:
       - _FAILURES_JSON = pathlib.Path("renders/ghost-mannequin/failures.json")
       - _AUDIT_DIR = pathlib.Path("renders/ghost-mannequin/audit")
       - _OUTPUT_DIR = pathlib.Path("renders/ghost-mannequin")
       - _GLB_CACHE_DIR = pathlib.Path("renders/3d/cache")
       - _DEFAULT_SPEND_CAP_USD = 25.0
       - _GHOST_MAX_SPEND_ENV = "GHOST_MAX_SPEND_USD"
       - _KEY_STRIP_RE: compiled re.Pattern to detect API key fragments in strings:
         re.compile(r'(?i)(sk-[a-zA-Z0-9]{20,}|AIza[a-zA-Z0-9]{35,}|Bearer\s+[a-zA-Z0-9]{20,})')
       - logger = logging.getLogger(__name__)

    4. _sanitize_error_message(msg: str) -> str:
       - Replace any substring matching _KEY_STRIP_RE with "[REDACTED]"
       - Return sanitized string
       - Used in all FailureEntry.error_message and AuditEntry writes

    5. GhostMannequinAgent class (inherits CreativeAgent for ADK telemetry):
       - __init__(self, spend_cap_usd: float | None = None, dry_run: bool = False):
         - self._spend_cap = spend_cap_usd or float(os.environ.get(_GHOST_MAX_SPEND_ENV, _DEFAULT_SPEND_CAP_USD))
         - self._dry_run = dry_run
         - self._cache = GLBCacheManager(_GLB_CACHE_DIR)
         - self._digitizer = TripoDigitizer(self._cache)
         - self._synthesizer = GhostMannequinSynthesizer()
         - self._prompt_registry = PromptRegistry()
         - self._qa = QualityAgent()
         - _OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
         - _AUDIT_DIR.mkdir(parents=True, exist_ok=True)

       - _check_budget(self, budget: RunBudget, next_cost_usd: float, stage: str) -> None:
         - projected = budget.spent_usd + next_cost_usd
         - if projected > self._spend_cap:
           - raise RuntimeError(f"Spend cap ${self._spend_cap:.2f} would be exceeded by {stage} (projected: ${projected:.2f}, spent: ${budget.spent_usd:.2f})")

       - _write_audit_entry(self, audit_path: pathlib.Path, entry: AuditEntry) -> None:
         - Serialise entry as JSON, sanitize with _sanitize_error_message, append newline to audit_path
         - entry_dict = dataclasses.asdict(entry)
         - line = json.dumps(entry_dict) + "\n"
         - with audit_path.open("a", encoding="utf-8") as f: f.write(line)

       - _append_failure(self, sku: str, attempt: int, stage: str, failure_class: str, error_message: str, score_breakdown: dict) -> None:
         - Build FailureEntry dict
         - Read existing failures.json if present (json.loads), else start with {}
         - Key: sku. Value: latest attempt entry (overwrites prior attempt per D-13)
         - Write updated JSON back to _FAILURES_JSON
         - error_message passed through _sanitize_error_message() before storing

       - _load_manifest(self, resolved: ResolvedSKU) -> dict:
         - Read resolved.manifest_path as JSON
         - Validate required keys: "files" must contain "source-photo" and "techflat-front"
         - Return parsed dict; raise ValueError if keys missing

       - async def run(self, sku_raw: str, budget: RunBudget, run_id: str, audit_path: pathlib.Path) -> GhostMannequinAgentResult:
         - resolved = resolve_sku(sku_raw)  (raises ValueError for unknown SKU; returns None for accessory)
         - If resolved is None:
           - self._append_failure(sku_raw, 0, "accessory-skip", "accessory_skip", "Accessory SKU skipped per D-12", {})
           - raise ValueError(f"SKU {sku_raw!r} is an accessory — skipped (logged to failures.json)")
         - manifest = self._load_manifest(resolved)
         - flatlay_path = resolved.bundle_dir / manifest["files"]["source-photo"]
         - techflat_path = resolved.bundle_dir / manifest["files"]["techflat-front"]
         - If not flatlay_path.exists(): raise FileNotFoundError(f"Flatlay not found: {flatlay_path}")
         - If not techflat_path.exists(): raise FileNotFoundError(f"Techflat not found: {techflat_path}")

         - dry_run block: if self._dry_run:
           - Print cost manifest to stdout (no API calls)
           - glb_hit = self._cache.exists(resolved.sku)
           - tier1_cost = _GHOST_MANNEQUIN_EST_COST_USD * 3  (ensemble-of-3)
           - tier2_cost = 0.08 if resolved.is_jersey else 0.0  (GPT-Image-1 attempt)
           - tier3_cost = 0.10 if resolved.is_jersey else 0.0  (Ideogram attempt)
           - digitize_cost = 0.0 if glb_hit else 0.50  (Tripo charge)
           - total_est = tier1_cost + tier2_cost + tier3_cost + digitize_cost
           - Print: "DRY RUN — cost manifest for {resolved.sku}\n  SKU: {resolved.sku}\n  Garment type: {resolved.garment_type}\n  .glb cache: {'HIT' if glb_hit else 'MISS (will digitize)'}\n  Estimated cost: ${total_est:.2f}\n  Spend cap: ${self._spend_cap:.2f}\n  Remaining headroom: ${self._spend_cap - total_est:.2f}"
           - Verify Tripo balance: try verify_tripo_region(); if False: print "WARNING: Tripo region check failed"
           - return early — raise SystemExit(0) to signal clean exit to CLI caller

         - GENERATION LOOP:
           - Step 1: .glb digitization
             - self._check_budget(budget, 0.50, "tripo-digitise")  (only if cache miss)
             - with tempfile.TemporaryDirectory() as tmp: glb_path = self._digitizer.digitize(resolved.sku, flatlay_path, pathlib.Path(tmp))
             - budget.spent_usd += 0.0 if cache hit, else 0.50

           - Step 2: Tier-1 ensemble synthesis (3 Gemini RAS calls)
             - self._check_budget(budget, _GHOST_MANNEQUIN_EST_COST_USD * 3, "gemini-ras-ensemble")
             - prompt = self._prompt_registry.get(resolved.garment_type, resolved.name, resolved.branding_spec, seed=42)
             - candidates = await self._synthesizer.synthesize_ensemble(prompt, flatlay_path)
             - budget.spent_usd += sum(c.cost_usd for c in candidates)

           - Step 3: Score all candidates
             - flatlay_bytes = flatlay_path.read_bytes()
             - techflat_bytes = techflat_path.read_bytes()
             - veto_results: list[tuple[CandidateImage, VetoResult]] = []
             - for candidate in candidates:
               - veto = await self._qa.score_ghost_mannequin(candidate.image_bytes, flatlay_bytes, techflat_bytes, resolved.sku, resolved.branding_spec)
               - veto_results.append((candidate, veto))
               - Write AuditEntry (see below)
             - best_passing = [(c, v) for c, v in veto_results if v.passed]

           - Step 4: If at least one candidate passed → accept best passing
             - Select first passing candidate (all have same tier; pick first)
             - output_path = _OUTPUT_DIR / f"{resolved.sku}-ghost-front.webp"
             - output_path.write_bytes(best_candidate.image_bytes)
             - return GhostMannequinAgentResult(sku=resolved.sku, output_path=str(output_path), passed_qa=True, tier_used="gemini-ras", total_cost_usd=budget.spent_usd, score_breakdown=_veto_to_score_dict(veto), audit_log_path=str(audit_path))

           - Step 5: All Tier-1 vetoed — cascade per garment type
             - if resolved.is_jersey:
               - self._check_budget(budget, 0.18, "jersey-cascade-gpt+ideogram")
               - cascade_result = await self._synthesizer.jersey_cascade(resolved.sku, prompt, flatlay_path)
               - if cascade_result is not None:
                 - Score cascade_result with score_ghost_mannequin
                 - Write AuditEntry
                 - if veto.passed: accept and return GhostMannequinAgentResult(tier_used=cascade_result.tier, ...)
               - All cascade options exhausted → self._append_failure(resolved.sku, attempt_number, "jersey-cascade", "qa_fail", "All cascade tiers vetoed", score_dict)
             - else (non-jersey):
               - self._check_budget(budget, _GHOST_MANNEQUIN_EST_COST_USD, "non-jersey-retry")
               - retry = await self._synthesizer.non_jersey_retry(prompt, flatlay_path, seed=99)
               - if retry is not None:
                 - Score retry, write AuditEntry
                 - if veto.passed: accept and return
               - self._append_failure(resolved.sku, 2, "non-jersey-retry", "qa_fail", "Mutated retry also vetoed", score_dict)

           - Step 6: Nothing passed — raise RuntimeError so caller (ghost_mannequin_node) catches and sets state
             - raise RuntimeError(f"SKU {resolved.sku}: all generation tiers exhausted. See failures.json.")

       Helper: _veto_to_score_dict(veto: VetoResult) -> dict:
         return {
           "silhouette_iou": veto.silhouette_iou,
           "color_delta_e": veto.color_delta_e,
           "clip_score": veto.clip_score,
           "logo_drift_pct": veto.logo_drift_pct,
           "corner_purity_rgb": veto.corner_purity.get("max_distance", -1),
         }

       AuditEntry writes for each candidate:
         - entry = AuditEntry(run_id=run_id, sku=resolved.sku, timestamp=datetime.datetime.utcnow().isoformat(), tier=candidate.tier, cost_usd=candidate.cost_usd, passed_qa=veto.passed, score_breakdown=_veto_to_score_dict(veto), failure_class="" if veto.passed else ",".join(veto.vetoes_failed))
         - self._write_audit_entry(audit_path, entry)

    6. ghost_mannequin_node(state: EliteStudioState) -> EliteStudioState:
       - Module-level async-capable node function consumed by LangGraph builder
       - Extract sku from state. Expected key: "sku" (confirm with builder.py read_first)
       - Extract budget: state.get("budget") or RunBudget(cap_usd=_DEFAULT_SPEND_CAP_USD, spent_usd=0.0)
       - run_id = datetime.datetime.utcnow().strftime("%Y%m%dT%H%M%S")
       - audit_path = _AUDIT_DIR / f"{run_id}.jsonl"
       - dry_run = state.get("dry_run", False)
       - spend_cap = state.get("spend_cap", None)
       - agent = GhostMannequinAgent(spend_cap_usd=spend_cap, dry_run=dry_run)
       - Try: result = asyncio.run(agent.run(sku, budget, run_id, audit_path))
       - Except RuntimeError as exc: log error; set result fields with passed_qa=False
       - Return updated state dict: {**state, "ghost_mannequin_agent_result": result, "gm_audit_log_path": str(audit_path)}

    CRITICAL: All error_message strings written to files pass through _sanitize_error_message() which strips API key fragments via _KEY_STRIP_RE before writing.
    CRITICAL: failures.json and JSONL audit log must NEVER contain API key values. Use _sanitize_error_message() on all exception repr strings before storing.
    CRITICAL: Retry logic is bounded per D-11. Jersey: max 1 GPT + 1 Ideogram attempt. Non-jersey: max 1 retry. No while loops without explicit attempt counter and max check.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -c "
from skyyrose.elite_studio.agents.ghost_mannequin_agent import (
    GhostMannequinAgent, ghost_mannequin_node, _sanitize_error_message,
    _DEFAULT_SPEND_CAP_USD, _FAILURES_JSON, _AUDIT_DIR
)
import inspect

# Basic importability
assert _DEFAULT_SPEND_CAP_USD == 25.0
assert 'renders/ghost-mannequin' in str(_FAILURES_JSON)
assert 'audit' in str(_AUDIT_DIR)

# ghost_mannequin_node is callable
assert callable(ghost_mannequin_node)

# _sanitize_error_message strips key fragments
dirty = 'Error: sk-abc123ABCDEFGHIJ1234567890 not valid'
clean = _sanitize_error_message(dirty)
assert 'sk-abc123' not in clean
assert '[REDACTED]' in clean

# GhostMannequinAgent instantiable
agent = GhostMannequinAgent(spend_cap_usd=10.0, dry_run=True)
assert agent._spend_cap == 10.0
assert agent._dry_run == True

print('ghost_mannequin_agent OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - ghost_mannequin_agent.py importable with no API keys in environment
    - _sanitize_error_message() removes API key patterns from error strings
    - GhostMannequinAgent instantiable with spend_cap_usd and dry_run params
    - ghost_mannequin_node is a callable that accepts state dict
    - _DEFAULT_SPEND_CAP_USD == 25.0
    - No existing file modified (new file only for this task)
  </acceptance_criteria>
  <done>All assertions pass with "ghost_mannequin_agent OK" output</done>
</task>

<task type="auto">
  <name>Task 2: Add enable_ghost_mannequin_agent to GraphConfig and wire node + edges</name>
  <files>skyyrose/elite_studio/graph/builder.py, skyyrose/elite_studio/graph/edges.py</files>
  <read_first>
    Read skyyrose/elite_studio/graph/builder.py in full — confirm GraphConfig frozen dataclass fields, how existing nodes are registered (add_node pattern), and the StateGraph setup.
    Read skyyrose/elite_studio/graph/edges.py in full — confirm existing conditional edge wiring pattern and where ghost_mannequin node should plug in.
    Check if "ghost_mannequin_node" import would create a circular import (builder.py imports from nodes.py; ghost_mannequin_agent.py imports from nodes.py — check for cycle).
  </read_first>
  <action>
    Extend builder.py and edges.py. Append-only modifications — do NOT alter any existing field, node registration, or edge.

    BUILDER.PY changes (3 changes only):
    1. Add to GraphConfig frozen dataclass: enable_ghost_mannequin_agent: bool = False
       Place after the last existing enable_* field. Default False keeps existing graphs unchanged.

    2. Add import at top of builder.py (with other node imports):
       from skyyrose.elite_studio.agents.ghost_mannequin_agent import ghost_mannequin_node

    3. In the graph builder function (wherever add_node calls are made for other nodes), add:
       if config.enable_ghost_mannequin_agent:
           graph.add_node("ghost_mannequin_agent", ghost_mannequin_node)

    EDGES.PY changes (1 change only):
    1. Add conditional edge for ghost_mannequin_agent node. Follow the exact same pattern used for existing ghost_mannequin_composite or three_d node edges. Specifically:
       - After ghost_mannequin_agent node runs → route to END (this is a terminal node for Phase 15; routing to downstream nodes is Phase 18 work)
       - Only add this edge if the node was registered (guard with: if "ghost_mannequin_agent" in graph.nodes)
       - Pattern: graph.add_edge("ghost_mannequin_agent", END) or equivalent per existing pattern in edges.py

    CRITICAL: Do NOT change any existing GraphConfig field default. enable_ghost_mannequin_agent defaults to False — all existing graph instantiations remain unaffected.
    CRITICAL: Do NOT remove or alter existing add_node calls for ghost_mannequin_composite_node, three_d_node, or any other node.
    CRITICAL: If circular import is detected in read_first: break cycle by importing ghost_mannequin_node inside the conditional if config.enable_ghost_mannequin_agent block rather than at module top level.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -c "
from skyyrose.elite_studio.graph.builder import GraphConfig, build_graph

# New flag present with correct default
cfg = GraphConfig()
assert hasattr(cfg, 'enable_ghost_mannequin_agent')
assert cfg.enable_ghost_mannequin_agent == False

# Existing config unaffected
assert hasattr(cfg, 'enable_ghost_mannequin_preflight')
assert hasattr(cfg, 'enable_ghost_mannequin_composite')

# Graph builds without ghost_mannequin_agent (flag off)
g = build_graph(cfg)
assert 'ghost_mannequin_agent' not in (g.nodes if hasattr(g, 'nodes') else [])

# Graph builds WITH ghost_mannequin_agent (flag on)
cfg2 = GraphConfig(enable_ghost_mannequin_agent=True)
g2 = build_graph(cfg2)

print('builder_edges OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - GraphConfig has enable_ghost_mannequin_agent: bool = False
    - build_graph(GraphConfig()) does not register ghost_mannequin_agent node (flag default off)
    - build_graph(GraphConfig(enable_ghost_mannequin_agent=True)) registers the node
    - All existing graph tests still pass
    - No existing GraphConfig field or add_node call modified
  </acceptance_criteria>
  <done>All assertions pass with "builder_edges OK" output</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EliteStudioState["sku"] → filesystem paths | SKU value from state flows into output path renders/ghost-mannequin/{sku}-ghost-front.webp |
| FailureEntry.error_message → failures.json | Exception repr may contain API key fragments if provider SDK includes them in errors |
| AuditEntry → JSONL file | All audit fields must be free of secret material |
| RunBudget → API dispatch gate | Budget state controls whether paid calls proceed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Tampering | sku from state → output path | mitigate | Call sanitize_sku(state["sku"]) at the top of ghost_mannequin_node before any path construction; ValueError propagates to LangGraph error handler |
| T-15-06-02 | Information Disclosure | API keys in failures.json / JSONL | mitigate | All error_message strings pass through _sanitize_error_message() with _KEY_STRIP_RE before any file write; pattern strips sk-*, AIza*, and Bearer tokens from error context |
| T-15-06-03 | Denial of Service | JSONL audit file unbounded growth | accept | Each run appends one line per attempt; with ensemble-of-3 + cascade = max 5 lines per SKU per run; at 28 SKUs per Phase 18 batch = ~140 lines; files are human-readable; no size guard needed for Phase 15 single-SKU mode |
| T-15-06-04 | Denial of Service | Unbounded retry loops | mitigate | D-11 bounds: jersey max = 3 ensemble + 1 GPT + 1 Ideogram = 5 attempts; non-jersey max = 3 ensemble + 1 retry = 4 attempts; explicit attempt counter checked before each cascade step; no while loop without counter |
| T-15-06-05 | Elevation of Privilege | asyncio.run in ghost_mannequin_node | accept | LangGraph runs nodes synchronously; asyncio.run creates a new event loop for the async agent; this is the same pattern used in three_d_node. No privilege escalation risk — all API calls are outbound HTTP |
| T-15-06-06 | Tampering | failures.json overwrite race condition | accept | Phase 15 is single-SKU; no concurrent writes. Phase 18 batch (multi-SKU) must address this with file locking — deferred to that phase |
| T-15-06-07 | Repudiation | spend_cap bypass via state injection | mitigate | _check_budget() uses self._spend_cap set at __init__ time from env/arg; state["spend_cap"] overrides only through explicit agent constructor call in ghost_mannequin_node — not via arbitrary state key mutation |
</threat_model>

<verification>
Run after both tasks complete:

  cd /Users/theceo/DevSkyy && python -c "
from skyyrose.elite_studio.agents.ghost_mannequin_agent import ghost_mannequin_node, _sanitize_error_message, _DEFAULT_SPEND_CAP_USD
from skyyrose.elite_studio.graph.builder import GraphConfig, build_graph

# Spend cap default
assert _DEFAULT_SPEND_CAP_USD == 25.0

# Key sanitization
assert '[REDACTED]' in _sanitize_error_message('key=AIzaXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX')

# Builder wiring
cfg = GraphConfig()
assert not cfg.enable_ghost_mannequin_agent
cfg2 = GraphConfig(enable_ghost_mannequin_agent=True)
assert cfg2.enable_ghost_mannequin_agent
build_graph(cfg2)  # must not raise

print('Phase 15-06 verified OK')
"
</verification>

<success_criteria>
- ghost_mannequin_agent.py importable with all agent logic
- ghost_mannequin_node registered in graph when enable_ghost_mannequin_agent=True
- GraphConfig.enable_ghost_mannequin_agent defaults to False
- _sanitize_error_message strips API key patterns
- RunBudget integration: _check_budget raises before cap-crossing call
- JSONL and failures.json writers functional
- pytest tests/ -x -q passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-ghost-mannequin-agent-qa/15-06-SUMMARY.md`
</output>
