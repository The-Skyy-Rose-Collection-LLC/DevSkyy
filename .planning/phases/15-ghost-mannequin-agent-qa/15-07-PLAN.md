---
phase: 15-ghost-mannequin-agent-qa
plan: "07"
type: execute
wave: 4
depends_on:
  - "15-06"
files_modified:
  - skyyrose/elite_studio/agents/ghost_mannequin_agent.py
  - tests/services/ml/test_ghost_mannequin_agent.py
  - tests/elite_studio/agents/test_retry_classification.py
  - tests/elite_studio/agents/test_full_generation_path.py
autonomous: true
requirements:
  - GM-05
  - GM-06
  - QA-02

must_haves:
  truths:
    - "SC1: python -m skyyrose.elite_studio.agents.ghost_mannequin_agent --sku br-001 --dry-run exits 0"
    - "SC2: ghost_mannequin_node with enable_ghost_mannequin_agent=True registers in StateGraph without error"
    - "SC3: Accessory SKU sg-007 writes to failures.json with failure_class \"accessory_skip\""
    - "SC4: Spend cap breach raises RuntimeError with amount spent and remaining in message"
    - "SC5: API key never appears in failures.json or JSONL audit output"
  artifacts:
    - path: "skyyrose/elite_studio/agents/ghost_mannequin_agent.py"
      provides: "__main__ CLI block: --sku, --dry-run, --max-spend flags"
      contains: "__main__"
    - path: "tests/services/ml/test_ghost_mannequin_agent.py"
      provides: "Integration tests covering all 5 roadmap success criteria"
      contains: "test_dry_run_produces_cost_manifest"
    - path: "tests/elite_studio/agents/test_retry_classification.py"
      provides: "Retry classification tests: 429 backoff vs safety-block immediate skip"
      contains: "TestRetryClassification"
    - path: "tests/elite_studio/agents/test_full_generation_path.py"
      provides: "Full generation path: 1200x1200 WebP output with corner pixel purity (SC2)"
      contains: "TestFullGenerationPath"
  key_links:
    - from: "tests/services/ml/test_ghost_mannequin_agent.py"
      to: "skyyrose/elite_studio/agents/ghost_mannequin_agent.py"
      via: "import GhostMannequinAgent, ghost_mannequin_node, _sanitize_error_message"
      pattern: "GhostMannequinAgent"
    - from: "tests/services/ml/test_ghost_mannequin_agent.py"
      to: "skyyrose/elite_studio/sku_resolver.py"
      via: "import sanitize_sku, resolve_sku"
      pattern: "sanitize_sku"
    - from: "tests/elite_studio/agents/test_retry_classification.py"
      to: "skyyrose/elite_studio/synthesis/ghost_mannequin_synthesizer.py"
      via: "import RetryableError, NonRetryableError"
      pattern: "RetryableError"
    - from: "tests/elite_studio/agents/test_full_generation_path.py"
      to: "skyyrose/elite_studio/agents/quality_agent.py"
      via: "import VetoResult"
      pattern: "VetoResult"
---

<objective>
Add the CLI entry point (__main__ block) to ghost_mannequin_agent.py and write the integration test suite that exercises all 5 Phase 15 roadmap success criteria without making any paid API calls. Two dedicated test files cover retry classification (SC3) and the full generation path with 1200×1200 white-corner output (SC2).

Purpose: GM-05 requires the CLI entry point. GM-06 requires accessory-skip to be end-to-end testable. The test suite is the verification gate that proves Phase 15 is shippable — it mocks all external paid APIs while exercising the real agent logic, budget guard, accessory skip, key sanitization, JSONL structure, and dry-run manifest.
Output: __main__ block in ghost_mannequin_agent.py (Edit only) + tests/services/ml/test_ghost_mannequin_agent.py (new file) + tests/elite_studio/agents/test_retry_classification.py (new file) + tests/elite_studio/agents/test_full_generation_path.py (new file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-CONTEXT.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-06-PLAN.md

<interfaces>
<!-- GhostMannequinAgent (from 15-06):
  __init__(spend_cap_usd: float | None, dry_run: bool)
  async run(sku_raw, budget, run_id, audit_path) -> GhostMannequinAgentResult
  _check_budget(budget, next_cost_usd, stage) -> None (raises RuntimeError on exceed)
  _append_failure(sku, attempt, stage, failure_class, error_message, score_breakdown) -> None
  _sanitize_error_message(msg: str) -> str (module-level)
  _DEFAULT_SPEND_CAP_USD = 25.0
  self._qa = QualityAgent()  (instance attribute — patch via patch.object(agent._qa, ...) or class-level patch)
-->
<!-- GhostMannequinAgentResult (from 15-01):
  sku, output_path, passed_qa, tier_used, total_cost_usd, score_breakdown, audit_log_path
-->
<!-- FailureEntry (from 15-01):
  sku, attempt_number, timestamp, stage, failure_class, error_message, score_breakdown
-->
<!-- RunBudget (existing budget.py):
  cap_usd: float, spent_usd: float
-->
<!-- Phase 15 ROADMAP success criteria (5 criteria — verbatim):
  1. python -m skyyrose.elite_studio.agents.ghost_mannequin_agent --sku br-001 --dry-run exits 0
  2. ghost_mannequin_node with enable_ghost_mannequin_agent=True registers in StateGraph without error
  3. Accessory SKU sg-007 writes to failures.json with failure_class "accessory_skip"
  4. Spend cap breach raises RuntimeError with amount spent and remaining in message
  5. API key never appears in failures.json or JSONL audit output
-->
<!-- CLI spec (D-18, GM-05):
  python -m skyyrose.elite_studio.agents.ghost_mannequin_agent --sku <id> [--dry-run] [--max-spend N]
  --sku: required, raw SKU string (sanitize_sku called internally)
  --dry-run: flag, enables dry-run mode (zero paid calls, shows cost manifest)
  --max-spend: optional float, overrides GHOST_MAX_SPEND_USD env var
-->
<!-- RetryableError / NonRetryableError (from 15-04):
  Module: skyyrose/elite_studio/synthesis/ghost_mannequin_synthesizer.py
  Import: from skyyrose.elite_studio.synthesis.ghost_mannequin_synthesizer import RetryableError, NonRetryableError
  class RetryableError(Exception): pass  # 429, transient 5xx — trigger exponential backoff
  class NonRetryableError(Exception): pass  # safety block, content filter — flag immediately, no retry
-->
<!-- VetoResult (from 15-05):
  Module: skyyrose/elite_studio/agents/quality_agent.py
  Import: from skyyrose.elite_studio.agents.quality_agent import VetoResult
  Frozen dataclass — 14 fields (canonical names):
    passed: bool
    silhouette_iou: float
    silhouette_passed: bool
    color_delta_e: float
    color_passed: bool
    clip_score: float
    clip_passed: bool
    logo_drift_pct: float
    logo_passed: bool
    corner_purity: dict  (raw output from corner_pixel_purity(); keys: "passed", "tolerance", "corners", "max_distance")
    corner_passed: bool
    pattern_passed: bool
    vetoes_failed: list[str]
    pattern_pass_was_exception_fallback: bool = False
-->
<!-- score_ghost_mannequin call shape (from 15-06):
  Called as: await self._qa.score_ghost_mannequin(candidate_bytes, flatlay_bytes, techflat_bytes, sku, branding_spec)
  self._qa is a QualityAgent instance stored on GhostMannequinAgent.
  To mock in tests: use patch.object(agent._qa, "score_ghost_mannequin", return_value=mock_veto)
  OR class-level: patch("skyyrose.elite_studio.agents.quality_agent.QualityAgent.score_ghost_mannequin", return_value=mock_veto)
  Do NOT patch via the ghost_mannequin_agent module namespace — score_ghost_mannequin
  is an instance method on self._qa, not a module-level import in that namespace.
-->
<!-- Output path convention (D-18):
  renders/ghost-mannequin/{sku}-ghost-front.webp — 1200x1200 WebP
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add __main__ CLI block to ghost_mannequin_agent.py</name>
  <files>skyyrose/elite_studio/agents/ghost_mannequin_agent.py</files>
  <read_first>
    Read skyyrose/elite_studio/agents/ghost_mannequin_agent.py tail — last 30 lines — to confirm current end-of-file and verify no __main__ block exists yet.
    Confirm RunBudget import is already present in the file (should be from 15-06).
  </read_first>
  <action>
    EDIT ghost_mannequin_agent.py — append __main__ block only. Do NOT modify any existing code above. Append to end of file:

    if __name__ == "__main__":
        import argparse
        import asyncio
        import sys
        # RunBudget and GhostMannequinAgent are already defined above in the module

        parser = argparse.ArgumentParser(
            description="Ghost Mannequin Agent — generate a validated white-background front shot for a single SKU.",
            formatter_class=argparse.RawDescriptionHelpFormatter,
        )
        parser.add_argument("--sku", required=True, help="Product SKU (e.g. br-001). Must be alphanumeric with hyphens/underscores.")
        parser.add_argument("--dry-run", action="store_true", help="Print cost manifest without making any paid API calls. Calls Tripo get_balance (free) to verify credits.")
        parser.add_argument("--max-spend", type=float, default=None, metavar="USD", help=f"Hard spend cap in USD (default: ${_DEFAULT_SPEND_CAP_USD:.2f} or GHOST_MAX_SPEND_USD env var).")
        args = parser.parse_args()

        logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(name)s %(message)s")

        try:
            sanitize_sku(args.sku)  # early validation; raises ValueError with clear message
        except ValueError as exc:
            print(f"ERROR: Invalid SKU — {exc}", file=sys.stderr)
            sys.exit(1)

        import datetime
        run_id = datetime.datetime.utcnow().strftime("%Y%m%dT%H%M%S")
        audit_path = _AUDIT_DIR / f"{run_id}.jsonl"
        budget = RunBudget(cap_usd=args.max_spend or _DEFAULT_SPEND_CAP_USD, spent_usd=0.0)

        agent = GhostMannequinAgent(spend_cap_usd=args.max_spend, dry_run=args.dry_run)
        try:
            result = asyncio.run(agent.run(args.sku, budget, run_id, audit_path))
            print(f"\nSUCCESS")
            print(f"  SKU          : {result.sku}")
            print(f"  Output       : {result.output_path}")
            print(f"  QA passed    : {result.passed_qa}")
            print(f"  Tier used    : {result.tier_used}")
            print(f"  Total cost   : ${result.total_cost_usd:.4f}")
            print(f"  Audit log    : {result.audit_log_path}")
            sys.exit(0)
        except SystemExit:
            raise  # dry-run clean exit passes through
        except ValueError as exc:
            print(f"\nSKIPPED: {exc}", file=sys.stderr)
            sys.exit(2)
        except RuntimeError as exc:
            print(f"\nFAILED: {exc}", file=sys.stderr)
            sys.exit(1)
        except Exception as exc:
            print(f"\nUNEXPECTED ERROR: {exc}", file=sys.stderr)
            logger.exception("Unhandled exception in ghost mannequin agent CLI")
            sys.exit(1)
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -m skyyrose.elite_studio.agents.ghost_mannequin_agent --help | grep -q "\-\-sku" && echo "cli_help OK"</automated>
  </verify>
  <acceptance_criteria>
    - --help output includes --sku, --dry-run, --max-spend flags
    - Invalid SKU (e.g. ../evil) exits with code 1 and error message to stderr
    - __main__ block at end of file, no existing code modified
    - Module still importable (no syntax error introduced)
  </acceptance_criteria>
  <done>--help shows all three flags; "cli_help OK" printed</done>
</task>

<task type="auto">
  <name>Task 2: Write integration test suite for all 5 Phase 15 success criteria</name>
  <files>tests/services/ml/test_ghost_mannequin_agent.py</files>
  <read_first>
    Check if tests/services/ml/ directory exists: ls tests/services/ml/ 2>/dev/null.
    Check if tests/services/ml/__init__.py exists.
    Read tests/services/ml/test_ml_enhancement.py lines 1-30 — confirm test style (pytest, unittest, fixtures) used in this test directory.
    Check if tests/conftest.py exists and if it provides any relevant fixtures (tmp_path, monkeypatch are stdlib pytest fixtures — always available).
    Read skyyrose/elite_studio/synthesis/ghost_mannequin_synthesizer.py lines 1-30 — confirm RetryableError and NonRetryableError are defined there.
    Read skyyrose/elite_studio/agents/quality_agent.py lines 1-50 — confirm VetoResult dataclass field names before writing fixtures.
  </read_first>
  <action>
    Create tests/services/ml/test_ghost_mannequin_agent.py. Also create tests/services/ml/__init__.py if absent (empty file).

    Test module structure:

    1. Imports: pytest, pathlib, json, io, datetime, tempfile, unittest.mock (patch, MagicMock, AsyncMock), PIL.Image.

    2. Fixtures:

    @pytest.fixture
    def tmp_renders(tmp_path):
        (tmp_path / "renders" / "ghost-mannequin" / "audit").mkdir(parents=True)
        (tmp_path / "renders" / "3d" / "cache").mkdir(parents=True)
        return tmp_path

    @pytest.fixture
    def white_webp_bytes():
        img = PIL.Image.new("RGB", (1200, 1200), (255, 255, 255))
        buf = io.BytesIO(); img.save(buf, format="WEBP"); return buf.getvalue()

    @pytest.fixture
    def fake_flatlay_bytes():
        img = PIL.Image.new("RGB", (800, 600), (50, 50, 80))
        buf = io.BytesIO(); img.save(buf, format="PNG"); return buf.getvalue()

    @pytest.fixture
    def fake_manifest(tmp_path, fake_flatlay_bytes):
        bundle = tmp_path / "bundles" / "br-001"
        bundle.mkdir(parents=True)
        flatlay = bundle / "br-001-crewneck.png"
        flatlay.write_bytes(fake_flatlay_bytes)
        techflat = bundle / "br-001-techflat.png"
        techflat.write_bytes(fake_flatlay_bytes)
        manifest = {"sku": "br-001", "name": "BLACK Rose Crewneck", "dirname": "br-001", "collection": "black-rose", "files": {"source-photo": "br-001-crewneck.png", "techflat-front": "br-001-techflat.png", "techflat-back": "br-001-techflat.png", "logo-ref": "br-001-techflat.png", "spec": ""}}
        (bundle / "manifest.json").write_text(json.dumps(manifest))
        return bundle

    3. TEST CLASSES (one per success criterion):

    class TestSanitization:
        """Success criterion 5: API keys never appear in failures.json or JSONL output."""

        def test_sanitize_error_message_strips_openai_key(self):
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import _sanitize_error_message
            dirty = "Request failed: sk-abcdefghijklmnopqrstuvwxyz1234567890"
            result = _sanitize_error_message(dirty)
            assert "sk-abcdef" not in result
            assert "[REDACTED]" in result

        def test_sanitize_error_message_strips_google_key(self):
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import _sanitize_error_message
            dirty = "AuthError: AIzaABCDEFGHIJKLMNOPQRSTUVWXYZ1234567"
            result = _sanitize_error_message(dirty)
            assert "AIzaABCD" not in result
            assert "[REDACTED]" in result

        def test_sanitize_error_message_preserves_clean_message(self):
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import _sanitize_error_message
            clean = "HTTP 429: rate limit exceeded"
            assert _sanitize_error_message(clean) == clean


    class TestAccessorySkip:
        """Success criterion 3: Accessory SKUs write to failures.json with failure_class 'accessory_skip'."""

        def test_resolve_sku_returns_none_for_sg007(self):
            from skyyrose.elite_studio.sku_resolver import resolve_sku
            result = resolve_sku("sg-007")
            assert result is None

        def test_resolve_sku_returns_none_for_lh005(self):
            from skyyrose.elite_studio.sku_resolver import resolve_sku
            result = resolve_sku("lh-005")
            assert result is None

        def test_accessory_skip_writes_failure_entry(self, tmp_renders, monkeypatch):
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import GhostMannequinAgent, _FAILURES_JSON
            import asyncio
            from skyyrose.elite_studio.budget import RunBudget

            monkeypatch.chdir(tmp_renders)
            (tmp_renders / "renders" / "ghost-mannequin").mkdir(parents=True, exist_ok=True)

            agent = GhostMannequinAgent(spend_cap_usd=10.0, dry_run=False)
            budget = RunBudget(cap_usd=10.0, spent_usd=0.0)
            audit_path = tmp_renders / "renders" / "ghost-mannequin" / "audit" / "test.jsonl"

            with pytest.raises((ValueError, Exception)):
                asyncio.run(agent.run("sg-007", budget, "test-run", audit_path))

            failures_path = tmp_renders / "renders" / "ghost-mannequin" / "failures.json"
            if failures_path.exists():
                data = json.loads(failures_path.read_text())
                assert "sg-007" in data
                assert data["sg-007"]["failure_class"] == "accessory_skip"


    class TestSKUSanitization:
        """Injection/path-traversal inputs rejected before any filesystem or API call."""

        def test_path_traversal_rejected(self):
            from skyyrose.elite_studio.sku_resolver import sanitize_sku
            with pytest.raises(ValueError, match="path-traversal"):
                sanitize_sku("../etc/passwd")

        def test_absolute_path_rejected(self):
            from skyyrose.elite_studio.sku_resolver import sanitize_sku
            with pytest.raises(ValueError, match="absolute"):
                sanitize_sku("/etc/passwd")

        def test_shell_metachar_rejected(self):
            from skyyrose.elite_studio.sku_resolver import sanitize_sku
            with pytest.raises(ValueError):
                sanitize_sku("br-001; rm -rf /")

        def test_valid_sku_accepted(self):
            from skyyrose.elite_studio.sku_resolver import sanitize_sku
            assert sanitize_sku("br-001") == "br-001"
            assert sanitize_sku("  sg-007  ") == "sg-007"


    class TestSpendCap:
        """Success criterion 4: Spend cap breach raises RuntimeError with spend context."""

        def test_check_budget_raises_on_overage(self):
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import GhostMannequinAgent
            from skyyrose.elite_studio.budget import RunBudget

            agent = GhostMannequinAgent(spend_cap_usd=1.0, dry_run=False)
            budget = RunBudget(cap_usd=1.0, spent_usd=0.95)

            with pytest.raises(RuntimeError) as exc_info:
                agent._check_budget(budget, 0.50, "gemini-ras-ensemble")

            msg = str(exc_info.value)
            assert "1.0" in msg or "cap" in msg.lower()
            assert "0.95" in msg or "spent" in msg.lower()

        def test_check_budget_passes_under_cap(self):
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import GhostMannequinAgent
            from skyyrose.elite_studio.budget import RunBudget

            agent = GhostMannequinAgent(spend_cap_usd=25.0, dry_run=False)
            budget = RunBudget(cap_usd=25.0, spent_usd=0.10)
            agent._check_budget(budget, 0.15, "gemini-ras-ensemble")  # must not raise


    class TestGraphRegistration:
        """Success criterion 2: ghost_mannequin_node registers in StateGraph without error."""

        def test_node_registered_when_flag_enabled(self):
            from skyyrose.elite_studio.graph.builder import GraphConfig, build_graph
            cfg = GraphConfig(enable_ghost_mannequin_agent=True)
            graph = build_graph(cfg)
            assert graph is not None

        def test_node_not_registered_by_default(self):
            from skyyrose.elite_studio.graph.builder import GraphConfig, build_graph
            cfg = GraphConfig()
            assert cfg.enable_ghost_mannequin_agent == False
            build_graph(cfg)  # must not raise


    class TestDryRun:
        """Success criterion 1: --dry-run exits 0 and prints a cost manifest."""

        def test_dry_run_mode_produces_manifest(self, tmp_renders, fake_manifest, monkeypatch, capsys):
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import GhostMannequinAgent
            from skyyrose.elite_studio.budget import RunBudget
            import asyncio

            monkeypatch.chdir(tmp_renders)

            from skyyrose.elite_studio.sku_resolver import ResolvedSKU
            mock_resolved = ResolvedSKU(
                sku="br-001",
                name="BLACK Rose Crewneck",
                garment_type="crewneck",
                bundle_dir=fake_manifest,
                manifest_path=fake_manifest / "manifest.json",
                branding_spec="Logo on chest",
                is_jersey=False,
            )

            with patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.resolve_sku", return_value=mock_resolved), \
                 patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.verify_tripo_region", return_value=True):

                agent = GhostMannequinAgent(spend_cap_usd=10.0, dry_run=True)
                budget = RunBudget(cap_usd=10.0, spent_usd=0.0)
                audit_path = tmp_renders / "renders" / "ghost-mannequin" / "audit" / "test.jsonl"

                with pytest.raises(SystemExit) as exc_info:
                    asyncio.run(agent.run("br-001", budget, "test-run", audit_path))

                assert exc_info.value.code == 0

            captured = capsys.readouterr()
            assert "DRY RUN" in captured.out or "cost manifest" in captured.out.lower()
            assert "br-001" in captured.out

    IMPORTANT: Use unittest.mock.patch with the full import path for all mocked dependencies. Use AsyncMock for async methods. All tests must run without requiring TRIPO3D_API_KEY, GOOGLE_API_KEY, OPENAI_API_KEY, or IDEOGRAM_API_KEY environment variables to be set.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -m pytest tests/services/ml/test_ghost_mannequin_agent.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - pytest runs the test file with zero failures and zero errors
    - TestSanitization: 3 tests pass (key stripping for OpenAI and Google keys, clean message preserved)
    - TestAccessorySkip: 3 tests pass (sg-007 and lh-005 resolve to None; failure entry written)
    - TestSKUSanitization: 4 tests pass (path-traversal, absolute path, shell metachar all rejected; valid SKU accepted)
    - TestSpendCap: 2 tests pass (overage raises RuntimeError; under-cap does not raise)
    - TestGraphRegistration: 2 tests pass (node registered with flag on; not registered with flag off)
    - TestDryRun: 1 test passes (dry-run exits 0, prints cost manifest)
    - All tests run without any paid API call to Tripo, Gemini, GPT, or Ideogram
  </acceptance_criteria>
  <done>pytest tests/services/ml/test_ghost_mannequin_agent.py passes with all tests green and no paid API calls</done>
</task>

<task type="auto">
  <name>Task 3: Test retry classification — 429 backoff vs safety-block immediate skip</name>
  <files>tests/elite_studio/agents/test_retry_classification.py, tests/elite_studio/__init__.py, tests/elite_studio/agents/__init__.py</files>
  <read_first>
    Check if tests/elite_studio/ directory exists: ls tests/elite_studio/ 2>/dev/null.
    Check if tests/elite_studio/agents/ exists.
    Read skyyrose/elite_studio/synthesis/ghost_mannequin_synthesizer.py lines 1-40 — confirm RetryableError and NonRetryableError class definitions and that they live in this exact module.
  </read_first>
  <action>
    Create tests/elite_studio/__init__.py (empty) and tests/elite_studio/agents/__init__.py (empty) if they do not exist.

    Create tests/elite_studio/agents/test_retry_classification.py:

    MODULE LAYOUT:

    1. Imports: pytest, json, io, asyncio, unittest.mock (patch, AsyncMock, MagicMock).

    2. Fixtures:

    @pytest.fixture
    def tmp_renders(tmp_path):
        (tmp_path / "renders" / "ghost-mannequin" / "audit").mkdir(parents=True)
        return tmp_path

    @pytest.fixture
    def white_webp_bytes():
        import PIL.Image
        img = PIL.Image.new("RGB", (1200, 1200), (255, 255, 255))
        buf = io.BytesIO(); img.save(buf, format="WEBP"); return buf.getvalue()

    @pytest.fixture
    def fake_flatlay_bytes():
        import PIL.Image
        img = PIL.Image.new("RGB", (800, 600), (50, 50, 80))
        buf = io.BytesIO(); img.save(buf, format="PNG"); return buf.getvalue()

    @pytest.fixture
    def fake_manifest(tmp_path, fake_flatlay_bytes):
        bundle = tmp_path / "bundles" / "br-001"
        bundle.mkdir(parents=True)
        for fname in ("br-001-crewneck.png", "br-001-techflat.png"):
            (bundle / fname).write_bytes(fake_flatlay_bytes)
        manifest = {
            "sku": "br-001", "name": "BLACK Rose Crewneck", "dirname": "br-001",
            "collection": "black-rose",
            "files": {
                "source-photo": "br-001-crewneck.png",
                "techflat-front": "br-001-techflat.png",
                "techflat-back": "br-001-techflat.png",
                "logo-ref": "br-001-techflat.png",
                "spec": ""
            }
        }
        (bundle / "manifest.json").write_text(json.dumps(manifest))
        return bundle

    3. TEST CLASS:

    class TestRetryClassification:
        """SC3: 429 triggers exponential backoff with retries; safety block flags immediately without retry."""

        def test_429_triggers_exponential_backoff_then_succeeds(self, tmp_renders, fake_manifest, monkeypatch, white_webp_bytes):
            """
            patches synthesizer._call_gemini_ras to raise RetryableError(status_code=429) twice then return
            a valid image bytes value; asserts call_count >= 3 (2 retries + 1 success); patches asyncio.sleep
            and asserts total sleep duration >= 6 seconds (exponential backoff: 2s + 4s minimum).
            """
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import GhostMannequinAgent
            from skyyrose.elite_studio.synthesis.ghost_mannequin_synthesizer import RetryableError
            from skyyrose.elite_studio.budget import RunBudget
            from skyyrose.elite_studio.sku_resolver import ResolvedSKU
            from skyyrose.elite_studio.agents.quality_agent import VetoResult

            monkeypatch.chdir(tmp_renders)
            (tmp_renders / "renders" / "ghost-mannequin").mkdir(parents=True, exist_ok=True)

            mock_resolved = ResolvedSKU(
                sku="br-001", name="BLACK Rose Crewneck", garment_type="crewneck",
                bundle_dir=fake_manifest, manifest_path=fake_manifest / "manifest.json",
                branding_spec="", is_jersey=False,
            )

            call_count = {"n": 0}
            sleep_total = {"secs": 0.0}

            async def flaky_gemini(*args, **kwargs):
                call_count["n"] += 1
                if call_count["n"] < 3:
                    raise RetryableError("HTTP 429: quota exceeded")
                return white_webp_bytes

            async def mock_sleep(delay):
                sleep_total["secs"] += delay

            mock_veto = VetoResult(
                passed=True,
                silhouette_iou=0.9,
                silhouette_passed=True,
                color_delta_e=1.5,
                color_passed=True,
                clip_score=0.85,
                clip_passed=True,
                logo_drift_pct=0.0,
                logo_passed=True,
                corner_purity={"passed": True, "tolerance": 5, "corners": {}, "max_distance": 0},
                corner_passed=True,
                pattern_passed=True,
                vetoes_failed=[],
                pattern_pass_was_exception_fallback=False,
            )

            with patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.resolve_sku", return_value=mock_resolved), \
                 patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.verify_tripo_region", return_value=True), \
                 patch("asyncio.sleep", side_effect=mock_sleep), \
                 patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.GhostMannequinSynthesizer") as mock_synth_cls, \
                 patch("skyyrose.elite_studio.agents.quality_agent.QualityAgent.score_ghost_mannequin", new_callable=AsyncMock, return_value=mock_veto):

                mock_synth = mock_synth_cls.return_value
                mock_synth._call_gemini_ras = flaky_gemini

                agent = GhostMannequinAgent(spend_cap_usd=10.0, dry_run=False)
                budget = RunBudget(cap_usd=10.0, spent_usd=0.0)
                audit_path = tmp_renders / "renders" / "ghost-mannequin" / "audit" / "test.jsonl"

                try:
                    asyncio.run(agent.run("br-001", budget, "test-run", audit_path))
                except Exception:
                    pass  # result may vary; key assertion is retry count

            # _call_gemini_ras was called at least 3 times (2 retries + 1 success)
            assert call_count["n"] >= 3, f"Expected 3+ calls (2 retries + success); got {call_count['n']}"
            # exponential backoff: first sleep >=2s, second sleep >=4s — total >=6s
            assert sleep_total["secs"] >= 6.0, f"Expected >=6s total backoff sleep; got {sleep_total['secs']}s"

            # Audit log must contain retry_backoff events followed by generation_success
            if audit_path.exists():
                entries = [json.loads(line) for line in audit_path.read_text().splitlines() if line.strip()]
                retry_events = [e for e in entries if e.get("event") == "retry_backoff"]
                success_events = [e for e in entries if e.get("event") == "generation_success"]
                assert len(retry_events) >= 2, f"Expected >=2 retry_backoff audit entries; got {len(retry_events)}"
                assert len(success_events) >= 1, f"Expected >=1 generation_success audit entry; got {len(success_events)}"

        def test_safety_block_immediately_flags_no_retry(self, tmp_renders, fake_manifest, monkeypatch):
            """
            patches synthesizer._call_gemini_ras to raise NonRetryableError(reason="safety_block");
            asserts failures.json has one entry with failure_class="non_retryable" and error_message
            containing "safety_block"; asserts call_count == 1 (no retry); asserts exit path is
            failure-not-fatal (no unhandled exception propagates to caller).
            """
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import GhostMannequinAgent
            from skyyrose.elite_studio.synthesis.ghost_mannequin_synthesizer import NonRetryableError
            from skyyrose.elite_studio.budget import RunBudget
            from skyyrose.elite_studio.sku_resolver import ResolvedSKU

            monkeypatch.chdir(tmp_renders)
            (tmp_renders / "renders" / "ghost-mannequin").mkdir(parents=True, exist_ok=True)

            mock_resolved = ResolvedSKU(
                sku="br-001", name="BLACK Rose Crewneck", garment_type="crewneck",
                bundle_dir=fake_manifest, manifest_path=fake_manifest / "manifest.json",
                branding_spec="", is_jersey=False,
            )

            call_count = {"n": 0}

            async def safety_blocked(*args, **kwargs):
                call_count["n"] += 1
                raise NonRetryableError("safety_block: content policy violation")

            with patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.resolve_sku", return_value=mock_resolved), \
                 patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.verify_tripo_region", return_value=True), \
                 patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.GhostMannequinSynthesizer") as mock_synth_cls:

                mock_synth = mock_synth_cls.return_value
                mock_synth._call_gemini_ras = safety_blocked

                agent = GhostMannequinAgent(spend_cap_usd=10.0, dry_run=False)
                budget = RunBudget(cap_usd=10.0, spent_usd=0.0)
                audit_path = tmp_renders / "renders" / "ghost-mannequin" / "audit" / "test.jsonl"

                # Must not propagate an unhandled exception — failure is non-fatal (failure-not-abort)
                try:
                    asyncio.run(agent.run("br-001", budget, "test-run", audit_path))
                except SystemExit:
                    pass  # SystemExit(non-zero) is acceptable for non-retryable failure
                except Exception as exc:
                    # Any other exception propagating means the agent did not handle it internally
                    pytest.fail(f"NonRetryableError should not propagate to caller; got: {type(exc).__name__}: {exc}")

            # NonRetryableError must NOT retry — call count must be 1 (one per variant attempt, not multiple retries)
            assert call_count["n"] == 1, f"NonRetryableError triggered retry: called {call_count['n']} times (expected 1)"

            # failures.json must have an entry with failure_class indicating non-retryable
            failures_path = tmp_renders / "renders" / "ghost-mannequin" / "failures.json"
            assert failures_path.exists(), "failures.json must be written for NonRetryableError"
            data = json.loads(failures_path.read_text())
            assert "br-001" in data, "failures.json must have entry for br-001"
            entry = data["br-001"]
            assert entry.get("failure_class") in ("non_retryable", "safety_block", "content_filter"), \
                f"failure_class should indicate non-retryable; got: {entry.get('failure_class')}"
            assert "safety_block" in entry.get("error_message", ""), \
                f"error_message should contain 'safety_block'; got: {entry.get('error_message')}"

    IMPORTANT: All tests must run without TRIPO3D_API_KEY, GOOGLE_API_KEY, OPENAI_API_KEY, or IDEOGRAM_API_KEY set.
    Do NOT patch at the synthesizer package root level — import from the full submodule path shown above.
    Import RetryableError and NonRetryableError from skyyrose.elite_studio.synthesis.ghost_mannequin_synthesizer.
    Patch score_ghost_mannequin at the class level: patch("skyyrose.elite_studio.agents.quality_agent.QualityAgent.score_ghost_mannequin", ...) — using the agent module namespace fails because score_ghost_mannequin is an instance method on self._qa, not a module-level name.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -m pytest tests/elite_studio/agents/test_retry_classification.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_429_triggers_exponential_backoff_then_succeeds: call_count >= 3, sleep_total >= 6.0s, audit log has >=2 retry_backoff events and >=1 generation_success event
    - test_safety_block_immediately_flags_no_retry: call_count == 1, failures.json written with failure_class non_retryable/safety_block, error_message contains "safety_block"
    - No real API calls made (all external calls mocked)
    - Tests run without any environment variables set
  </acceptance_criteria>
  <done>pytest tests/elite_studio/agents/test_retry_classification.py passes — both tests green</done>
</task>

<task type="auto">
  <name>Task 4: Test full generation path — 1200x1200 WebP output with corner pixel purity (SC2)</name>
  <files>tests/elite_studio/agents/test_full_generation_path.py</files>
  <read_first>
    Confirm tests/elite_studio/agents/__init__.py exists (created in Task 3).
    Read skyyrose/elite_studio/agents/quality_agent.py lines 1-80 — confirm VetoResult frozen dataclass field names exactly (canonical: silhouette_passed, color_passed, clip_passed, logo_passed, corner_purity as dict, corner_passed, pattern_pass_was_exception_fallback).
    Read skyyrose/elite_studio/agents/ghost_mannequin_agent.py lines 1-50 — confirm self._qa attribute name and how score_ghost_mannequin is called (self._qa.score_ghost_mannequin(...)).
    Confirm renders/ghost-mannequin/ output path convention from CONTEXT.md D-18.
  </read_first>
  <action>
    Create tests/elite_studio/agents/test_full_generation_path.py.

    MODULE LAYOUT:

    1. Imports: pytest, json, io, asyncio, pathlib, unittest.mock (patch, AsyncMock, MagicMock).

    2. Fixtures:

    @pytest.fixture
    def tmp_renders(tmp_path):
        (tmp_path / "renders" / "ghost-mannequin" / "audit").mkdir(parents=True)
        (tmp_path / "renders" / "3d" / "cache").mkdir(parents=True)
        return tmp_path

    @pytest.fixture
    def white_webp_1200(tmp_path):
        """A 1200x1200 all-white WebP image as bytes — simulates valid synthesize_ensemble output."""
        import PIL.Image
        img = PIL.Image.new("RGB", (1200, 1200), (255, 255, 255))
        buf = io.BytesIO(); img.save(buf, format="WEBP"); return buf.getvalue()

    @pytest.fixture
    def fake_flatlay_bytes():
        import PIL.Image
        img = PIL.Image.new("RGB", (800, 600), (50, 50, 80))
        buf = io.BytesIO(); img.save(buf, format="PNG"); return buf.getvalue()

    @pytest.fixture
    def fake_manifest(tmp_path, fake_flatlay_bytes):
        bundle = tmp_path / "bundles" / "br-001"
        bundle.mkdir(parents=True)
        for fname in ("br-001-crewneck.png", "br-001-techflat.png"):
            (bundle / fname).write_bytes(fake_flatlay_bytes)
        manifest = {
            "sku": "br-001", "name": "BLACK Rose Crewneck", "dirname": "br-001",
            "collection": "black-rose",
            "files": {
                "source-photo": "br-001-crewneck.png",
                "techflat-front": "br-001-techflat.png",
                "techflat-back": "br-001-techflat.png",
                "logo-ref": "br-001-techflat.png",
                "spec": ""
            }
        }
        (bundle / "manifest.json").write_text(json.dumps(manifest))
        return bundle

    3. TEST CLASS:

    class TestFullGenerationPath:
        """SC2: agent against a non-jersey SKU produces renders/ghost-mannequin/{sku}-ghost-front.webp
        at 1200x1200px with all four corner pixels within 5 RGB units of pure white (255, 255, 255)."""

        def test_non_jersey_sku_produces_valid_white_corner_webp(self, tmp_renders, fake_manifest, monkeypatch, white_webp_1200):
            """
            Builds a 1200x1200 all-white WebP, wraps it as the output of synthesize_ensemble (3 candidates),
            patches QualityAgent.score_ghost_mannequin (via patch.object on the agent's _qa instance) returning
            a passing VetoResult with corner_passed=True, patches TripoDigitizer.digitize, runs
            agent.run("br-001", budget=10.0, run_id="t2", ...), then asserts:
              - output at renders/ghost-mannequin/br-001-ghost-front.webp exists
              - image size == (1200, 1200)
              - all four corner pixels are within 5 RGB units of (255, 255, 255)
              - result.passed_qa is True
            """
            import PIL.Image
            from skyyrose.elite_studio.agents.ghost_mannequin_agent import GhostMannequinAgent
            from skyyrose.elite_studio.budget import RunBudget
            from skyyrose.elite_studio.sku_resolver import ResolvedSKU
            from skyyrose.elite_studio.agents.quality_agent import VetoResult

            monkeypatch.chdir(tmp_renders)
            out_dir = tmp_renders / "renders" / "ghost-mannequin"
            out_dir.mkdir(parents=True, exist_ok=True)

            mock_resolved = ResolvedSKU(
                sku="br-001",
                name="BLACK Rose Crewneck",
                garment_type="crewneck",
                bundle_dir=fake_manifest,
                manifest_path=fake_manifest / "manifest.json",
                branding_spec="Logo on chest",
                is_jersey=False,
            )

            passing_veto = VetoResult(
                passed=True,
                silhouette_iou=0.95,
                silhouette_passed=True,
                color_delta_e=1.0,
                color_passed=True,
                clip_score=0.88,
                clip_passed=True,
                logo_drift_pct=0.0,
                logo_passed=True,
                corner_purity={"passed": True, "tolerance": 5, "corners": {
                    "top_left": {"rgb": (253, 253, 253), "distance_from_white": 2, "passed": True},
                    "top_right": {"rgb": (253, 253, 253), "distance_from_white": 2, "passed": True},
                    "bottom_left": {"rgb": (253, 253, 253), "distance_from_white": 2, "passed": True},
                    "bottom_right": {"rgb": (253, 253, 253), "distance_from_white": 2, "passed": True},
                }, "max_distance": 2},
                corner_passed=True,
                pattern_passed=True,
                vetoes_failed=[],
                pattern_pass_was_exception_fallback=False,
            )

            with patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.resolve_sku", return_value=mock_resolved), \
                 patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.verify_tripo_region", return_value=True), \
                 patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.GhostMannequinSynthesizer") as mock_synth_cls, \
                 patch("skyyrose.elite_studio.agents.ghost_mannequin_agent.TripoDigitizer") as mock_digitizer_cls:

                mock_synth = mock_synth_cls.return_value
                mock_synth.synthesize_ensemble = AsyncMock(
                    return_value=[white_webp_1200, white_webp_1200, white_webp_1200]
                )
                mock_synth._call_gemini_ras = AsyncMock(return_value=white_webp_1200)

                mock_digitizer = mock_digitizer_cls.return_value
                mock_digitizer.digitize = MagicMock(
                    return_value=tmp_renders / "renders" / "3d" / "cache" / "br-001.glb"
                )
                # Create a stub .glb so the path reference is valid
                glb_path = tmp_renders / "renders" / "3d" / "cache" / "br-001.glb"
                glb_path.parent.mkdir(parents=True, exist_ok=True)
                glb_path.write_bytes(b"glTF\x00" * 10)

                agent = GhostMannequinAgent(spend_cap_usd=10.0, dry_run=False)
                budget = RunBudget(cap_usd=10.0, spent_usd=0.0)
                audit_path = tmp_renders / "renders" / "ghost-mannequin" / "audit" / "test.jsonl"

                # Patch score_ghost_mannequin on the agent's live _qa instance (correct intercept point)
                with patch.object(agent._qa, "score_ghost_mannequin", new_callable=AsyncMock, return_value=passing_veto):
                    result = asyncio.run(agent.run("br-001", budget, "t2", audit_path))

            # SC2 assertions
            assert result.passed_qa is True, "VetoResult passed=True must propagate to result.passed_qa"

            out_path = tmp_renders / "renders" / "ghost-mannequin" / "br-001-ghost-front.webp"
            assert out_path.exists(), f"Output WebP must exist at {out_path}"

            img = PIL.Image.open(out_path).convert("RGB")
            w, h = img.size
            assert w == 1200 and h == 1200, f"Output must be 1200x1200; got {w}x{h}"

            # All four corner pixels within 5 RGB units of pure white
            corners = [
                img.getpixel((0, 0)),
                img.getpixel((w - 1, 0)),
                img.getpixel((0, h - 1)),
                img.getpixel((w - 1, h - 1)),
            ]
            for r, g, b in corners:
                assert abs(r - 255) <= 5, f"Corner R channel not white: {r}"
                assert abs(g - 255) <= 5, f"Corner G channel not white: {g}"
                assert abs(b - 255) <= 5, f"Corner B channel not white: {b}"

    IMPORTANT: All four corners must be checked independently. Do not use corner_pixel_purity() helper from
    imagery/visual_comparison.py — the test verifies the output image directly via PIL, not through the
    production helper (avoids hiding bugs in the helper itself). No environment variable keys required.
    Use patch.object(agent._qa, "score_ghost_mannequin", new_callable=AsyncMock, return_value=passing_veto)
    to intercept the instance-method call — this is the correct patch form because 15-06 calls
    self._qa.score_ghost_mannequin(...) and score_ghost_mannequin is NOT a module-level import in ghost_mannequin_agent.py.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -m pytest tests/elite_studio/agents/test_full_generation_path.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_non_jersey_sku_produces_valid_white_corner_webp passes
    - Output WebP exists at renders/ghost-mannequin/br-001-ghost-front.webp (relative to tmp_renders)
    - Image dimensions are exactly 1200x1200
    - All four corner pixels within 5 RGB units of (255, 255, 255)
    - result.passed_qa is True
    - No real API calls made; no environment variables required
  </acceptance_criteria>
  <done>pytest tests/elite_studio/agents/test_full_generation_path.py passes — test green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI --sku arg → agent run() | argparse argument flows into sanitize_sku() then resolve_sku() |
| Test mocks → production code paths | Mocked synthesizer/QA results must not accidentally cover up real bugs |
| Test fixtures → CI environment | Test files must not leak real API keys into CI logs |
| Test mock boundaries → real client calls | Mock-vs-real client confusion must not trigger actual paid API call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-07-01 | Tampering | CLI SystemExit(0) dry-run passthrough | LOW | accept | dry-run raises SystemExit(0) inside asyncio.run; __main__ catches all exceptions except SystemExit; re-raise allows clean exit. This is intentional control flow, not an error swallow |
| T-15-07-02 | Repudiation | Test mocks hiding real sanitization gaps | MEDIUM | mitigate | TestSanitization uses the real _sanitize_error_message() with no mocking — tests the actual regex, not a mock. TestSKUSanitization uses the real sanitize_sku() with the real catalog. These are unit tests, not mock-everything tests |
| T-15-07-03 | Denial of Service | pytest hitting real Tripo/Gemini APIs | MEDIUM | mitigate | TestDryRun patches resolve_sku and verify_tripo_region at the ghost_mannequin_agent module level, not at the sku_resolver level — ensures the real Tripo client is never instantiated during test. All other test classes use only project-local code with no external calls |
| T-15-07-04 | Information Disclosure | Malicious test fixture leaking real API key into CI logs | HIGH | mitigate | test_retry_classification.py and test_full_generation_path.py patch all external clients at the ghost_mannequin_agent module namespace before any client is instantiated. No test fixture reads from os.environ for API keys; all key-consuming code paths are intercepted by the mock before environment lookup occurs |
| T-15-07-05 | Elevation of Privilege | Mock-vs-real client confusion triggering real paid API call | HIGH | mitigate | GhostMannequinSynthesizer and TripoDigitizer are patched at the agents.ghost_mannequin_agent import namespace (not at the synthesizer/glb_cache module level), guaranteeing the agent's own reference to these classes resolves to the mock. score_ghost_mannequin is patched via patch.object on the live agent._qa instance (Task 4) or class-level patch (Task 3), preventing any real QualityAgent API call. AsyncMock prevents any async method from falling through to a real network call |
</threat_model>

<verification>
Run after all tasks complete:

  cd /Users/theceo/DevSkyy && python -m pytest tests/services/ml/test_ghost_mannequin_agent.py tests/elite_studio/agents/test_retry_classification.py tests/elite_studio/agents/test_full_generation_path.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR|test_"

  And verify the CLI works:
  cd /Users/theceo/DevSkyy && python -m skyyrose.elite_studio.agents.ghost_mannequin_agent --help

  And verify no existing tests were broken:
  cd /Users/theceo/DevSkyy && python -m pytest tests/ -x -q --ignore=tests/services/ml/test_ghost_mannequin_agent.py --ignore=tests/elite_studio/ 2>&1 | tail -5
</verification>

<success_criteria>
- pytest tests/services/ml/test_ghost_mannequin_agent.py: all tests PASSED
- pytest tests/elite_studio/agents/test_retry_classification.py: both tests PASSED
- pytest tests/elite_studio/agents/test_full_generation_path.py: test PASSED
- python -m skyyrose.elite_studio.agents.ghost_mannequin_agent --help: shows --sku, --dry-run, --max-spend
- python -m skyyrose.elite_studio.agents.ghost_mannequin_agent --sku '../evil' exits 1 with error to stderr
- All 5 ROADMAP success criteria covered by at least one test each (SC1→TestDryRun, SC2→TestFullGenerationPath, SC3→TestRetryClassification, SC4→TestSpendCap, SC5→TestSanitization)
- Zero paid API calls in any test
- Existing test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-ghost-mannequin-agent-qa/15-07-SUMMARY.md`
</output>
