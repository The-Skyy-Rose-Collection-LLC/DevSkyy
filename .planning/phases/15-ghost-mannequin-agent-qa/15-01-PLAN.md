---
phase: 15-ghost-mannequin-agent-qa
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skyyrose/elite_studio/models.py
  - skyyrose/elite_studio/graph/state.py
autonomous: true
requirements:
  - GM-04
  - GM-05
  - QA-02

must_haves:
  truths:
    - "GhostMannequinAgentResult frozen dataclass is importable from skyyrose.elite_studio.models"
    - "failures.json entries are schema-validated at write time — missing fields raise ValueError"
    - "Audit JSONL entries have all 6 required fields and serialise to valid JSON"
    - "EliteStudioState carries ghost-mannequin-agent fields without breaking existing node code"
  artifacts:
    - path: "skyyrose/elite_studio/models.py"
      provides: "GhostMannequinAgentResult frozen dataclass + FailureEntry TypedDict"
      contains: "GhostMannequinAgentResult"
    - path: "skyyrose/elite_studio/graph/state.py"
      provides: "ghost_mannequin_agent_result + gm_audit_log_path fields in EliteStudioState"
      contains: "ghost_mannequin_agent_result"
  key_links:
    - from: "skyyrose/elite_studio/graph/state.py"
      to: "skyyrose/elite_studio/models.py"
      via: "import GhostMannequinAgentResult"
      pattern: "GhostMannequinAgentResult"
---

<objective>
Define the data contracts — frozen dataclasses, TypedDicts, and state fields — that every downstream Phase 15 plan builds against.

Purpose: All other plans import from models.py and state.py. Establishing these contracts first eliminates scavenger-hunt behaviour in Wave 2+.
Output: GhostMannequinAgentResult dataclass, FailureEntry TypedDict, AuditEntry TypedDict, and two new EliteStudioState fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-CONTEXT.md

<interfaces>
<!-- Existing frozen dataclasses tail of skyyrose/elite_studio/models.py (lines 237-246 per prior read). -->
<!-- GhostMannequinCompositeResult ALREADY EXISTS — do NOT reuse that name. -->
<!-- EliteStudioState in graph/state.py uses total=False TypedDict. -->
<!-- Existing state fields relevant to Phase 15: ghost_mannequin_front_path (str), -->
<!--   ghost_mannequin_back_path (str), ghost_mannequin_composite_result (GhostMannequinCompositeResult), -->
<!--   preflight_result (PreflightResult), budget (RunBudget), run_summary_path (str). -->
<!-- _default_budget() is already in state.py — reuse, do not duplicate. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GhostMannequinAgentResult dataclass and schema TypedDicts to models.py</name>
  <files>skyyrose/elite_studio/models.py</files>
  <read_first>
    Read skyyrose/elite_studio/models.py lines 230-260 (tail, near existing GhostMannequinCompositeResult).
    Confirm the last existing frozen dataclass name and its fields.
    Confirm existing imports at top of file (dataclasses, typing, pathlib used).
  </read_first>
  <action>
    Append — after the existing GhostMannequinCompositeResult block — the following new types.
    All are append-only; touch no existing code.

    GhostMannequinAgentResult: frozen dataclass (per D-01, D-02, D-03) with fields:
      sku: str
      output_path: str  (absolute path to renders/ghost-mannequin/{sku}-ghost-front.webp)
      passed_qa: bool
      tier_used: str  (literal: "gemini-ras" | "gpt-image-1" | "ideogram-v3")
      total_cost_usd: float
      score_breakdown: dict  (keys: silhouette_iou, color_delta_e, clip_score, logo_drift_pct, corner_purity_rgb; values: float)
      audit_log_path: str  (absolute path to renders/ghost-mannequin/audit/{run_timestamp}.jsonl)

    FailureEntry: TypedDict (per GM-05, D-15) with fields:
      sku: str
      attempt_number: int
      timestamp: str  (ISO-8601)
      stage: str  (e.g. "tripo-digitise", "gemini-ras", "gpt-image-1", "qa-veto")
      failure_class: str  (e.g. "safety_block", "rate_limit", "qa_fail", "accessory_skip")
      error_message: str
      score_breakdown: dict  (same keys as GhostMannequinAgentResult.score_breakdown; empty dict if failure before scoring)

    AuditEntry: TypedDict (per QA-02, D-17) with fields:
      run_id: str
      sku: str
      timestamp: str
      tier: str
      cost_usd: float
      passed_qa: bool
      score_breakdown: dict
      failure_class: str  (empty string if passed)
      bg_removal_model: str  (e.g. "briaai/RMBG-2.0"; empty string if bg removal not applied)

    Both TypedDicts use typing.TypedDict.
    Import: add "from typing import TypedDict" only if not already imported (check file header first).
    Do NOT define JSON serialisation here — that belongs in the node that writes the files.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -c "from skyyrose.elite_studio.models import GhostMannequinAgentResult, FailureEntry, AuditEntry; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - GhostMannequinAgentResult importable, frozen, all 7 fields present with correct types
    - FailureEntry importable as TypedDict with 7 fields
    - AuditEntry importable as TypedDict with 9 fields
    - No existing model class modified
    - "from skyyrose.elite_studio.models import GhostMannequinCompositeResult" still works (no breakage)
  </acceptance_criteria>
  <done>python import succeeds with "OK" output, no import errors</done>
</task>

<task type="auto">
  <name>Task 2: Extend EliteStudioState with ghost-mannequin-agent fields</name>
  <files>skyyrose/elite_studio/graph/state.py</files>
  <read_first>
    Read skyyrose/elite_studio/graph/state.py in full (file is small — under 100 lines per anatomy).
    Confirm all existing Optional fields, the import block, and create_initial_state() signature.
    Confirm that _default_budget() is present and imports RunBudget.
  </read_first>
  <action>
    Add two Optional fields to EliteStudioState (total=False TypedDict — all fields are implicitly Optional):
      ghost_mannequin_agent_result: GhostMannequinAgentResult
        Purpose: holds the terminal result returned by the LangGraph ghost_mannequin_node (15-06).
      gm_audit_log_path: str
        Purpose: path to the JSONL audit file opened at run start, passed through state so 15-06's node can append entries without reopening.

    Add import at top: "from .models import GhostMannequinAgentResult" (alongside existing model imports).

    Do NOT modify create_initial_state() — the fields default to absent (total=False semantics).
    Do NOT alter _default_budget() or any existing field.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -c "from skyyrose.elite_studio.graph.state import EliteStudioState, create_initial_state; s = create_initial_state('test-sku'); print(s.get('ghost_mannequin_agent_result', 'absent'), s.get('gm_audit_log_path', 'absent'))"</automated>
  </verify>
  <acceptance_criteria>
    - EliteStudioState importable; create_initial_state('test-sku') returns without error
    - ghost_mannequin_agent_result field present in type hints (grep confirms)
    - gm_audit_log_path field present in type hints (grep confirms)
    - All existing state fields and create_initial_state() behaviour unchanged
    - "from skyyrose.elite_studio.graph.state import EliteStudioState" works in isolation
  </acceptance_criteria>
  <done>Verify command prints "absent absent" (fields not set on new state) with no exceptions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models.py → consumers | Downstream plans import these types; incorrect field types silently corrupt audit logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01-01 | Tampering | FailureEntry.error_message | MEDIUM | mitigate | error_message field is str only — callers must not pass raw exception repr with embedded API key fragments; sanitise at write site in 15-06 (strip any substring matching known key patterns) |
| T-15-01-02 | Information Disclosure | AuditEntry serialisation | HIGH | mitigate | AuditEntry must never carry raw API keys; cost_usd and score_breakdown contain no secrets — accept as-is; key sanitisation enforced at JSONL write site in 15-06 |
| T-15-01-03 | Denial of Service | score_breakdown dict unbounded | LOW | accept | dict is populated by 15-05 vetoes with a fixed 5-key schema; no user-controlled key injection at this layer |
</threat_model>

<verification>
Run after both tasks complete:

  cd /Users/theceo/DevSkyy && python -c "
from skyyrose.elite_studio.models import (
    GhostMannequinAgentResult, FailureEntry, AuditEntry,
    GhostMannequinCompositeResult  # must still import
)
from skyyrose.elite_studio.graph.state import EliteStudioState, create_initial_state
s = create_initial_state('br-001')
assert 'ghost_mannequin_agent_result' not in s
assert 'gm_audit_log_path' not in s
print('Phase 15-01 contracts verified OK')
"
</verification>

<success_criteria>
- GhostMannequinAgentResult, FailureEntry, AuditEntry all importable from models.py
- EliteStudioState carries ghost_mannequin_agent_result and gm_audit_log_path fields
- Zero existing tests broken: pytest tests/services/test_image_ingestion.py tests/test_catalog_retriever.py tests/test_creative_hub.py -x passes
- No modification to any existing dataclass or TypedDict
</success_criteria>

<output>
After completion, create `.planning/phases/15-ghost-mannequin-agent-qa/15-01-SUMMARY.md`
</output>
