---
phase: 12-responsive-typography
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css
  - wordpress-theme/skyyrose-flagship/assets/css/homepage-v2.css
  - wordpress-theme/skyyrose-flagship/assets/css/front-page.css
  - wordpress-theme/skyyrose-flagship/assets/css/footer.css
  - wordpress-theme/skyyrose-flagship/assets/css/collections.css
  - wordpress-theme/skyyrose-flagship/assets/css/interactive-cards.css
  - wordpress-theme/skyyrose-flagship/assets/css/single-product.css
  - wordpress-theme/skyyrose-flagship/assets/css/about.css
  - wordpress-theme/skyyrose-flagship/assets/css/contact.css
  - wordpress-theme/skyyrose-flagship/assets/css/generic-pages.css
  - wordpress-theme/skyyrose-flagship/assets/css/woocommerce-single.css
autonomous: true
requirements: [RESP-01, RESP-04]

must_haves:
  truths:
    - "Heading sizes scale smoothly from mobile (320px) through tablet (768px) to desktop (1440px+)"
    - "Typography hierarchy h1 > h2 > h3 > body > small is visually consistent across all templates"
    - "All hardcoded px font sizes for text elements are replaced with clamp() values or design token references"
  artifacts:
    - path: "wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css"
      provides: "Consistent heading and body text sizing using clamp() and --text-* tokens"
    - path: "wordpress-theme/skyyrose-flagship/assets/css/homepage-v2.css"
      provides: "Story, craft, footer text using design tokens for smooth scaling"
    - path: "wordpress-theme/skyyrose-flagship/assets/css/front-page.css"
      provides: "Section headers, card text, and testimonial text using consistent scale"
    - path: "wordpress-theme/skyyrose-flagship/assets/css/footer.css"
      provides: "Footer headings and text using design token scale"
  key_links:
    - from: "wordpress-theme/skyyrose-flagship/assets/css/brand-variables.css"
      to: "all modified CSS files"
      via: "var(--text-xs) through var(--text-5xl) tokens"
      pattern: "var\\(--text-"
    - from: "all modified CSS files"
      to: "wordpress-theme/skyyrose-flagship/assets/css/*.min.css"
      via: "npm run build:css"
      pattern: "build:css"
---

<objective>
Replace hardcoded px font sizes with clamp() values and design tokens to ensure smooth font scaling across breakpoints, and enforce a consistent typography hierarchy (h1 > h2 > h3 > body > small) across all page templates.

Purpose: RESP-01 requires font sizes to scale appropriately across mobile/tablet/desktop. RESP-04 requires consistent typography hierarchy across all templates. The brand-variables.css already defines excellent clamp() tokens (--text-xs through --text-5xl) but many template CSS files use hardcoded px sizes instead.

Output: Updated CSS files using design tokens and clamp(), plus rebuilt minified output.
</objective>

<execution_context>
@/Users/theceo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theceo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-responsive-typography/12-01-SUMMARY.md

<interfaces>
<!-- CANONICAL design tokens from brand-variables.css (DO NOT REDEFINE): -->
--text-xs:   clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem)    /* 12-14px  fine print  */
--text-sm:   clamp(0.875rem, 0.83rem + 0.22vw, 1rem)       /* 14-16px  secondary   */
--text-base: clamp(1rem, 0.95rem + 0.25vw, 1.125rem)       /* 16-18px  body        */
--text-lg:   clamp(1.125rem, 1.05rem + 0.375vw, 1.375rem)  /* 18-22px  lead        */
--text-xl:   clamp(1.375rem, 1.2rem + 0.875vw, 1.875rem)   /* 22-30px  subhead     */
--text-2xl:  clamp(1.75rem, 1.5rem + 1.25vw, 2.5rem)       /* 28-40px  section     */
--text-3xl:  clamp(2rem, 1.7rem + 1.5vw, 3rem)             /* 32-48px  display     */
--text-4xl:  clamp(2.5rem, 2.1rem + 2vw, 4rem)             /* 40-64px  hero        */
--text-5xl:  clamp(3rem, 2.5rem + 2.5vw, 5rem)             /* 48-80px  statement   */

<!-- Typography Hierarchy to enforce: -->
h1 (page title):     --text-4xl or --text-5xl (hero/statement)
h2 (section title):  --text-2xl or --text-3xl (section/display)
h3 (card title):     --text-xl or --text-lg (subhead)
body:                --text-base (16-18px)
small/meta/labels:   --text-xs or --text-sm (12-16px)

<!-- Decorative tokens from design-tokens.css (for oversized display text): -->
--text-decorative-sm: clamp(2.25rem, 4vw, 4.5rem)
--text-decorative-md: clamp(3rem, 6vw, 6rem)
--text-decorative-lg: clamp(5rem, 14vw, 12.5rem)

<!-- Font families (already defined in brand-variables.css): -->
--font-heading: 'Playfair Display', Georgia, serif
--font-body: 'Cormorant Garamond', Georgia, serif
--font-accent: 'Playfair Display', Georgia, serif
--font-mono: 'Space Mono', 'Courier New', monospace
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded px font sizes with clamp() values in core template CSS</name>
  <files>
    wordpress-theme/skyyrose-flagship/assets/css/collection-v4.css,
    wordpress-theme/skyyrose-flagship/assets/css/homepage-v2.css,
    wordpress-theme/skyyrose-flagship/assets/css/front-page.css,
    wordpress-theme/skyyrose-flagship/assets/css/footer.css,
    wordpress-theme/skyyrose-flagship/assets/css/collections.css,
    wordpress-theme/skyyrose-flagship/assets/css/interactive-cards.css
  </files>
  <action>
Replace hardcoded px font sizes with clamp() or var(--text-*) tokens. Some values already use clamp() (good!) -- leave those. Focus on the many hardcoded px values.

**IMPORTANT**: Do NOT change font sizes that are already using clamp() or var(--text-*). Only convert hardcoded px values. Do NOT change font sizes on elements that are intentionally small for decorative/label purposes (like 10-11px monospace labels) -- those use --text-xs or stay at their current small size for the luxury aesthetic. The goal is SCALING, not enlarging.

**Typography hierarchy mapping for conversions:**

Hero titles (h1 level): Use existing clamp() or --text-4xl/--text-5xl
Section titles (h2 level): --text-2xl to --text-3xl range (28-48px)
Card/subsection titles (h3 level): --text-xl to --text-lg range (18-30px)
Body text: --text-base (16-18px) or clamp() for 15-17px range
Small text/meta: --text-sm (14-16px) or --text-xs (12-14px)
Micro labels (10-11px mono): Leave as-is -- these are intentional luxury typography

**collection-v4.css conversions:**
- `.col-mf__body` `font-size: 17px` -> `font-size: var(--text-base)` (16-18px range matches)
- `.col-feat__desc` `font-size: 17px` -> `font-size: var(--text-base)`
- `.col-card__name` `font-size: 17px` -> `font-size: var(--text-base)`
- `.col-card__desc` `font-size: 13px` -> `font-size: var(--text-xs)` (12-14px range)
- `.col-card__price` `font-size: 24px` -> `font-size: clamp(20px, 2vw, 24px)`
- `.col-nl__desc` `font-size: 16px` -> `font-size: var(--text-base)`
- `.col-nl__input` `font-size: 15px` -> `font-size: var(--text-sm)`
- `.col-modal__name` `font-size: 24px` -> `font-size: var(--text-xl)` (22-30px)
- `.col-modal__price` `font-size: 34px` -> `font-size: clamp(28px, 3vw, 34px)`
- `.col-modal__desc` `font-size: 15px` -> `font-size: var(--text-sm)`
- Leave all `font-size: 10px`, `font-size: 11px`, `font-size: 12px`, `font-size: 13px` on monospace/label elements (Space Mono, Bebas Neue labels) as-is -- they are intentionally small for the luxury label aesthetic.
- Leave all existing clamp() values untouched.
- The 480px breakpoint overrides (e.g. `.col-mf__head { font-size: 22px }`) should be REMOVED since the parent clamp() now handles scaling. If the parent already has clamp(), the fixed-px override at 480px defeats the purpose.

**homepage-v2.css conversions:**
- `.homepage-v2` base `font-size: 17px` -> `font-size: var(--text-base)`
- `.story-body` `font-size: 17px` -> `font-size: var(--text-base)`
- `.craft-desc` `font-size: 14px` -> `font-size: var(--text-sm)`
- `.ft-desc` `font-size: 14px` -> `font-size: var(--text-sm)`
- `.ft-links a` `font-size: 14px` -> `font-size: var(--text-sm)`
- `.craft-label` `font-size: 13px` -> leave (Cinzel display label, intentionally compact)
- `.nl-desc` `font-size: 16px` -> `font-size: var(--text-base)`
- `.nl-input` `font-size: 15px` -> `font-size: var(--text-sm)`
- `.lookbook-header p` `font-size: 15px` -> `font-size: var(--text-sm)`
- `.craft-header p` `font-size: 16px` -> `font-size: var(--text-base)`
- Leave all 10-11px monospace labels as-is.

**front-page.css conversions:**
- `.hero__subtitle` `font-size: 0.9rem` -> `font-size: var(--text-sm)` (14-16px)
- `.collections__card-desc` `font-size: 0.85rem` -> `font-size: var(--text-xs)`
- `.value-prop__desc` `font-size: 0.85rem` -> `font-size: var(--text-xs)`
- `.featured-product__name` `font-size: 0.9rem` -> `font-size: var(--text-sm)`
- `.featured-product__price` `font-size: 0.85rem` -> `font-size: var(--text-xs)`
- `.brand-story__text` `font-size: 0.9rem` -> `font-size: var(--text-sm)`
- `.testimonials__quote` `font-size: 0.9375rem` -> `font-size: var(--text-sm)`
- `.section-header__subtitle` `font-size: 0.95rem` -> `font-size: var(--text-sm)`
- `.why-skyyrose__card-text` `font-size: 0.85rem` -> `font-size: var(--text-xs)`
- Leave all 0.65-0.75rem label sizes as-is.

**footer.css conversions:**
- `.footer-newsletter__text` `font-size: 0.875rem` -> `font-size: var(--text-sm)`
- `.footer-newsletter__input` `font-size: 0.875rem` -> `font-size: var(--text-sm)`
- `.footer-grid__list a` `font-size: 0.875rem` -> `font-size: var(--text-sm)`
- `.footer-brand__description` `font-size: 0.8125rem` -> `font-size: var(--text-xs)`
- `.footer-copyright__text` `font-size: 0.8125rem` -> `font-size: var(--text-xs)`

**collections.css**: Read the file first, then apply same conversion pattern.

**interactive-cards.css conversions:**
- `.ipc__title` `font-size: 16px` -> `font-size: var(--text-base)`
- `.prc__title` `font-size: 18px` -> `font-size: var(--text-lg)` (18-22px)
- `.prc__desc` `font-size: 13px` -> `font-size: var(--text-xs)`
- `.prc__tease` `font-size: 14px` -> `font-size: var(--text-sm)`
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy/wordpress-theme/skyyrose-flagship && echo "=== Tokens used ===" && grep -c "var(--text-" assets/css/collection-v4.css assets/css/homepage-v2.css assets/css/front-page.css assets/css/footer.css assets/css/interactive-cards.css && echo "=== Remaining hardcoded body-size px ===" && grep -n "font-size: 1[4-8]px" assets/css/collection-v4.css assets/css/homepage-v2.css | grep -v "letter-spacing" | head -10</automated>
  </verify>
  <done>All body-range font sizes (14-18px) use var(--text-*) tokens or clamp(). Micro labels (10-12px mono) remain hardcoded intentionally. Typography scales smoothly via CSS custom properties.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce consistent typography hierarchy across remaining templates and rebuild</name>
  <files>
    wordpress-theme/skyyrose-flagship/assets/css/single-product.css,
    wordpress-theme/skyyrose-flagship/assets/css/about.css,
    wordpress-theme/skyyrose-flagship/assets/css/contact.css,
    wordpress-theme/skyyrose-flagship/assets/css/generic-pages.css,
    wordpress-theme/skyyrose-flagship/assets/css/woocommerce-single.css
  </files>
  <action>
Read each file, identify hardcoded font sizes, and convert to the design token scale. Then rebuild all CSS.

**Hierarchy rules to enforce across ALL templates:**
- h1 / page hero titles: --text-3xl to --text-5xl (32-80px, using clamp)
- h2 / section titles: --text-2xl to --text-3xl (28-48px, using clamp)
- h3 / subsection titles: --text-xl (22-30px)
- h4 / card titles: --text-lg (18-22px)
- Body / descriptions: --text-base (16-18px)
- Secondary text: --text-sm (14-16px)
- Fine print / labels: --text-xs (12-14px)

For each file:
1. Read the file to identify all font-size declarations
2. Map each to the appropriate token based on its semantic role
3. Replace hardcoded values with var(--text-*) tokens
4. Leave any existing clamp() values if they already match the hierarchy
5. Leave intentionally-small decorative/label text (10-11px monospace) as-is

**single-product.css**: Product title -> --text-2xl, price -> clamp(24-36px), description -> --text-base, meta text -> --text-sm
**about.css**: Page title -> --text-3xl, section headings -> --text-2xl, body text -> --text-base
**contact.css**: Form labels -> --text-sm, headings -> --text-2xl, body -> --text-base
**generic-pages.css**: Headings -> --text-2xl, body -> --text-base
**woocommerce-single.css**: Product title -> --text-2xl, price -> similar to collection card price clamp

After all conversions, rebuild:
```bash
cd wordpress-theme/skyyrose-flagship && npm run build:css
```

Verify the build succeeds and minified files are updated.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy/wordpress-theme/skyyrose-flagship && npm run build:css 2>&1 | tail -5 && echo "=== Token adoption ===" && grep -rc "var(--text-" assets/css/single-product.css assets/css/about.css assets/css/contact.css assets/css/generic-pages.css assets/css/woocommerce-single.css</automated>
  </verify>
  <done>All template CSS files use design tokens for font sizing. Typography hierarchy is consistent: h1 > h2 > h3 > body > small follows the --text-* scale. CSS rebuild is clean with matching minified output.</done>
</task>

</tasks>

<verification>
1. Verify font sizes use var(--text-*) tokens or clamp() across all modified files (no remaining hardcoded 14-18px values in body text)
2. Verify heading hierarchy: grep for h1/h2/h3 related selectors and confirm they use progressively larger tokens
3. `npm run build:css` completes without errors
4. All .min.css files are up-to-date with source changes
</verification>

<success_criteria>
- Heading and body text scale smoothly from 320px mobile through 768px tablet to 1440px+ desktop
- Typography hierarchy (h1 > h2 > h3 > body > small) uses consistent token scale across all templates
- All modified files use design tokens from brand-variables.css, not hardcoded px values for body-range text
- CSS rebuild succeeds, minified files are current
</success_criteria>

<output>
After completion, create `.planning/phases/12-responsive-typography/12-02-SUMMARY.md`
</output>
