---
phase: 12-responsive-typography
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - RESP-01
  - RESP-02
  - RESP-03
  - RESP-04

must_haves:
  truths:
    - "REQUIREMENTS.md RESP-01..04 entries carry commit references (8ad0df313 / dfc4e1e94)"
    - "ROADMAP.md Phase 12 plan list descriptions match verification-only scope"
    - "ROADMAP.md Phase 12 plan list checkboxes are [x]"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "RESP-01..04 annotated with v1.1 commit SHAs"
      contains: "8ad0df313"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 plan list updated to verification scope"
      contains: "12-01-PLAN.md"
  key_links:
    - from: "REQUIREMENTS.md RESP-01..04"
      to: "git log --oneline"
      via: "commit SHAs 8ad0df313, dfc4e1e94"
      pattern: "8ad0df313|dfc4e1e94"
---

<objective>
Close Phase 12 in the planning artifacts by (1) annotating REQUIREMENTS.md
RESP-01..04 with the v1.1 commit SHAs that shipped the work, and (2) rewriting
the stale ROADMAP.md Phase 12 plan descriptions from old implementation-work
language to the actual verification-only scope, then flipping the checkboxes
to [x].

Purpose: Keeps planning artifacts honest — requirements trace to real commits,
ROADMAP reflects what was actually planned (not a stale copy from a prior
planning cycle).

Output: Updated .planning/REQUIREMENTS.md and .planning/ROADMAP.md.
No theme files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Known commit SHAs from v1.1 responsive/typography cycle — verified in planning session -->

8ad0df313  — v6.2.0 collection page rebuild (RESP-01, RESP-02, RESP-04 primary)
dfc4e1e94  — v4.1.0 WCAG 2.2 pass (RESP-03 touch-targets; also covers RESP-01 font-size audit)
b5ee01217  — additional responsive candidate (search git log to confirm scope)
d9e5154b7  — additional responsive candidate (search git log to confirm scope)
e5e80d6d4  — additional responsive candidate (search git log to confirm scope)
61e42abe0  — additional responsive candidate (search git log to confirm scope)

<!-- Current ROADMAP Phase 12 plan list (stale — to be rewritten) -->
- [ ] 12-01-PLAN.md -- Fix horizontal overflow at 320px and touch targets to 44x44px minimum (RESP-02, RESP-03)
- [ ] 12-02-PLAN.md -- Replace hardcoded font sizes with clamp()/design tokens and enforce typography hierarchy (RESP-01, RESP-04)

<!-- Target ROADMAP Phase 12 plan list (after this plan executes) -->
- [x] 12-01-PLAN.md -- Regression gate: clamp() token assertions + 320px inline-width scan (RESP-01, RESP-02)
- [x] 12-02-PLAN.md -- Planning artifact closure: annotate RESP-01..04 with v1.1 commits + update ROADMAP (RESP-01..04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify commit SHAs and confirm RESP mapping</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Run the following git log commands to confirm which commits are responsible for
each RESP requirement. Do NOT guess — read the actual output:

```
git log --oneline --all | grep -i "responsive\|typography\|overflow\|touch\|font\|clamp\|wcag\|v4.1\|v6.2" | head -20
git show --stat 8ad0df313 | head -20
git show --stat dfc4e1e94 | head -20
```

From the output, determine:
- Which SHA(s) introduced clamp() tokens / fluid font scale (RESP-01)
- Which SHA(s) fixed 320px overflow (RESP-02)
- Which SHA(s) set 44x44px touch targets (RESP-03)
- Which SHA(s) enforced typography hierarchy (RESP-04)

Primary assignments from planning context (confirm via git show):
- RESP-01 (fluid font scale): 8ad0df313 (v6.2.0), dfc4e1e94 (v4.1.0)
- RESP-02 (320px overflow): 8ad0df313 (v6.2.0)
- RESP-03 (44px touch targets): dfc4e1e94 (v4.1.0)
- RESP-04 (typography hierarchy): 8ad0df313 (v6.2.0), dfc4e1e94 (v4.1.0)

Then edit `.planning/REQUIREMENTS.md`. For each of RESP-01..04, append the
commit reference inline after the existing entry text using the format:
`— Complete (v1.1, commits {SHA1} + {SHA2})`

The four entries currently read (abbreviated):
- [x] **RESP-01**: Font sizes scale appropriately...
- [x] **RESP-02**: No horizontal overflow...
- [x] **RESP-03**: Touch targets meet minimum 44x44px...
- [x] **RESP-04**: Typography hierarchy is consistent...

After your edit they must read exactly:
- [x] **RESP-01**: Font sizes scale appropriately across mobile/tablet/desktop breakpoints — Complete (v1.1, commits 8ad0df313 + dfc4e1e94)
- [x] **RESP-02**: No horizontal overflow or layout breaking on mobile devices (320px+) — Complete (v1.1, commit 8ad0df313)
- [x] **RESP-03**: Touch targets meet minimum 44x44px on mobile — Complete (v1.1, commit dfc4e1e94)
- [x] **RESP-04**: Typography hierarchy is consistent across all page templates — Complete (v1.1, commits 8ad0df313 + dfc4e1e94)

If git show reveals a different SHA is the correct primary commit for any
requirement, use the real SHA instead — don't blindly apply the planning
context values if git evidence contradicts them.

Do not touch any other line in REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -c "Complete (v1.1" .planning/REQUIREMENTS.md | grep -q "^4$" && echo "PASS: 4 RESP entries annotated" || echo "FAIL: expected 4"</automated>
  </verify>
  <done>REQUIREMENTS.md RESP-01..04 each have "Complete (v1.1, commit..." annotation with real SHA(s).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ROADMAP Phase 12 plan list and flip checkboxes</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Read `.planning/ROADMAP.md` and locate the Phase 12 section (search for
`### Phase 12` or `12-responsive-typography`).

The current plan list entries are stale implementation-work descriptions from
a prior planning cycle. Replace BOTH lines with the correct verification-only
descriptions AND flip checkboxes from `[ ]` to `[x]`:

CURRENT (stale — remove):
```
- [ ] 12-01-PLAN.md -- Fix horizontal overflow at 320px and touch targets to 44x44px minimum (RESP-02, RESP-03)
- [ ] 12-02-PLAN.md -- Replace hardcoded font sizes with clamp()/design tokens and enforce typography hierarchy (RESP-01, RESP-04)
```

REPLACE WITH:
```
- [x] 12-01-PLAN.md -- Regression gate: clamp() token assertions + 320px inline-width scan (RESP-01, RESP-02)
- [x] 12-02-PLAN.md -- Planning artifact closure: annotate RESP-01..04 with v1.1 commits + update ROADMAP (RESP-01..04)
```

Also update the `**Plans:**` count line if it exists (keep as-is if already
says `2 plans` — Phase 12 has exactly 2 plans).

Also update the phase-level status marker if ROADMAP uses a `[ ]` / `[x]`
checkbox next to `### Phase 12` itself — flip it to `[x]` only if ALL
requirements (RESP-01..04) are satisfied, which they are.

Do not touch any other section of ROADMAP.md.
  </action>
  <verify>
    <automated>grep "12-01-PLAN.md" .planning/ROADMAP.md | grep -q "\[x\]" && grep "12-02-PLAN.md" .planning/ROADMAP.md | grep -q "\[x\]" && echo "PASS: both plan entries checked" || echo "FAIL"</automated>
  </verify>
  <done>
    ROADMAP.md Phase 12 plan list shows [x] for both plans with verification-only
    scope descriptions. Old "Fix horizontal overflow" / "Replace hardcoded font sizes"
    language is gone.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit planning artifact closure</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Stage and commit only the two planning files:

```
git add .planning/REQUIREMENTS.md .planning/ROADMAP.md
git commit -m "docs(phase-12): annotate RESP-01..04 with v1.1 commit SHAs + close ROADMAP entries"
```

Do not stage any other file. Do not push.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "phase-12" && echo "PASS" || echo "FAIL: commit missing"</automated>
  </verify>
  <done>One commit on main. Only .planning/REQUIREMENTS.md and .planning/ROADMAP.md changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git log output → planner | Commit SHAs from git log are ground truth; planning context SHAs are weak priors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | REQUIREMENTS.md edit | accept | Read-only planning docs; no auth surface; internal repo only |
| T-12-02-02 | Information Disclosure | git show output | accept | Commit metadata is non-sensitive; no secrets in commit messages |
</threat_model>

<verification>
After execution:

```bash
grep -A2 "RESP-01" .planning/REQUIREMENTS.md | grep "Complete (v1.1"
grep -A2 "RESP-02" .planning/REQUIREMENTS.md | grep "Complete (v1.1"
grep -A2 "RESP-03" .planning/REQUIREMENTS.md | grep "Complete (v1.1"
grep -A2 "RESP-04" .planning/REQUIREMENTS.md | grep "Complete (v1.1"
grep "12-01-PLAN.md" .planning/ROADMAP.md
grep "12-02-PLAN.md" .planning/ROADMAP.md
git log --oneline -1
```

All six grep lines should return matches. git log shows the closure commit.
</verification>

<success_criteria>
- REQUIREMENTS.md: RESP-01..04 each annotated with real v1.1 commit SHAs
- ROADMAP.md: Phase 12 plan list shows [x] for both plans with accurate descriptions
- One atomic commit: `docs(phase-12): annotate RESP-01..04 with v1.1 commit SHAs + close ROADMAP entries`
- Zero theme files modified
- Zero paid API calls
</success_criteria>

<output>
After completion, create `.planning/phases/12-responsive-typography/12-02-SUMMARY.md`
using the standard summary template.
</output>
