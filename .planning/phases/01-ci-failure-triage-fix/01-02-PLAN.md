---
phase: 01-ci-failure-triage-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/security-gate.yml
  - .github/workflows/dast-scan.yml
autonomous: true
requirements:
  - CI-01

must_haves:
  truths:
    - "grep for continue-on-error: true across all 3 workflow files returns zero matches"
    - "Security scanning steps in ci.yml fail the job on any finding (no || true suppression)"
    - "Gitleaks and TruffleHog in security-gate.yml are hard failures with no suppression"
    - "pip-licenses in security-gate.yml uses a license allowlist instead of continue-on-error"
    - "DAST workflow steps have early-exit guards instead of continue-on-error"
    - "Deploy tag step is restructured as a post-deploy step that does not block the pipeline"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with zero continue-on-error directives"
    - path: ".github/workflows/security-gate.yml"
      provides: "Security gate with zero continue-on-error and zero || true on secrets scanners"
    - path: ".github/workflows/dast-scan.yml"
      provides: "DAST workflow with early-exit guards instead of continue-on-error"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "bandit/pip-audit/semgrep tools"
      via: "security step exit codes (no || true)"
      pattern: "bandit.*-ll(?!.*\\|\\| true)"
    - from: ".github/workflows/security-gate.yml"
      to: "gitleaks/trufflehog"
      via: "hard failure on secrets detection (no || true, no continue-on-error)"
      pattern: "gitleaks detect.*--verbose.*--redact$"
    - from: ".github/workflows/dast-scan.yml"
      to: "staging/run_dast_scan.sh"
      via: "early-exit guard checking script existence"
      pattern: "if \\[ ! -f"
---

<objective>
Remove all 17 continue-on-error: true directives from ci.yml (12), security-gate.yml (3), and dast-scan.yml (2), and remove companion || true suppressions from security tool commands.

Purpose: With all underlying failures fixed in Plan 01, the suppression directives are now safe to remove. After this plan, CI produces hard failures on real problems -- nothing is silently swallowed.
Output: Three workflow files with zero continue-on-error directives, using proper exit codes and early-exit guards.
</objective>

<execution_context>
@/Users/theceo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theceo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ci-failure-triage-fix/01-CONTEXT.md
@.planning/phases/01-ci-failure-triage-fix/01-RESEARCH.md
@.planning/phases/01-ci-failure-triage-fix/01-01-SUMMARY.md

<interfaces>
<!-- Research inventory: all 17 directives with line numbers and fix strategies -->

ci.yml (12 directives to remove):
| # | Line | Step | Fix |
|---|------|------|-----|
| 1 | 89 | Check black formatting | Remove directive (Plan 01 fixed underlying) |
| 2 | 93 | Check isort imports | Remove directive (was already clean) |
| 3 | 105 | Run mypy type checking | Remove directive (Plan 01 fixed config) |
| 4 | 216 | Run pip-audit | Remove directive AND || true; let gate step handle |
| 5 | 228 | Run bandit | Remove directive AND || true; let gate step handle |
| 6 | 233 | Run safety check | Remove directive; let gate step handle |
| 7 | 239 | Run semgrep | Remove directive AND || true; let gate step handle |
| 8 | 250 | Run custom security audit | Remove directive; if-file guard already exists |
| 9 | 327 | Run ESLint | Remove directive (Plan 01 migrated to eslint CLI) |
| 10 | 331 | Run TypeScript type check | Remove directive (was already clean) |
| 11 | 368 | Run Three.js collection tests | Remove directive (408 tests pass) |
| 12 | 680 | Create deployment tag | Remove directive; restructure as post-deploy/non-blocking |

security-gate.yml (3 directives to remove):
| # | Line | Step | Fix |
|---|------|------|-----|
| 13 | 44 | Run Gitleaks | Remove directive AND || true -- hard failure |
| 14 | 52 | Run TruffleHog | Remove directive AND || true -- hard failure |
| 15 | 135 | Check Python licenses | Remove directive; add license allowlist |

dast-scan.yml (2 directives to remove):
| # | Line | Step | Fix |
|---|------|------|-----|
| 16 | 57 | Run DAST scan | Remove directive; add early-exit guard |
| 17 | 105 | Compare with baseline | Remove directive; add early-exit guard |

|| true patterns to REMOVE (per research):
- ci.yml line 214: pip-audit JSON || true -> REMOVE
- ci.yml line 224: bandit || true -> REMOVE
- ci.yml line 237: semgrep || true -> REMOVE
- security-gate.yml line 43: gitleaks || true -> REMOVE
- security-gate.yml line 51: trufflehog || true -> REMOVE

|| true patterns to KEEP:
- ci.yml line 176: Codecov upload || true -> KEEP (optional, not a quality gate)
- security-gate.yml line 129: pip install || true -> KEEP (defensive install fallback)
- dast-scan.yml line 30: pip install requirements || true -> KEEP (requirements.txt may not exist)
- dast-scan.yml line 212: Slack webhook || true -> KEEP (notification failure should not break scan)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove continue-on-error from ci.yml and restructure security/deploy steps</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Remove all 12 continue-on-error: true directives from ci.yml and fix companion issues. Work through each step:

**Lint job (directives 1-3, lines ~89, 93, 105):**
- Remove `continue-on-error: true` from "Check black formatting" step
- Remove `continue-on-error: true` from "Check isort imports" step
- Remove `continue-on-error: true` from "Run mypy type checking" step
- These tools now pass clean after Plan 01 fixes

**Security job (directives 4-8, lines ~216, 228, 233, 239, 250):**
For pip-audit step:
- Remove `continue-on-error: true`
- Remove `|| true` from the JSON output command (line ~214)
- Keep the `|| echo "::warning::"` on the text output line as an informational message, but change to proper exit code handling:
  ```yaml
  - name: Run pip-audit (dependency vulnerabilities)
    id: pip_audit
    run: |
      pip-audit --format=json --output=pip-audit-results.json
      pip-audit --strict 2>&1 | tee pip-audit.txt
  ```

For bandit step:
- Remove `continue-on-error: true`
- Remove `|| true` from the JSON output command (line ~224)
- Clean up to:
  ```yaml
  - name: Run bandit (code security)
    id: bandit
    run: |
      bandit -r . \
        -x ./tests,./venv,./.venv,./node_modules \
        -f json -o bandit-results.json \
        -ll
      bandit -r . \
        -x ./tests,./venv,./.venv,./node_modules \
        -ll 2>&1 | tee bandit.txt
  ```

For safety step:
- Remove `continue-on-error: true`
- Clean up to:
  ```yaml
  - name: Run safety check
    run: |
      safety check --full-report 2>&1 | tee safety.txt
  ```

For semgrep step:
- Remove `continue-on-error: true`
- Remove `|| true` from the JSON output command (line ~237)
- Clean up to:
  ```yaml
  - name: Run semgrep (SAST)
    run: |
      semgrep scan --config=auto --json --output=semgrep-results.json .
      semgrep scan --config=auto . 2>&1 | tee semgrep.txt
    env:
      SEMGREP_RULES: "p/python"
  ```

For custom security audit step:
- Remove `continue-on-error: true`
- The if-file guard already exists; no other change needed

**KEEP the "Fail on critical vulnerabilities" gate step** (lines 287-297) as defense-in-depth per user decision.

**Frontend Tests job (directives 9-11, lines ~327, 331, 368):**
- Remove `continue-on-error: true` from "Run ESLint" step
- Remove `continue-on-error: true` from "Run TypeScript type check" step
- Remove `continue-on-error: true` from "Run Three.js collection tests" step
- All three now pass clean after Plan 01 fixes

**Deploy Production job (directive 12, line ~680):**
- Remove `continue-on-error: true` from "Create deployment tag" step
- Restructure per user decision (tag failure should not block deploy):
  ```yaml
  - name: Create deployment tag
    if: success()
    run: |
      TAG="deploy-$(date +%Y%m%d-%H%M%S)-${GITHUB_SHA::7}"
      git tag -a "$TAG" -m "Production deployment ${GITHUB_SHA::7}"
      git push origin "$TAG" || echo "::warning::Could not push tag $TAG"
  ```
  The `if: success()` ensures it only runs after a successful deploy. The `|| echo` on `git push` is acceptable because tag push failure is informational, not a quality gate. The `git tag` command itself does NOT have `|| true` -- if tag creation fails, the step fails, which is correct since the `if: success()` condition already gates on deploy success.

**PRESERVE these patterns (do NOT change):**
- All `if: always()` on artifact upload steps
- SHA-pinned action references
- Codecov upload `|| true` (line 176) -- optional, not a quality gate
- Concurrency control settings
- Redis service container configuration
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && grep -c 'continue-on-error: true' .github/workflows/ci.yml | grep -q '^0$' && echo "PASS: zero continue-on-error in ci.yml"</automated>
  </verify>
  <done>ci.yml has zero continue-on-error: true directives. Security tool commands have no || true suppression (except Codecov). Deploy tag is restructured as non-blocking post-deploy. Gate step preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove continue-on-error from security-gate.yml and dast-scan.yml</name>
  <files>.github/workflows/security-gate.yml, .github/workflows/dast-scan.yml</files>
  <action>
**security-gate.yml (3 directives):**

Gitleaks step (directive 13, line ~44):
- Remove `continue-on-error: true`
- Remove `|| true` from the gitleaks command
- Per user decision: secrets in code should be a hard failure
- Result:
  ```yaml
  - name: Run Gitleaks
    run: gitleaks detect --source . --verbose --redact
  ```

TruffleHog step (directive 14, line ~52):
- Remove `continue-on-error: true`
- Remove `|| true` from the trufflehog command
- Per user decision: secrets in code should be a hard failure
- Result:
  ```yaml
  - name: Run TruffleHog
    run: trufflehog git file://. --only-verified
  ```

License check step (directive 15, line ~135):
- Remove `continue-on-error: true`
- Remove `|| echo "::warning::Found copyleft licenses"` from pip-licenses command
- Add a proper license allowlist per user decision:
  ```yaml
  - name: Check Python licenses
    run: |
      pip-licenses --format=markdown --with-urls > licenses.md
      pip-licenses --fail-on="GPL-3.0-only;AGPL-3.0-only;GPL-3.0-or-later;AGPL-3.0-or-later" \
        --allow-only="MIT;BSD-2-Clause;BSD-3-Clause;Apache-2.0;ISC;PSF-2.0;Python-2.0;MPL-2.0;Unlicense;LGPL-2.1-only;LGPL-3.0-only;Public Domain"
  ```
  Note: Include LGPL in the allow-only list since LGPL is permissive for library usage (not copyleft like GPL). The --fail-on targets full GPL and AGPL only.

**PRESERVE in security-gate.yml:**
- `pip install -e ".[dev]" || pip install -e . || true` on line ~129 -- defensive install fallback, KEEP
- All SHA-pinned action references
- CodeQL and dependency-review jobs (no continue-on-error to remove there)
- SBOM generation job (no continue-on-error there)

**dast-scan.yml (2 directives):**

DAST scan step (directive 16, line ~57):
- Remove `continue-on-error: true`
- Add early-exit guard for missing script per user decision:
  ```yaml
  - name: Run DAST scan
    id: dast_scan
    env:
      STAGING_URL: ${{ env.STAGING_URL }}
      ZAP_API_KEY: ${{ secrets.ZAP_API_KEY || 'changeme' }}
      MAX_SCAN_DURATION: 3600
      NUCLEI_TIMEOUT: 30m
      RUN_PARALLEL: true
    run: |
      if [ ! -f "staging/run_dast_scan.sh" ]; then
        echo "::notice::DAST scan script (staging/run_dast_scan.sh) not yet provisioned. Skipping."
        exit 0
      fi
      chmod +x staging/run_dast_scan.sh
      ./staging/run_dast_scan.sh
  ```

Compare with baseline step (directive 17, line ~105):
- Remove `continue-on-error: true`
- Add early-exit guard for missing script:
  ```yaml
  - name: Compare with baseline
    if: always()
    run: |
      LATEST_COMBINED=$(ls -t ${{ env.REPORTS_DIR }}/combined_vulnerability_report_*.json 2>/dev/null | head -1)
      BASELINE="${{ env.REPORTS_DIR }}/vulnerability_baseline.json"

      if [ ! -f "$BASELINE" ] || [ -z "$LATEST_COMBINED" ] || [ ! -f "$LATEST_COMBINED" ]; then
        echo "::notice::No baseline or scan report found, skipping comparison."
        exit 0
      fi

      if [ ! -f "staging/compare_baseline.py" ]; then
        echo "::notice::Baseline comparison script (staging/compare_baseline.py) not yet provisioned. Skipping."
        exit 0
      fi

      python3 staging/compare_baseline.py \
        "$LATEST_COMBINED" \
        "$BASELINE" \
        "${{ env.REPORTS_DIR }}/vulnerability_delta_$(date +%Y%m%d_%H%M%S).json"
  ```

**PRESERVE in dast-scan.yml:**
- `pip install --no-cache-dir -r requirements.txt || true` (line ~30) -- requirements.txt may not exist, KEEP
- `"$SLACK_WEBHOOK_URL" || true` (line ~212) -- notification failure should not break scan, KEEP
- All `if: always()` on artifact uploads
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && TOTAL=$(grep -c 'continue-on-error: true' .github/workflows/ci.yml .github/workflows/security-gate.yml .github/workflows/dast-scan.yml 2>/dev/null | tail -1 | cut -d: -f2) && test "$TOTAL" = "0" && echo "PASS: zero continue-on-error across all 3 files"</automated>
  </verify>
  <done>security-gate.yml has zero continue-on-error: true directives. Gitleaks and TruffleHog are hard failures. pip-licenses uses allowlist. dast-scan.yml has zero continue-on-error: true directives. Both DAST steps use early-exit guards for missing scripts. All preserved patterns (defensive || true on installs, Slack webhook, artifact uploads) remain intact.</done>
</task>

</tasks>

<verification>
Final phase-gate verification -- run after both tasks complete:

```bash
cd /Users/theceo/DevSkyy

# Phase gate: zero continue-on-error across all workflow files
echo "=== CI-01: continue-on-error count ==="
grep -c 'continue-on-error: true' .github/workflows/ci.yml .github/workflows/security-gate.yml .github/workflows/dast-scan.yml
# Expected: all three lines show :0

# Verify no accidental || true on security tools (should have been removed)
echo "=== Verify security tool || true removed ==="
grep -n '|| true' .github/workflows/ci.yml | grep -v 'codecov\|Codecov'
# Expected: no output (only Codecov || true should remain)

grep -n '|| true' .github/workflows/security-gate.yml | grep -v 'pip install'
# Expected: no output (only pip install fallback should remain)

# Verify DAST early-exit guards exist
echo "=== Verify DAST guards ==="
grep -c 'if \[ ! -f' .github/workflows/dast-scan.yml
# Expected: 2 (one for run_dast_scan.sh, one for compare_baseline.py)

# CI-02: Verify all tools still pass (from Plan 01)
echo "=== CI-02: Tool checks ==="
black --check . && echo "PASS: black"
isort --check-only . && echo "PASS: isort"
ruff check . && echo "PASS: ruff"
mypy . --ignore-missing-imports && echo "PASS: mypy"
cd frontend && npx eslint . && echo "PASS: eslint"
npx tsc --noEmit && echo "PASS: tsc"
cd /Users/theceo/DevSkyy && npm run test:collections && echo "PASS: three.js"
```
</verification>

<success_criteria>
- Zero `continue-on-error: true` directives across ci.yml, security-gate.yml, and dast-scan.yml (17 removed)
- Security tool commands in ci.yml have no `|| true` suppression (except Codecov upload)
- Gitleaks and TruffleHog are hard failures (no `|| true`, no `continue-on-error`)
- pip-licenses uses explicit allowlist instead of suppression
- DAST steps use early-exit guards for missing scripts
- Deploy tag restructured as non-blocking post-deploy step
- All underlying tools still pass clean (black, isort, ruff, mypy, eslint, tsc, three.js tests)
</success_criteria>

<output>
After completion, create `.planning/phases/01-ci-failure-triage-fix/01-02-SUMMARY.md`
</output>
