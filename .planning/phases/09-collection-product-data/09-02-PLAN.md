---
phase: 09-collection-product-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_live_structure.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Live https://skyyrose.co/collection-black-rose/ renders an img src containing sr-collection-black-rose.webp"
    - "The page wrapper contains data-collection=\"black-rose\""
    - "REQUIREMENTS.md DATA-01 checkbox is [x] (closed)"
    - "ROADMAP.md Phase 9 plan checkboxes are [x]"
  artifacts:
    - path: "scripts/verify_live_structure.py"
      provides: "Extended with Black Rose hero asset assertion (substring match on img src)"
      contains: "sr-collection-black-rose.webp"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DATA-01 marked [x]"
      contains: "[x] **DATA-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 plan checkboxes closed"
      contains: "[x] 09-01-PLAN.md"
  key_links:
    - from: "scripts/verify_live_structure.py"
      to: "https://skyyrose.co/collection-black-rose/"
      via: "HTTP GET (read-only curl)"
      pattern: "sr-collection-black-rose\\.webp"
---

<objective>
Verify the Black Rose hero banner is correct on live production (DATA-01), then close the stale requirement checkbox in REQUIREMENTS.md and update ROADMAP.md plan checkboxes. No code fix required — live diagnostic already confirms the hero asset is correct; this plan generates a repeatable assertion and records the verified state.

Purpose: DATA-01 checkbox in REQUIREMENTS.md is stale (marked [  ] pending) but live site already serves the correct asset. This plan proves it with an automated assertion, then closes the paperwork.
Output: Extended verify_live_structure.py + updated REQUIREMENTS.md + updated ROADMAP.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-collection-product-data/09-CONTEXT.md
@.planning/phases/09-collection-product-data/09-UI-SPEC.md
@.planning/phases/09-collection-product-data/09-PATTERNS.md

<interfaces>
<!-- From scripts/verify_live_structure.py (517 lines, extend — do not rewrite) -->
<!-- Key structures to reuse: -->

```python
# Assertion dataclass (already imported at top of file)
@dataclass
class Assertion:
    selector: str       # CSS selector for soup.select()
    min_count: int      # assertion: len(matches) >= min_count
    label: str          # human-readable label for error output
    max_count: int = 9999  # assertion: len(matches) <= max_count

# COLLECTION_CARD_FLOORS dict already present:
# COLLECTION_CARD_FLOORS = {"black-rose": N, "love-hurts": N, "signature": N, ...}

# Pattern for per-page assertion lists:
# BLACK_ROSE_ASSERTIONS = [Assertion(...), ...]
# run_page_assertions(url, assertions, label) executes and reports
```

<!-- Hero asset verification requires src attribute substring match, not just selector presence -->
<!-- Use a custom check function or soup.find("img", src=re.compile(...)) after fetching the page -->
<!-- The Assertion dataclass checks element count, not attribute values — extend with a helper -->

<!-- UI-SPEC hero pass conditions (09-UI-SPEC.md): -->
<!-- img src contains: sr-collection-black-rose.webp -->
<!-- page wrapper: data-collection="black-rose" -->
<!-- NO crimson or warm tone verification possible via curl (visual only — omit from automated check) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend verify_live_structure.py with Black Rose hero asset assertion</name>
  <files>scripts/verify_live_structure.py</files>
  <action>
Read `scripts/verify_live_structure.py` in full first (517 lines). Identify:
- Where per-collection assertion lists are defined (e.g. `BLACK_ROSE_ASSERTIONS`)
- The `run_page_assertions()` or equivalent runner function signature
- Whether a `check_attr_contains()` helper already exists for src substring matching

Add a `check_hero_asset(html_content, expected_substring, label)` helper function (or equivalent name matching the file's existing style) that:
- Parses the HTML with BeautifulSoup (already imported)
- Finds all `<img>` elements whose `src` attribute contains `expected_substring`
- Returns `(passed: bool, detail: str)` — True if at least one such img is found, False with a descriptive message if not

Add a `check_data_collection_attr(html_content, expected_value, label)` helper that:
- Finds any element with `data-collection` attribute equal to `expected_value`
- Returns `(passed: bool, detail: str)`

In the Black Rose page assertion block (create one if it doesn't exist for collection-black-rose), add calls to both helpers:
- `check_hero_asset(html, "sr-collection-black-rose.webp", "Black Rose hero asset")`
- `check_data_collection_attr(html, "black-rose", "data-collection attribute")`

The URL to fetch is `https://skyyrose.co/collection-black-rose/` (read-only GET — no auth, no writes, no paid API calls).

Wire the new checks into whatever reporting mechanism the file uses (print-based or return-value-based — match existing style). Do not change the overall file structure, CLI args, or existing assertions.

STOP AND SHOW: if, after running the script, the assertions fail and you conclude a version bump + redeploy is needed, you MUST stop and print the exact action before executing. Per CLAUDE.md: any `deploy-theme.sh` execution requires explicit "y" from the user first.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python scripts/verify_live_structure.py 2>&1 | grep -i "black.rose\|hero\|data-collection\|PASS\|FAIL" | head -20</automated>
  </verify>
  <done>Script runs without error; Black Rose hero asset assertion shows PASS with sr-collection-black-rose.webp confirmed in img src; data-collection="black-rose" assertion passes</done>
</task>

<task type="auto">
  <name>Task 2: Close DATA-01 in REQUIREMENTS.md and update ROADMAP.md plan checkboxes</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Update `.planning/REQUIREMENTS.md`:
- Find the DATA-01 line (currently `- [ ] **DATA-01**` — Black Rose hero banner). Change `[ ]` to `[x]`.
- Find the Phase 9 success criteria item 1 (line ~169): change status from "Pending" to "Complete" if a status marker exists, or update the narrative to reflect verified state.

Update `.planning/ROADMAP.md`:
- Find the Phase 9 Plans block (lines ~173-175):
  ```
  Plans:
  - [ ] 09-01-PLAN.md -- Filter pre-orders from catalog grids and sync product data (DATA-02, DATA-03)
  - [ ] 09-02-PLAN.md -- Diagnose and fix Black Rose hero banner (DATA-01)
  ```
- Change both `[ ]` to `[x]`.
- Update the plan descriptions to reflect actual scope (verify, not fix/diagnose):
  - 09-01-PLAN.md: "Verify pre-order filter and cross-collection integrity via CSV integrity test (DATA-02, DATA-03)"
  - 09-02-PLAN.md: "Verify Black Rose hero asset on live + close stale DATA-01 requirement (DATA-01)"

Read both files before editing to get exact line content. Use Edit (targeted) not Write (full rewrite) for these changes.
  </action>
  <verify>
    <automated>grep -n "DATA-01\|09-01-PLAN\|09-02-PLAN" /Users/theceo/DevSkyy/.planning/REQUIREMENTS.md /Users/theceo/DevSkyy/.planning/ROADMAP.md</automated>
  </verify>
  <done>REQUIREMENTS.md shows `[x] **DATA-01**`; ROADMAP.md shows `[x] 09-01-PLAN.md` and `[x] 09-02-PLAN.md`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → skyyrose.co | Outbound HTTP GET only; no auth tokens sent, no writes, read-only |
| planning files → git | Local file edits; checkbox closure is a documentation-only change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | HTTP GET to skyyrose.co | accept | Read-only curl of public page; no credentials in flight |
| T-09-02-02 | Tampering | REQUIREMENTS.md / ROADMAP.md | accept | Git-controlled; any bad edit is reversible via revert |
</threat_model>

<verification>
1. `python scripts/verify_live_structure.py` — Black Rose hero asset and data-collection assertions PASS
2. `grep "\[x\].*DATA-01" .planning/REQUIREMENTS.md` — returns a match
3. `grep "\[x\].*09-0[12]-PLAN" .planning/ROADMAP.md` — returns two matches
</verification>

<success_criteria>
- verify_live_structure.py extended with hero asset substring check (no rewrite of existing structure)
- Live assertion confirms sr-collection-black-rose.webp in Black Rose page img src
- REQUIREMENTS.md DATA-01 checkbox is [x]
- ROADMAP.md Phase 9 plan checkboxes both [x]
- No deploy executed (live site already correct — verification only)
- No paid API calls made
</success_criteria>

<output>
After completion, create `.planning/phases/09-collection-product-data/09-02-SUMMARY.md`
</output>
