---
phase: 13-luxury-cursor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - scripts/verify_live_structure.py
autonomous: true
requirements:
  - CURS-01
  - CURS-02
  - CURS-03
must_haves:
  truths:
    - "CURS-01 entry in REQUIREMENTS.md references the specific commit that shipped z-index supremacy"
    - "CURS-02 entry references the commit that shipped modal-pause MutationObserver"
    - "CURS-03 entry is annotated as GAP (not rubber-stamped) with fix pointer"
    - "ROADMAP.md Phase 13 reflects CURS-01/CURS-02 complete, CURS-03 open"
    - "verify_live_structure.py asserts cursor JS present on front-page"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Commit-annotated CURS-01..03 entries"
      contains: "97e226804"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 status with CURS-03 gap noted"
      contains: "CURS-03"
    - path: "scripts/verify_live_structure.py"
      provides: "Live assertion: luxury-cursor script tag on front-page"
      contains: "luxury-cursor"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "git log"
      via: "commit sha references inline in requirement entries"
      pattern: "97e226804|0632f5897|69d603b34"
---

<objective>
Annotate REQUIREMENTS.md CURS-01..03 with commit references. Update ROADMAP.md Phase 13 to reflect CURS-01/CURS-02 complete and CURS-03 as an open gap. Add a live-page assertion for luxury-cursor presence on front-page to scripts/verify_live_structure.py.

Purpose: Close the documentation loop for shipped cursor work. Surface CURS-03 gap clearly so it is not silently treated as done.
Output: Three files updated. No code shipped. No deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Cursor commit references — confirmed from git log this session. -->

Commits relevant to cursor phase:
  97e226804  fix(13-01): cursor z-index supremacy, modal-paused state, dead selector cleanup
             → CURS-01 primary: z-index: 99999 for .cursor-dot/.cursor-ring
  47ee2fa9d  feat(13-01): modal-aware cursor pause/resume with MutationObserver
             → CURS-02 primary: pause/resume on modal open/close
  a94bdd279  feat(13-01): modal-aware cursor pause/resume with MutationObserver
             → CURS-02 secondary (earlier iteration)
  0632f5897  feat: size guide modal, form feedback states, custom cursor
             → CURS-01/CURS-02 foundational: initial cursor + modal interaction
  69d603b34  fix: cursor disappearing — Jetpack instant search overlay blocks pointer events
             → CURS-01 hardening: pointer-events fix for Jetpack overlay

CURS-03 status: GAP — cursor JS loaded globally in skyyrose_enqueue_global_scripts() with
  no immersive slug exclusion. Current code confirmed at inc/enqueue.php lines 249-259.
  Do NOT annotate CURS-03 as [x] complete. Annotate as [ ] with gap note.

verify_live_structure.py pattern (from existing assertions in that file):
  Assertion(
      url="https://skyyrose.co/",
      selector="script[src*='luxury-cursor']",
      description="Luxury cursor JS loaded on front-page",
      expected_present=True,
  )
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate CURS-01..03 in REQUIREMENTS.md with commit refs and gap note</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read .planning/REQUIREMENTS.md. Locate the CURS-01, CURS-02, CURS-03 entries.

Update each entry IN-PLACE (preserve surrounding content):

CURS-01 — currently marked [x]. Append commit reference after the requirement text:
  `<!-- shipped: 97e226804 (z-index: 99999 supremacy), 69d603b34 (Jetpack pointer-events fix), 0632f5897 (initial cursor) -->`

CURS-02 — currently marked [x]. Append commit reference:
  `<!-- shipped: 47ee2fa9d (MutationObserver modal pause/resume), a94bdd279 (earlier iteration) | Playwright test deferred to v1.3 per CONTEXT.md -->`

CURS-03 — currently marked [x] but INCORRECT. Change to [ ] and append gap note:
  `<!-- GAP: cursor JS enqueued globally in skyyrose_enqueue_global_scripts() — no immersive slug exclusion gate. Fix: move to skyyrose_enqueue_template_scripts() with $slug !== "immersive" guard. Test: tests/test_luxury_cursor.py::test_cursor_not_loaded_on_immersive (currently FAILS). -->`

Do not alter any other line in REQUIREMENTS.md. The comment annotations go on the line immediately following the requirement text line (or inline if the entry is a single line).
  </action>
  <verify>
    <automated>grep -n "97e226804\|CURS-03 GAP\|MutationObserver" /Users/theceo/DevSkyy/.planning/REQUIREMENTS.md | head -10</automated>
  </verify>
  <done>
    - CURS-01 entry has commit sha 97e226804 in an HTML comment
    - CURS-02 entry has commit sha 47ee2fa9d in an HTML comment
    - CURS-03 entry is changed from [x] to [ ] with "GAP:" annotation naming the fix
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 13 and add verify_live_structure.py assertion</name>
  <files>.planning/ROADMAP.md, scripts/verify_live_structure.py</files>
  <action>
**ROADMAP.md update:**
Read .planning/ROADMAP.md. Find the Phase 13 section (search for "13-luxury-cursor" or "Phase 13").

In the Phase 13 plans list, ensure both plan entries are present:
  - [ ] 13-01-PLAN.md — pytest tests for CURS-01 (passes) and CURS-03 (fails — gap surfaced)
  - [ ] 13-02-PLAN.md — annotate REQUIREMENTS.md with commits, surface CURS-03 gap

In the Phase 13 success criteria or notes block, add or update:
  ```
  Status (v1.1 delivery audit):
  - CURS-01 [x] z-index supremacy shipped — commit 97e226804
  - CURS-02 [x] modal-pause MutationObserver shipped — commit 47ee2fa9d (Playwright test deferred v1.3)
  - CURS-03 [ ] OPEN GAP — cursor JS enqueued globally; immersive slug gate not implemented
  ```

Do not touch any other phase entries.

**verify_live_structure.py update:**
Read scripts/verify_live_structure.py. Find where Assertion objects are defined for the front-page (url="https://skyyrose.co/" assertions). Add the following assertion to that group:

  Assertion(
      url="https://skyyrose.co/",
      selector="script[src*='luxury-cursor']",
      description="CURS-01/CURS-03 — luxury cursor JS present on front-page (global enqueue confirmed)",
      expected_present=True,
  )

If no front-page assertion group exists, add it after the last existing Assertion block before any closing structure. Follow the exact indentation and formatting pattern of adjacent Assertion entries in the file.
  </action>
  <verify>
    <automated>grep -n "luxury-cursor\|CURS-03\|13-01-PLAN\|13-02-PLAN" /Users/theceo/DevSkyy/.planning/ROADMAP.md | head -15 && grep -n "luxury-cursor" /Users/theceo/DevSkyy/scripts/verify_live_structure.py</automated>
  </verify>
  <done>
    - ROADMAP.md Phase 13 shows both plan filenames
    - ROADMAP.md Phase 13 status block lists CURS-01 [x], CURS-02 [x], CURS-03 [ ] OPEN GAP
    - verify_live_structure.py contains an Assertion with selector containing "luxury-cursor" on the skyyrose.co/ URL
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → planner writes | Editing planning docs only; no production writes, no deploys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | REQUIREMENTS.md CURS-03 status | mitigate | Explicitly change [x] to [ ] — prevents false-complete signal reaching future phases |
</threat_model>

<verification>
grep -c "97e226804" /Users/theceo/DevSkyy/.planning/REQUIREMENTS.md  # must be >= 1
grep "CURS-03" /Users/theceo/DevSkyy/.planning/REQUIREMENTS.md       # must show [ ] not [x]
grep "luxury-cursor" /Users/theceo/DevSkyy/scripts/verify_live_structure.py  # must match
</verification>

<success_criteria>
- REQUIREMENTS.md: CURS-01 has commit 97e226804, CURS-02 has commit 47ee2fa9d, CURS-03 is [ ] with gap note
- ROADMAP.md: Phase 13 has plan list + status block with CURS-03 marked open
- verify_live_structure.py: Assertion for luxury-cursor on front-page URL present
- No production files touched, no deploy executed
</success_criteria>

<output>
After completion, create .planning/phases/13-luxury-cursor/13-02-SUMMARY.md
</output>
