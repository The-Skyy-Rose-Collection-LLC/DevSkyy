---
phase: 13-luxury-cursor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_luxury_cursor.py
autonomous: true
requirements:
  - CURS-01
  - CURS-03
must_haves:
  truths:
    - "CURS-01: luxury-cursor CSS z-index exceeds 9999 (renders above modals)"
    - "CURS-03 gap is surfaced: cursor JS is loaded globally (no immersive slug gate), test fails intentionally"
    - "Test suite runs with pytest and produces clear pass/fail output per requirement"
  artifacts:
    - path: "tests/test_luxury_cursor.py"
      provides: "CURS-01 z-index assertion + CURS-03 enqueue-gate assertion"
      exports: ["test_cursor_zindex_above_modals", "test_cursor_not_loaded_on_immersive"]
  key_links:
    - from: "tests/test_luxury_cursor.py"
      to: "wordpress-theme/skyyrose-flagship/inc/enqueue.php"
      via: "open() + regex parse of enqueue function bodies"
      pattern: "skyyrose_enqueue_global_scripts"
    - from: "tests/test_luxury_cursor.py"
      to: "wordpress-theme/skyyrose-flagship/assets/css/luxury-cursor.css"
      via: "open() + regex parse of z-index rules"
      pattern: "z-index:\\s*(\\d+)"
---

<objective>
Write pytest tests that encode the CURS-01 and CURS-03 requirements against the shipped theme files. CURS-01 passes (z-index: 99999 confirmed). CURS-03 fails — cursor JS is enqueued globally with no immersive slug gate; the failing test surfaces this gap for remediation.

Purpose: Requirements-driven verification, not implementation validation. Tests reflect what MUST be true, not what currently is.
Output: tests/test_luxury_cursor.py with two requirement-scoped test functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key values extracted from codebase — executor uses these directly. -->

From wordpress-theme/skyyrose-flagship/assets/css/luxury-cursor.css (lines 13-21):
  .cursor-dot, .cursor-ring { z-index: 99999; }
  CURS-01 assertion target: any rule applying z-index to .cursor-dot OR .cursor-ring must be > 9999.

From wordpress-theme/skyyrose-flagship/inc/enqueue.php:
  skyyrose_enqueue_global_styles()  — lines ~165-220 — enqueues luxury-cursor.css globally (no slug gate)
  skyyrose_enqueue_global_scripts() — lines ~230-280 — enqueues luxury-cursor.min.js globally (no slug gate)
  skyyrose_enqueue_template_styles() — lines ~290-400 — template-slug-gated enqueues
  Template slug map (lines ~370-391):
    'template-immersive-black-rose.php'    => 'immersive'
    'template-immersive-love-hurts.php'    => 'immersive'
    'template-immersive-signature.php'     => 'immersive'
    'template-immersive-kids-capsule.php'  => 'immersive'
  CURS-03 assertion target: luxury-cursor enqueue must be INSIDE a block conditioned on slug != 'immersive'.
  Current state: enqueue is in global functions — no slug gate exists. Test WILL FAIL.

From wordpress-theme/skyyrose-flagship/assets/css/immersive.css:
  No .cursor-dot or .cursor-ring rules found — immersive CSS does NOT hide the cursor.
  CURS-03 fails on both axes (load gate absent + CSS hide absent).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test_luxury_cursor.py with CURS-01 and CURS-03 assertions</name>
  <files>tests/test_luxury_cursor.py</files>
  <behavior>
    - test_cursor_zindex_above_modals: parse luxury-cursor.css, find z-index declarations on .cursor-dot or .cursor-ring selectors, assert each value > 9999. PASSES with current code (z-index: 99999).
    - test_cursor_not_loaded_on_immersive: parse inc/enqueue.php, find the body of skyyrose_enqueue_global_scripts(), assert that any luxury-cursor enqueue call within it is ABSENT or wrapped in a condition that excludes the 'immersive' slug. FAILS with current code (enqueue is unconditional) — this is the CURS-03 gap signal.
    - test_cursor_css_enqueued_globally: parse skyyrose_enqueue_global_styles() body, assert luxury-cursor.css IS enqueued (sanity check that CSS ships at all). PASSES.
  </behavior>
  <action>
Create tests/test_luxury_cursor.py. Use only stdlib (pathlib, re) — no pytest plugins beyond pytest itself.

File layout:
- MODULE_DIR constant = Path(__file__).parent.parent / "wordpress-theme/skyyrose-flagship"
- CSS_FILE = MODULE_DIR / "assets/css/luxury-cursor.css"
- ENQUEUE_FILE = MODULE_DIR / "inc/enqueue.php"

Helper extract_function_body(php_text, fn_name) -> str:
  Locate the function declaration by searching for `function {fn_name}`, then extract from the opening `{` to its matching closing `}` using a brace-depth counter. Return the body string.

test_cursor_zindex_above_modals():
  Read CSS_FILE. Find all occurrences of z-index property values using pattern r'z-index\s*:\s*(\d+)'. For each match, cast to int and assert > 9999. Assert at least one match found. Use pytest.fail with message "CURS-01: no z-index > 9999 found in luxury-cursor.css" if none qualify.

test_cursor_css_enqueued_globally():
  Read ENQUEUE_FILE. Extract body of skyyrose_enqueue_global_styles using extract_function_body. Assert the string 'luxury-cursor' appears in the body. Marks CSS as globally shipped (sanity).

test_cursor_not_loaded_on_immersive():
  Read ENQUEUE_FILE. Extract body of skyyrose_enqueue_global_scripts using extract_function_body.
  Check if 'luxury-cursor' appears in the body. If it does, the enqueue is unconditional — apply the slug-gate check:
    Search the body for a pattern like: if.*slug.*immersive or if.*immersive.*slug (i.e., a conditional that references both 'slug' and 'immersive') wrapping the luxury-cursor enqueue.
    If no such conditional is found wrapping the luxury-cursor enqueue block, fail with:
      pytest.fail(
          "CURS-03 GAP: luxury-cursor JS is enqueued in skyyrose_enqueue_global_scripts() "
          "with no immersive-slug exclusion gate. "
          "Fix: move enqueue to skyyrose_enqueue_template_scripts() with condition: "
          "if ($slug !== 'immersive') { wp_enqueue_script(...); }"
      )
  This test is EXPECTED TO FAIL against current code. That failure is the gap report.

Add a module-level docstring:
  \"\"\"
  Phase 13 — Luxury Cursor Verification
  CURS-01: cursor z-index > 9999 (above all modals/overlays)
  CURS-03: cursor JS must NOT load on immersive templates (slug gate required)

  test_cursor_not_loaded_on_immersive is expected to FAIL against current enqueue.php.
  This is intentional — the failing test surfaces the CURS-03 gap for remediation.
  Fix target: wrap luxury-cursor enqueue in skyyrose_enqueue_template_scripts() or
              add slug-conditional guard in skyyrose_enqueue_global_scripts().
  \"\"\"

No mocks. No external HTTP calls. Pure filesystem reads.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -m pytest tests/test_luxury_cursor.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    pytest output shows:
    - test_cursor_zindex_above_modals PASSED
    - test_cursor_css_enqueued_globally PASSED
    - test_cursor_not_loaded_on_immersive FAILED with message containing "CURS-03 GAP"
    All three tests run without import errors or fixture errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → test | Tests read local theme files; no network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | test_luxury_cursor.py path resolution | accept | MODULE_DIR anchored to __file__; tests fail if files absent rather than pass silently |
</threat_model>

<verification>
cd /Users/theceo/DevSkyy && python -m pytest tests/test_luxury_cursor.py -v
Expected: 2 PASSED, 1 FAILED (CURS-03 gap). No errors, no import failures.
</verification>

<success_criteria>
- tests/test_luxury_cursor.py exists and is importable
- CURS-01 test passes (z-index: 99999 confirmed)
- CURS-03 test fails with actionable gap message naming the fix
- pytest exits non-zero (expected — CURS-03 gap is real)
</success_criteria>

<output>
After completion, create .planning/phases/13-luxury-cursor/13-01-SUMMARY.md
</output>
