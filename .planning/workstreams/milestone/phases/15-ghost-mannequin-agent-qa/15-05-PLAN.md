---
phase: 15-ghost-mannequin-agent-qa
plan: "05"
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - skyyrose/elite_studio/agents/quality_agent.py
  - imagery/visual_comparison.py
autonomous: true
requirements:
  - QA-01
  - QA-04

must_haves:
  truths:
    - "QualityAgent.score_ghost_mannequin() returns a VetoResult with per-metric pass/fail per D-03"
    - "Silhouette IoU below 0.96 forces veto=True regardless of other metrics"
    - "Color delta-E above 2.0 (LAB) forces veto=True regardless of other metrics"
    - "CLIP semantic below 0.85 forces veto=True"
    - "Logo drift above 5 percent forces veto=True"
    - "Corner-pixel purity: any corner pixel outside 5 RGB units of (255,255,255) forces veto=True"
    - "visual_comparison.py thresholds promoted from log-only to hard vetoes for Phase 15 ghost mannequin scoring"
    - "D-01: Silhouette IoU floor is 0.96 — below this threshold forces veto=True"
    - "D-02: Techflat is the logo reference for logo drift scoring (not the live garment photo)"
    - "D-03: Per-metric structured vetoes — each metric reports independently in VetoResult"
  artifacts:
    - path: "skyyrose/elite_studio/agents/quality_agent.py"
      provides: "VetoResult dataclass + score_ghost_mannequin() method on QualityAgent"
      contains: "VetoResult"
    - path: "imagery/visual_comparison.py"
      provides: "corner_pixel_purity() function + hard-veto path for ghost mannequin calls"
      contains: "corner_pixel_purity"
  key_links:
    - from: "skyyrose/elite_studio/agents/quality_agent.py"
      to: "imagery/visual_comparison.py"
      via: "from imagery.visual_comparison import score_images, corner_pixel_purity"
      pattern: "visual_comparison"
    - from: "skyyrose/elite_studio/agents/quality_agent.py"
      to: "skyyrose/elite_studio/models.py"
      via: "from skyyrose.elite_studio.models import AuditEntry"
      pattern: "AuditEntry"
---

<objective>
Extend QualityAgent with per-metric structured vetoes (D-01 through D-04) and promote the visual_comparison.py thresholds from log-only warnings to hard gates for ghost mannequin scoring. Add corner-pixel purity check per QA-04.

Purpose: QA-01 requires per-metric veto logic — blended score is insufficient. QA-04 requires corner-pixel purity check. The existing quality_agent.py dual-judge (OpenAI + Gemini) provides the pattern-preservation and CLIP semantic pass/fail; this plan wires the numeric metric gates around it.
Output: VetoResult dataclass + score_ghost_mannequin() in quality_agent.py; corner_pixel_purity() + hard-veto path in visual_comparison.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-CONTEXT.md
@.planning/phases/15-ghost-mannequin-agent-qa/15-01-PLAN.md

<interfaces>
<!-- From existing quality_agent.py (per prior session reads):
  class QualityAgent(BaseSuperAgent):
    _PASS_THRESHOLD = 80
    _GHOST_QA_PROMPT: str  (existing template)
    async def verify(self, image_path: str, product_data: ProductData) -> QualityVerification
      # dual-judge: OpenAI + Gemini in parallel via asyncio.gather
      # Returns QualityVerification with score, recommendation, details
-->
<!-- From imagery/visual_comparison.py (per prior session reads):
  class ComparisonResult(BaseModel):
    ssim_score: float
    color_similarity: float
    perceptual_hash_score: float (if present)
    clip_similarity: float (if present)
  def score_images(image_a: bytes, image_b: bytes) -> ComparisonResult
  Existing thresholds 0.22 / 0.28 currently as log-only WARNING messages
-->
<!-- D-03 veto floors:
  Silhouette IoU >= 0.96
  Color delta-E (LAB, garment region) <= 2.0
  CLIP semantic similarity >= 0.85 (against flatlay)
  Pattern preservation: qualitative pass via Claude Sonnet QA
  Logo position/scale: <= 5% drift from techflat reference
  Corner-pixel purity (QA-04): all four corners within 5 RGB units of (255, 255, 255)
-->
<!-- AuditEntry from 15-01 (9 fields):
  run_id: str, sku: str, timestamp: str, tier: str, cost_usd: float,
  passed_qa: bool, score_breakdown: dict, failure_class: str,
  bg_removal_model: str  (e.g. "briaai/RMBG-2.0"; empty string if not applied)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add corner_pixel_purity() to visual_comparison.py</name>
  <files>imagery/visual_comparison.py</files>
  <read_first>
    Read imagery/visual_comparison.py in full (description says it's the SSIM+color+hash+CLIP engine). Note:
    - exact class name of ComparisonResult (BaseModel or dataclass?)
    - existing import block (PIL, numpy, torch, sklearn, etc.)
    - existing threshold constants (0.22 and 0.28 noted in CONTEXT.md)
    - where to safely append the new function without touching existing code
  </read_first>
  <action>
    Append the following new standalone function to imagery/visual_comparison.py. Do NOT modify any existing function, class, or constant.

    corner_pixel_purity(image_bytes: bytes, tolerance: int = 5) -> dict:
    - Purpose: checks all four corner pixels of the image against pure white (255, 255, 255) per QA-04.
    - Import PIL.Image locally if not already imported at module level (check read_first result).
    - Algorithm:
      - Load image: img = Image.open(io.BytesIO(image_bytes)).convert("RGB")
      - corners: dict mapping name -> (x, y) pixel coordinates:
        - "top_left": (0, 0)
        - "top_right": (img.width - 1, 0)
        - "bottom_left": (0, img.height - 1)
        - "bottom_right": (img.width - 1, img.height - 1)
      - For each corner: pixel = img.getpixel((x, y)) — returns (R, G, B) tuple
      - distance from white = max(abs(255 - R), abs(255 - G), abs(255 - B))  (Chebyshev distance)
      - passed = distance <= tolerance
    - Returns dict:
      {
        "passed": bool  (True only if ALL four corners pass),
        "tolerance": int,
        "corners": {
          "top_left": {"rgb": (R, G, B), "distance_from_white": int, "passed": bool},
          "top_right": {"rgb": (R, G, B), "distance_from_white": int, "passed": bool},
          "bottom_left": {"rgb": (R, G, B), "distance_from_white": int, "passed": bool},
          "bottom_right": {"rgb": (R, G, B), "distance_from_white": int, "passed": bool},
        },
        "max_distance": int  (max distance_from_white across all four corners)
      }
    - Function signature: def corner_pixel_purity(image_bytes: bytes, tolerance: int = 5) -> dict:

    If io is not already imported at the top of visual_comparison.py, add it to the import block.
    If PIL.Image is not imported at module level, use a local import inside the function body.
    Touch only what is needed — one function append, one possible import addition. No other changes.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -c "
from imagery.visual_comparison import corner_pixel_purity
from PIL import Image
import io

# All-white 100x100 image — all corners pass
img = Image.new('RGB', (100, 100), (255, 255, 255))
buf = io.BytesIO(); img.save(buf, format='PNG'); white_bytes = buf.getvalue()
r = corner_pixel_purity(white_bytes, tolerance=5)
assert r['passed'] == True, f'All-white should pass: {r}'
assert r['max_distance'] == 0

# Top-left corner off-white by 10 units — should fail
img2 = Image.new('RGB', (100, 100), (255, 255, 255))
img2.putpixel((0, 0), (245, 255, 255))  # R off by 10
buf2 = io.BytesIO(); img2.save(buf2, format='PNG'); dirty_bytes = buf2.getvalue()
r2 = corner_pixel_purity(dirty_bytes, tolerance=5)
assert r2['passed'] == False, f'Dirty corner should fail: {r2}'
assert r2['corners']['top_left']['passed'] == False

print('corner_pixel_purity OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - corner_pixel_purity() importable from imagery.visual_comparison
    - Returns passed=True for all-white image
    - Returns passed=False when any corner exceeds tolerance
    - Returns max_distance and per-corner rgb + distance_from_white in result dict
    - All existing visual_comparison functions still work (ComparisonResult, score_images)
  </acceptance_criteria>
  <done>Assertions pass with "corner_pixel_purity OK" output</done>
</task>

<task type="auto">
  <name>Task 2: Add VetoResult and score_ghost_mannequin() to QualityAgent</name>
  <files>skyyrose/elite_studio/agents/quality_agent.py</files>
  <read_first>
    Read skyyrose/elite_studio/agents/quality_agent.py in full (CONTEXT.md notes it has _PASS_THRESHOLD = 80 and dual-judge via asyncio.gather).
    Confirm: exact class signature (QualityAgent inherits BaseSuperAgent or CreativeAgent?), existing imports, existing method names, where to safely append new code.
    Confirm whether asyncio is already imported.
  </read_first>
  <action>
    Extend skyyrose/elite_studio/agents/quality_agent.py. Append-only modifications:

    STEP 1 — Add imports at the top of the file (only if not already present):
    - from dataclasses import dataclass, field
    - from imagery.visual_comparison import corner_pixel_purity
    - from .models import AuditEntry  (if models not already imported)

    STEP 2 — Define VetoResult frozen dataclass BEFORE the QualityAgent class definition (append after existing imports, before the class):

    VetoResult frozen dataclass fields (14 total — per WARNING 7 / QA-01):
      passed: bool  (True only if ALL per-metric vetoes pass)
      silhouette_iou: float
      silhouette_passed: bool  (iou >= 0.96)
      color_delta_e: float
      color_passed: bool  (delta_e <= 2.0)
      clip_score: float
      clip_passed: bool  (score >= 0.85)
      logo_drift_pct: float
      logo_passed: bool  (drift <= 5.0)
      corner_purity: dict  (raw output from corner_pixel_purity())
      corner_passed: bool  (corner_purity["passed"])
      pattern_passed: bool  (qualitative, from dual-judge verdict)
      vetoes_failed: list[str]  (names of metrics that forced veto; empty if passed)
      pattern_pass_was_exception_fallback: bool = False
        (True when pattern_passed was set to True because self.verify() raised an exception,
         NOT because the dual-judge genuinely approved. The 15-06 agent must write this flag
         to AuditEntry.score_breakdown["pattern_pass_was_exception_fallback"] so operators
         can distinguish genuine QA passes from exception-driven fallbacks in audit analysis.)

    STEP 3 — Add score_ghost_mannequin() as a new method on QualityAgent. Place AFTER the existing verify() method:

    async def score_ghost_mannequin(
        self,
        candidate_bytes: bytes,
        flatlay_bytes: bytes,
        techflat_bytes: bytes,
        sku: str,
        branding_spec: str = "",
    ) -> VetoResult:
      Method body:
      1. Import score_images lazily: from imagery.visual_comparison import score_images, ComparisonResult
      2. comparison = score_images(candidate_bytes, flatlay_bytes)  # ComparisonResult
      3. Map ComparisonResult fields:
         - silhouette_iou: use comparison.ssim_score as the silhouette proxy (SSIM ≈ structural similarity ≈ silhouette proxy until Phase 16 ships true IoU scorer). Log a WARNING noting this proxy: "silhouette_iou using ssim_score proxy; true IoU scorer not yet available"
         - color_delta_e: use 1.0 - comparison.color_similarity mapped to 0-10 range: color_delta_e = (1.0 - comparison.color_similarity) * 10.0. (color_similarity is 0-1 with 1=identical; delta_e 0=identical, >2.0=veto)
         - clip_score: comparison.clip_similarity if the field exists, else 0.85 (treat as passing if CLIP scorer not present)
         - logo_drift_pct: compute from techflat vs candidate using score_images(candidate_bytes, techflat_bytes).ssim_score as logo proxy: logo_drift_pct = (1.0 - logo_comparison.ssim_score) * 100.0. Run score_images(candidate_bytes, techflat_bytes) separately.
      4. corner_purity = corner_pixel_purity(candidate_bytes, tolerance=5)
      5. pattern_passed and pattern_pass_was_exception_fallback:
         - Initialise: pattern_pass_was_exception_fallback = False
         - Call existing self.verify() dual-judge with a temporary path write of candidate_bytes. Since verify() takes image_path: str, write candidate_bytes to a temp file, call await self.verify(tmp_path, product_data=None), extract recommendation.
         - If recommendation == "approve": pattern_passed = True
         - Else: pattern_passed = False
         - If verify() raises any exception:
             pattern_passed = True  (fail-open — do not block on judge API failure)
             pattern_pass_was_exception_fallback = True
             Log WARNING: "score_ghost_mannequin: dual-judge raised {type(exc).__name__}; pattern_passed defaulting to True (exception fallback)"
      6. Compute per-metric pass/fail per D-03 floors:
         - silhouette_passed = silhouette_iou >= 0.96
         - color_passed = color_delta_e <= 2.0
         - clip_passed = clip_score >= 0.85
         - logo_passed = logo_drift_pct <= 5.0
         - corner_passed = corner_purity["passed"]
      7. vetoes_failed = [name for name, ok in [("silhouette_iou", silhouette_passed), ("color_delta_e", color_passed), ("clip_score", clip_passed), ("logo_drift_pct", logo_passed), ("corner_purity", corner_passed), ("pattern", pattern_passed)] if not ok]
      8. passed = len(vetoes_failed) == 0
      9. Return VetoResult(passed=passed, silhouette_iou=..., ..., vetoes_failed=vetoes_failed, pattern_pass_was_exception_fallback=pattern_pass_was_exception_fallback)

    CRITICAL: Do NOT modify the existing verify() method signature or body. score_ghost_mannequin() is a new method only. VetoResult is a new dataclass only. All existing behaviour is unchanged.
    CRITICAL: When calling self.verify() in step 5, handle the case where verify() expects a ProductData argument. Pass None if ProductData is Optional, or create a minimal stub. Executor must read the exact verify() signature first (read_first covers this).
    CRITICAL: Temporary file write for pattern check — use tempfile.NamedTemporaryFile with delete=True and .webp suffix. Clean up in finally block.
  </action>
  <verify>
    <automated>cd /Users/theceo/DevSkyy && python -c "
from skyyrose.elite_studio.agents.quality_agent import QualityAgent, VetoResult
import dataclasses, inspect

# VetoResult is a frozen dataclass
assert dataclasses.is_dataclass(VetoResult)
fields = {f.name for f in dataclasses.fields(VetoResult)}
required = {'passed', 'silhouette_iou', 'silhouette_passed', 'color_delta_e', 'color_passed', 'clip_score', 'clip_passed', 'logo_drift_pct', 'logo_passed', 'corner_purity', 'corner_passed', 'pattern_passed', 'vetoes_failed', 'pattern_pass_was_exception_fallback'}
missing = required - fields
assert not missing, f'Missing VetoResult fields: {missing}'
assert len(fields) >= 14, f'Expected 14 fields, got {len(fields)}'

# score_ghost_mannequin is an async method on QualityAgent
assert hasattr(QualityAgent, 'score_ghost_mannequin')
assert inspect.iscoroutinefunction(QualityAgent.score_ghost_mannequin)

# Existing verify() method still present
assert hasattr(QualityAgent, 'verify')

# pattern_pass_was_exception_fallback has default value False
defaults = {f.name: f.default for f in dataclasses.fields(VetoResult) if f.default is not dataclasses.MISSING}
assert 'pattern_pass_was_exception_fallback' in defaults, 'Field must have default=False'
assert defaults['pattern_pass_was_exception_fallback'] == False

print('quality_agent extensions OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - VetoResult frozen dataclass importable with all 14 required fields
    - pattern_pass_was_exception_fallback field present with default value False
    - pattern_pass_was_exception_fallback set to True when self.verify() raises an exception
    - score_ghost_mannequin() is an async method on QualityAgent
    - Existing verify() method unchanged (same signature, same body)
    - VetoResult.vetoes_failed is a list of metric name strings
    - corner_pixel_purity imported and used within score_ghost_mannequin
    - pytest tests/ -x -q passes
  </acceptance_criteria>
  <done>Assertions pass with "quality_agent extensions OK" output; existing tests still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate_bytes (from Gemini/GPT/Ideogram) → pixel comparison | External API image bytes decoded and compared against repo-resident flatlay |
| VetoResult → caller | Structured veto verdict consumed by ghost_mannequin_node to decide retry vs accept |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05-01 | Tampering | silhouette_iou SSIM proxy | LOW | accept | SSIM proxy is explicitly logged as WARNING; true IoU scorer is Phase 16 follow-up. Proxy may produce false-passes on garments with same structural score but different silhouette — acceptable risk for Phase 15, documented in VetoResult docstring |
| T-15-05-02 | Tampering | pattern_passed fail-open | MEDIUM | mitigate | fail-open (pattern_passed=True on verify() exception) sets pattern_pass_was_exception_fallback=True and logs WARNING; 15-06 writes this flag to AuditEntry.score_breakdown so operators can distinguish genuine QA passes from exception-driven fallbacks |
| T-15-05-03 | Denial of Service | score_images called twice per candidate | LOW | accept | Two score_images calls (flatlay + techflat) per candidate; with ensemble-of-3 that is 6 calls per SKU. score_images is CPU-bound (SSIM, histogram, CLIP); no external API call; acceptable compute cost |
| T-15-05-04 | Information Disclosure | temp file write of candidate_bytes | MEDIUM | mitigate | Use tempfile.NamedTemporaryFile(delete=True) in finally block — file deleted on normal exit and on exception. Temp path must be in /tmp not in repo directory |
| T-15-05-05 | Repudiation | VetoResult not logged to audit | MEDIUM | mitigate | 15-06 must write VetoResult.vetoes_failed and pattern_pass_was_exception_fallback to AuditEntry.score_breakdown; this plan ensures the data is available in VetoResult; 15-06 is responsible for the JSONL write |
</threat_model>

<verification>
Run after both tasks complete:

  cd /Users/theceo/DevSkyy && python -c "
from imagery.visual_comparison import corner_pixel_purity
from skyyrose.elite_studio.agents.quality_agent import QualityAgent, VetoResult
import dataclasses

# Corner purity
from PIL import Image
import io
img = Image.new('RGB', (100, 100), (255, 255, 255))
buf = io.BytesIO(); img.save(buf, format='PNG')
r = corner_pixel_purity(buf.getvalue())
assert r['passed'] == True

# VetoResult structure — 14 fields
assert dataclasses.is_dataclass(VetoResult)
fields = {f.name for f in dataclasses.fields(VetoResult)}
assert 'pattern_pass_was_exception_fallback' in fields
assert len(fields) >= 14
assert hasattr(QualityAgent, 'score_ghost_mannequin')
print('Phase 15-05 verified OK')
"
</verification>

<success_criteria>
- corner_pixel_purity() functional in visual_comparison.py
- VetoResult dataclass importable with all 14 fields including pattern_pass_was_exception_fallback
- pattern_pass_was_exception_fallback defaults to False; set True when verify() raises
- score_ghost_mannequin() async method on QualityAgent
- All 6 per-metric veto conditions implemented per D-03
- Existing verify() and QualityAgent behaviour unchanged
- pytest tests/ -x -q passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-ghost-mannequin-agent-qa/15-05-SUMMARY.md`
</output>
