# Learn Codebase: WordPress Theme (skyyrose-flagship) — Progress

Scope: `wordpress-theme/skyyrose-flagship/` read IN FULL (main thread → claude-mem PostToolUse `observation` hook captures, keyed to real paths; also gets `file-context` digest of prior obs).
Size: ~319 source files / ~113k lines (php 32k, css 21k, js 18k, html 27k, json 14k). Multi-session certain.

SKIP (not source codebase): `vendor/`, `node_modules/`, `*.min.css`/`*.min.js`/`*.map` (4033 files), `data/**/*.jpeg` (96 images), `tests/*.html` (47 Playwright output), `test-results/`, `docs/*.html` (11 ThemeForest prose docs), `languages/*.pot`. All per-dir `CLAUDE.md` = empty claude-mem placeholders (skip; confirmed pattern from Elite Studio run).

Resume rule: start at first unchecked cluster. Don't re-read checked ones.
Capture note (verified from worker-service.cjs): use the **Read tool on real source paths** (NOT cat→.txt) so observations key to the file path AND the file-context digest surfaces prior WP-theme obs (heavy prior coverage exists).

## Clusters

- [x] W0  Arch/config — DONE. functions.php, theme.json, style.css, composer.json, asset-manifest.json, readme.txt, webpack.config.js, scripts/build-{js,css}.js, DELETION_LOG, VERIFICATION_LOG, phpstan.neon, phpunit.xml.dist.
- [x] W1  `inc/*.php` core modules — DONE (all 42 present inc/*.php read in full; `brand.generated.php` is build-generated, absent from working tree — verify on next deploy). Count note: tracker said "36 remaining" but actual unread was 38 (5 prior-done: brand-colors, collections-config, product-catalog, product-catalog-display, theme-setup; 38 read this session). admin-experience-dashboard.php is NOT an orphan — `experience-engine.php` lazy-`require`s it in `skyyrose_see_render_dashboard()`.
- [x] W2  `inc/builders/*.php` (7) + `elementor/widgets/*.php` (8) — DONE 2026-06-06. ("elementor/*.php" in plan = `elementor/widgets/`; no top-level elementor/*.php exists.)
- [x] W3  Root chrome/templates — DONE 2026-06-06 (13 files: front-page, header, footer, index, page, single, archive, 404, search, searchform, comments, sidebar, page-wishlist).
- [x] W4  Page templates — DONE 2026-06-06 (21 template-*.php: 4 collection stubs, 4 immersive, 4 landing, 3 elementor, preorder-gateway, about, contact, faq, shipping-returns, coming-soon).
- [x] W5  `template-parts/*.php` — DONE 2026-06-06 (40 partials read in full).
- [x] W6  `woocommerce/*.php` (5 overrides) + `patterns/*.php` (4) — DONE 2026-06-06 (9 files read in full).
- [x] W7  `assets/css/*.css` — DONE 2026-06-06. 47 source `.css` read in full (W7a foundation 14 + W7b chrome 9 + W7c product/woo 7 + W7d pages 17 via 3 parallel readers). The 47 `.min.css` = minified single-line PRODUCTION build outputs (skip-for-reading, never-delete). **Deletion executed: homepage-v7.css + .min + enqueue block (proven dead).**
- [x] W8  `assets/js/*.js` — DONE 2026-06-06 (44 source read via 2 parallel readers). **Deletion executed: dead Three.js 3D experiences cluster (27 files + functions.php/enqueue edits).**
- [x] W9  `data/` canon — DONE 2026-06-06 (catalog.csv + 2 json + 35 dossiers + 11 brand-logo md, via reader). Catalog = **33 products** (not 34).
- [x] W10 `tests/` source — DONE 2026-06-06 (bootstrap.php + wp-stubs.php + 4 unit tests, via reader). coverage/html = gitignored generated artifact (not source).

## Notes / synthesis per cluster

### W0 Arch/config
- **Version: 1.5.28** (functions.php `SKYYROSE_VERSION` + readme stable tag). **Drift:** `asset-manifest.json` stale at theme_version 1.1.1 (generated 2026-05-11); VERIFICATION_LOG/DELETION_LOG at 1.0.0 (2026-04-12). readme changelog top entry mislabeled "6.5.2".
- `functions.php` = bootstrap. Constants: SKYYROSE_VERSION/DIR/URI + legacy aliases (THEME_DIR/THEME_URI/ASSETS_URI). `SKYYROSE_COMING_SOON_MODE` (default false) → inc/maintenance.php routes all public to template-coming-soon.php + HTTP 503 (editors/admin/ajax/rest/assets bypass). `CONCATENATE_SCRIPTS=false` (kills WP.com _jb_static MIME/404 issues).
- **Load order (authoritative module map, supersedes stale CLAUDE.md "21 modules"):** core includes (~33) load `inc/brand.generated.php` FIRST (SoT constants from assets/brand/brand.yaml: SKYYROSE_BRAND_TAGLINE, COLOR_*, skyyrose_brand_collections(), skyyrose_json_ld_organization()), then redirects, theme-setup, brand-colors, collections-config, enqueue, enqueue-performance, enqueue-experiences, enqueue-phases, customizer, template-functions, security, accessibility-fix, accessibility-seo, seo, ajax-handlers, collection-content, product-catalog, product-catalog-display, immersive-product-adapter, product-taxonomy, facebook-sdk, maintenance, menu-setup, theme-activation-setup, klaviyo-integration, experience-engine, fastapi-client, rest-api-experience, personalization, performance-guardian, performance, patterns.
- **Woo includes (only if class_exists WooCommerce):** woocommerce, woocommerce-preorder, skyyrose-product-meta, wc-product-functions, immersive-ajax, wishlist-functions, class-wishlist-widget, experience-analyzer, woocommerce-kids-capsule, rest-kids-capsule.
- **Builders:** detection.php + shared.php always; elementor.php + elementor-compat.php on `elementor/loaded` hook; divi/beaver-builder/bricks self-guarded stubs. Admin includes + mascot/brand-ambassador REMOVED in v5.2.0 (dead-code cleanup — note: "v5.2.0" comment despite theme at 1.5.28; legacy versioning scheme reference).
- `composer.json`: type wordpress-theme, **psr-4 `SkyyRose\` → inc/**. Requires AI providers (wordpress/php-ai-client, wp-ai-client, ai-provider-for-{openai,anthropic,google}). dev: phpunit 9.5, wpcs 3, phpstan 2 + phpstan-wordpress, wp-cli, php/woo stubs. scripts: test/phpcs/phpstan.
- `theme.json` v2: appearanceTools, contentSize 1200 / wideSize 1400. 15-color palette (page-bg #0A0A0A, card-bg #111, surface #1A1A1A, rose-gold #B76E79, gold #D4AF37, metallic-silver #C0C0C0, crimson #DC143C, red #FF0000, pink #FFB6C1, lavender #E6E6FA, etc). 6 gradients incl per-collection (signature/love-hurts/black-rose/kids-capsule). 4 duotones. 8 fontFamilies declared (Inter/Playfair Display/Cinzel/Cormorant Garamond/Oswald/Barlow/Bebas Neue/Instrument Serif — all self-hosted woff2 via fontFace, zero Google CDN; readme/CLAUDE say "9" — drift). fluid clamp() font sizes xs→gigantic. custom tokens: transitions(smooth/spring/fast/slow/luxury), shadows(+roseGlow/goldGlow), borderRadius, glassmorphism. styles: bg #0A0A0A/text white, h1-h6 Playfair, link rose-gold→gold hover, button rose-gold→gold. templateParts header/footer. customTemplates: template-{love-hurts,signature,black-rose,preorder,kids-capsule}.
- `style.css` (root, 1239L): design-token-driven (`var(--color-*, fallback)`). normalize + base typography + buttons (.btn/.btn-primary/secondary/crimson) + forms + glassmorphism nav (.site-header fixed, .scrolled) + logo gradient text-clip + hero + cards + footer + woo button theming + three-container + a11y (skip-link, screen-reader-text) + responsive 1200/768/480 + print. **homepage-v2 hides official .site-header/.site-footer** (front-page.php has own .nav/.ft). **⚠️ LIKELY CSS BUG style.css:515-522**: `.homepage-v2 .site-content, …` selector list appears to MISSING its opening `{` before `padding-top: 0;` — malformed rule (note, not fixed).
- **Build pipeline (TWO JS paths — conflict):** `webpack.config.js` (glob assets/js/*.js, terser, IIFE, **source-map ON**, drop_debugger, keeps console) VS `scripts/build-js.js` (scans assets/js + experiences/ + system/, terser passes:2 + mangle + **drop_console + NO source map "so prod doesn't leak unminified JS"**). Conflicting source-map policy between the two. `scripts/build-css.js` (CleanCSS, source maps ON, preserves root style.css WP header comment, recursive assets/css/**).
- `phpstan.neon` level 5, paths = inc/ + template-parts/ + woocommerce/ + patterns/ + all root php; excludes vendor/node_modules/tests/blueprints/docs; woo stubs; ignores Elementor unknown-class. `phpunit.xml.dist`: suite tests/unit/*Test.php, bootstrap tests/bootstrap.php, coverage ONLY 3 pure modules (brand-colors, collections-config, product-catalog).
- DELETION_LOG (2026-04-12): inc/elementor.php→builders/, .pot rename to skyyrose, build scripts/cart.js removed. VERIFICATION_LOG: phpcs/php-syntax PASS at v1.0.0.

### W1 foundation (data/config layer — 5 of 41 inc/ done)
- `brand-colors.php` — PHP mirror of `assets/css/design-tokens.css`. Constants guarded (`defined()||define()`) because `brand.generated.php` defines same earlier. Core: ROSE_GOLD #B76E79, GOLD #D4AF37, CRIMSON #DC143C, SILVER #C0C0C0, DARK #0A0A0A. Extended: DEEP_BLACK #0D0D0D (BR primary), DEEP_RED #8B0000 (LH primary), PURPLE #4A1942 (LH bruise-plum), NAVY #1A1818/DEEP_BLUE #221A1A (legacy warm-near-black), SOFT_PINK #FFB6C1 (KC primary), LAVENDER #FFF0F5 (KC secondary — NOTE: theme.json lavender=#E6E6FA differs). `skyyrose_brand_colors()` 12-key array (canonical helper, do NOT invent skyyrose_brand_palette). `skyyrose_hex_to_rgba($hex,$alpha)`.
- `collections-config.php` — **collection registry SoT** (static-cached). `skyyrose_get_collections_config()` keyed by slug: black-rose(abbr br, accent SILVER, Collection 01), love-hurts(lh, CRIMSON, 02), signature(sg, GOLD, 03), kids-capsule(kc, SOFT_PINK, 04, show_on_front=FALSE). Each: slug/class_abbr/label/short_desc/accent/glow/tagline/description/page_url(home_url /collection-<slug>/)/palette(primary/secondary/accent)/front_page(title HTML, poetic_tagline, label, num, image lookbook webp, show_on_front). Helpers: get_cross_nav(exclude), get_front_page_collections(filter show_on_front), get_collection(slug, sanitize_key), get_collection_field.
- `product-catalog.php` (since 7.0.0) — **CSV SoT loader**. `skyyrose_catalog_csv_path()` = data/skyyrose-catalog.csv (ONLY authoritative; yaml/manifest/products.json retired). `skyyrose_get_product_catalog()` keyed by SKU: static + wp_cache (Redis, HOUR TTL); fgetcsv parse, pads short rows, unknown columns NOT passed through (fixed field set: sku/name/price/collection/description/badge/image/front_model_image/back_image/back_model_image/sizes/color/edition_size/published(bool)/is_preorder(bool)/garment_type_lock/dossier_slug). Helpers: get_product(sku, sanitize_key), get_collection_products(collection, cached), normalize_sku (strip -tee/-shorts + trailing letter variant), format_price (Coming Soon if !published&&!preorder; Pre-Order if preorder&&price<=0; else $N), product_image_uri (placeholder fallback), get_product_display_image (slot precedence image→front_model→back→back_model), **get_product_dossier(sku)** (reads data/dossiers/<dossier_slug>.md, regex-parses front-matter + Garment type lock + ## Branding ###Front/Back/Other → structured, sanitized, cached), **get_similarity_skus(sku,limit)** (data/product-similarities.json `products.<sku>.global`, CLIP-embedding ranking, retained though complete-the-look cross-sell retired), product_url (wc permalink → preorder #anchor → collection #anchor fallback).
- `product-catalog-display.php` (split from catalog v6.5.1, <800-line cap) — projection layer. `skyyrose_catalog_to_static_card($cat)` = SoT card shape for product-card-holo (?? guarded). `skyyrose_resolve_display_products()` = WC-first/catalog-fallback (matches WC by sku then normalized_sku → WC_Product else static card; skips unpublished non-preorder; WP_DEBUG trigger_error on drift). `get_collection_display_products`, `get_preorder_products` (grouped by collection), `get_featured_catalog_products` (default 8 SKUs br-001/sg-015/br-004/lh-004/sg-006/br-010/sg-013/kids-001, `skyyrose_featured_product_skus` filter), `get_featured_display_products` (TWO-TIER cache: per-request static + 5min transient of lightweight descriptors {type:wc id|catalog sku} rehydrated to live WC_Product/card — never caches WC_Product directly; ?nocache bypass; re-checks visibility on rehydrate). Flush hooks: save_post_product/woocommerce_update_product/delete/trash/trashed_post/untrashed_post → flush common limits {0,4,6,8,10,12,16,20,24}.
- `theme-setup.php` — `skyyrose_setup` (after_setup_theme): textdomain, title-tag, post-thumbnails, image sizes (product-card 400×533 3:4, product-hero 1024×1536 2:3, skyyrose-featured 1200×675, thumbnail 400×300, medium 800×600), **nav menus: primary/collection/experiences/footer-legal/footer** (mobile/top-bar comment-listed but NOT registered), html5, custom-logo (100×400 flex), selective-refresh, align-wide, editor-styles, responsive-embeds, custom line-height/spacing/units, woocommerce + wc-product-gallery zoom/lightbox/slider, elementor. HPOS compat declared (before_woocommerce_init). content_width 1200. 5 widget areas (sidebar-1 + footer-1..4) + shop-sidebar (if WC). **Wishlist CPT** registered (private, show_ui, show_in_rest, supports title+author). body_classes (customizer-preview/woocommerce-page/elementor-page/singular). pingback header. `skyyrose_disable_wpcom_optimization` (init pri 1): jetpack_photon_skip + implode_frontend_css false + js/css_do_concat false.

### W1 inc/ modules (38 read this session, 2026-06-06) — synthesis

**Enqueue pipeline (4 files, the authoritative asset map):**
- `enqueue.php` — **`skyyrose_get_current_template_slug()` is the canonical template-detection helper** (static-cached; maps WC conditionals + page templates → slug: front-page, single-product, cart, checkout, shop-archive, collection-standalone, immersive, about, contact, preorder-gateway, faq, shipping-returns, landing, elementor-{editorial,canvas,fullwidth}, kc-launch (KC launch mode), single, search, blog, page, default, 404). Hook priorities: **5 fonts → 10 global styles+scripts → 15 localize → 20 template styles+scripts** (25 commercial-polish, 30/40/42 phases, 65 experiences). `skyyRoseData` localized on `skyyrose-navigation` (fallback jquery): ajaxUrl/nonce(`skyyrose-nonce`)/themeUri/assetsUri. Global JS: navigation, toast (window.skyyToast), footer-cro, motion-one (self-hosted lib/motion.min.js v11) + premium-interactions, page-transitions. Lightweight slugs (cart/checkout/blog/single/404/search/default) skip premium CSS+JS (~65KB). Per-template CSS map (`$template_styles`) + per-template JS map (`$template_scripts`). GSAP self-hosted (lib/gsap.min.js 3.12.2) only on preorder-gateway/immersive/kc-launch; ScrollTrigger only preorder-gateway/kc-launch; Lenis only preorder-gateway. immersive-core.{css,js} on immersive+preorder. Holo cards (product-card-holo.{css,js}) on collection/front/shop/preorder/search/landing/elementor-editorial/single-product. Front-page preloads hero AVIF via `skyyrose_avif_sibling_pair()`. Mascot CSS+JS commented out (art not finalized). KC launch mode → kids-capsule.css + kids-capsule-launch.js (gsap+ST).
- `enqueue-performance.php` — woo style dequeue (keep layout, drop general+smallscreen), font preload (latin woff2: inter/playfair/cormorant/bebas; cinzel only on BR collection/immersive), resource hints (only jsdelivr preconnect left, all Google Fonts removed 4.1.0), `skyyrose_defer_scripts` (prefix `skyyrose-`/`threejs`/wc-variation, never jquery), critical style fetchpriority, hero LCP preload (front/product/collection/immersive/about), jQuery Migrate removal, block-library dequeue on non-block pages.
- `enqueue-phases.php` — Experience Engine phase enqueues, all gated by `skyyrose_see_is_module_active()`: phase2 (perf-guardian global + brand-atmosphere collection-only), phase3 (analyzer/smart-showcase/micro-interactions on product slugs via `skyyrose_phase3_product_slugs()`), phase4 (personalization). Also commercial-polish (pri 25) + `skyyrose_enqueue_collection_styles()` (collection-pages.css + view-transitions.css, called from enqueue.php).
- `enqueue-experiences.php` — Three.js r147 **self-hosted** (lib/three.min.js + three-examples/ UMD addons: OrbitControls/GLTFLoader/DRACOLoader/RGBELoader/EffectComposer/RenderPass/UnrealBloomPass/ShaderPass/CopyShader; r147 pinned = last with examples/js UMD). Per-template scene map. `window.skyyrose3D` config + nonce `skyyrose_3d_nonce`. Pri 65.

**Experience Engine (SEE, 6 files, prefix `skyyrose_see_`):**
- `experience-engine.php` — SoT. 6 modules (perf_guardian/experience_analyzer/brand_atmosphere/smart_showcase/micro_interactions/personalization), **all default OFF**, priority-sorted, toggled via `skyyrose_see_settings` option (no deploy). Settings helpers get/update/update_options. Narrative directive pipeline (conflict resolution by priority+target). Creates `{prefix}skyyrose_analytics` table via dbDelta (DB version 1.0.0). Admin dashboard page (add_dashboard_page).
- `experience-analyzer.php` — behavioral event storage (upsert UPDATE-then-INSERT into analytics table; no PII, keyed by visitor_hash), `skyyrose_see_get_summary()` rollups, daily cleanup cron (90-day retention). phpcs PreparedSQL disabled file-wide (table-name interpolation only, values all %s/%d/%f).
- `rest-api-experience.php` — `skyyrose/v1` REST: POST /analytics/events (public, 10/min/IP rate-limit), GET /analytics/summary (admin), GET /personalization/{hash} (public, ML→local fallback, URL validation), POST /settings/narrative + GET/PUT /settings (admin via `manage_options`). Whitelisted settings keys.
- `fastapi-client.php` — bridge to devskyy.app FastAPI. `skyyrose_see_is_safe_url()` SSRF guard (blocks 169.254.169.254, private/reserved IPs; loopback only in local env). Health-check transient cache. POST/GET helpers; relay analytics (non-blocking), ML recommendations, narrative analysis. Default base http://localhost:8000.
- `personalization.php` — visitor hash cookie (`skyy_visitor`, 16-hex, 90d, httponly=false so JS reads it), localizes `SkyyCurated` for personalization.js (pri 45, after phase4 enqueue at 42).
- `performance-guardian.php` — CLS-prevention inline CSS (contain on grids/cards/hero/atmosphere) + reduced-motion overrides, FPS watchdog budget `SkyyPerformanceBudget` (fpsThreshold 30, maxConcurrentAnims 8).
- `admin-experience-dashboard.php` — Phase 5 admin UI (module toggles, analytics bars, narrative form), self-contained `<style>`+`<script>` using REST. Lazy-required by experience-engine.

**WooCommerce (8 files):**
- `woocommerce.php` — `SKYYROSE_FREE_SHIPPING_THRESHOLD=200`. Loop 4-col/12-per-page, sidebar removed, content wrappers, gallery/single/thumbnail image sizes, AJAX cart fragments (.cart-count/.cart-subtotal), related products DISABLED (H4 launch, hooked on `wp`), cross-sells limited 4. **"Complete the Look" FULLY removed 2026-05-27** (was commented, now gone). `skyyRoseWoo` localize on `skyyrose-template-woocommerce`. 3D model button + meta box (`_product_3d_model`). `skyyrose_wc_ghost_loop_image` filter — substitutes catalog ghost render when WC product has no featured image (front_model→image precedence). `skyyrose_get_cart_wears_with()` cart cross-sell helper.
- `woocommerce-preorder.php` — **pre-order canonical meta `_is_preorder`** (+ _preorder_edition_size/_available/_ship_date/_preorder_price). Meta box, single-product notice (pri 6), "Pre-Order Now" button text, $0→"Pre-Order" price_html. Referral credit ($10 on completed order, SKYY-prefix coupons, Founding Ambassador badge first 100, idempotent).
- `skyyrose-product-meta.php` (since 1.0.1) — **catalog post-meta schema SoT** mapping non-WC CSV columns → `_skyyrose_*` meta. `skyyrose_product_meta_schema()` (collection/badge/edition_size/imagery/render-pipeline fields incl engine_override enum gemini-pro|fashn|tripo|meshy|trellis|nano-banana). register_post_meta (REST, auth manage_woocommerce). WC "SkyyRose" product-data tab. `skyyrose_get_product_meta_field()` typed getter. **WP-CLI `wp skyyrose import-catalog-meta`** (idempotent CSV upsert, matches by SKU). Explicitly does NOT duplicate `_is_preorder`.
- `wc-product-functions.php` — `skyyrose_get_product_collection()` (WC category → collection key incl parent check), `skyyrose_collection_config()` (per-collection accent/bg/label/nav_font/gradient/body_class — BR silver, LH crimson, SG gold, KC pink), legacy `_skyyrose_*` meta (material/fit/detail/care/made_in/limited/edition_of) WC general-tab + save, related-by-category query, `skyyrose_localize_product_data` (`skyyrose` obj on single-product), body class. **`skyyrose_current_wc_product()` = canonical safe WC_Product fetch** (used by seo/404/search/enqueue-performance).
- `immersive-ajax.php` — immersive AJAX (nonce `skyyrose-immersive-nonce`): get_product_by_sku, get_collection_products, immersive_add_to_cart (30/min/IP rate-limit, variant SKU strip). **`skyyrose_build_product_data()`** shared by rest-kids-capsule. `skyyrose_immersive_is_preorder` falls back to `_is_preorder`.
- `wishlist-functions.php` — guests use transients (30d, cap 50), logged-in use options. CRUD + move-to-cart + clear + move-all. AJAX (nonce `skyyrose-wishlist-nonce`) + REST `skyyrose/v1/wishlist*` (auth-required). nopriv move-all removed. `skyyRoseWishlist` localize. delete_user cleanup. Wishlist button hooks removed v6.2.0 (holo card renders own heart).
- `class-wishlist-widget.php` — `SkyyRose_Wishlist_Widget extends WP_Widget`.
- `woocommerce-kids-capsule.php` — KC meta box (`_kc_age_range`/`_kc_matching_adult_id`/`_kc_drop_number`, KC-category-only), matching-set frontend render (loop/self-ref guards). `rest-kids-capsule.php` — `skyyrose/v1/kids-capsule/matching-set/{id}`.

**SEO/meta (`seo.php`, `accessibility-seo.php`):**
- `seo.php` — ALL schema/OG/Twitter/canonical/meta/title functions **defer to Yoast when active** (WPSEO_VERSION / WPSEO_WOO_VERSION guards). Product schema (pri 1), Organization (front, name "SkyyRose", founder Corey Foster, sameAs instagram/tiktok skyyroseco), WebSite SearchAction, BreadcrumbList, OG tags (collection-template-aware, fallback img assets/branding/sr-primary-hero.webp), Twitter cards (@skyyroseco default), canonical (theme owns it — security.php removes core rel_canonical), meta description (per-template 155-char map), `pre_get_document_title` (pri 99, per-template titles, "collections"/9327 special), core-sitemaps + product image sitemap, robots (noindex search/404/attachment), CollectionPage schema (WC tax), **ItemList schema for the 4 custom collection PAGE templates** (CSV-backed, published-only), favicon (SR rose-icon webp fallback). All JSON-LD via wp_json_encode JSON_HEX_TAG.
- `accessibility-seo.php` (despite name, A11y only — SEO split to seo.php iter 28) — a11y CSS/JS enqueue, nav ARIA labels (incl 'mobile' theme_location — **note: 'mobile' not registered in theme-setup**), `role=menuitem`, ARIA live regions, image-alt enforcement, WC a11y (add-to-cart/cart-remove aria-labels), A11y&SEO admin testing page (WAVE/Rich-Results/PageSpeed buttons).

**Security (`security.php`):** CSP (script/style unsafe-inline required for WP/WC/Elementor; whitelists wp.com, jsdelivr, facebook, etc; blob: for Three.js), HSTS (HTTPS-only), X-Frame SAMEORIGIN, nosniff, Permissions-Policy, COOP/CORP. XML-RPC disabled unconditionally (v3.2.2 fix — prior Jetpack check re-enabled it). WP version strip, head cleanup (removes core rel_canonical → seo.php owns it), nonce helpers (`skyyrose_` prefix), REST user-enum block, author-enum block, DISALLOW_FILE_EDIT, app-passwords admin-only, sanitize helpers.

**AJAX/forms (`ajax-handlers.php`):** contact submit (honeypot `website`, referral whitelist via `skyyrose_get_referral_options()` SoT, header-injection guard), newsletter subscribe (5/email/15min, fires `skyyrose_newsletter_signup`), incentive signup (fires `skyyrose_incentive_signup`), signin (per-email 5/15min — per-IP removed, WP.com proxy IP shared), referral tracking (`sr_ref` 30d cookie), mascot chat (scripted stub, future AI). **`accessibility-fix.php`** — output-buffer HTML post-processor (V3, no theme-hook deps): screen-reader CSS, button type, dedupe IDs, empty-heading/link aria, lazy-load below-fold, LH $0→Pre-Order JS.

**Integrations & infra:**
- `klaviyo-integration.php` — Profiles API v3 (2024-02-15), keys+list-IDs in wp_options (Customizer panel pri 200), subscribe-to-list, hooks on newsletter/incentive signup, generic AJAX subscribe (nonce `skyyrose-nonce`, list_slug whitelist), **`skyyrose/v1/stock/{sku}` public REST** (edition/claimed/remaining/percent, 2min cache), Placed Order event on `woocommerce_checkout_order_created`, onsite tracking script (public key).
- `performance.php` — AVIF uploads + MIME fix + sizes, Google Fonts removal (Elementor filter + dequeue + hint strip), `skyyrose_picture_sources()`/`skyyrose_url_to_path()`/`skyyrose_render_picture()`/`skyyrose_avif_sibling_pair()` next-gen <picture> negotiation (strips Jetpack Photon prefix — Photon transcodes avif→jpeg), wraps wp_get_attachment_image in <picture>, GD AVIF/WebP sibling generation on upload (q62/q82, 12MP downsize guard), **WP-CLI `wp skyyrose nextgen-backfill`** (idempotent, `_skyyrose_nextgen_done` meta, size whitelist, gc per encode).
- `customizer.php` — Brand Identity (rose-gold/gold/dark colors + logo), Social (7 networks + twitter_handle + contact email/phone), Kids Capsule Launch (`skyyrose_kc_mode` launch|live + launch date; `skyyrose_kc_is_launch_mode()`). Customizer CSS only emits when differs from defaults.
- `template-functions.php` — SVG kses whitelists, collection color/palette (pull from collections-config palette), `skyyrose_get_products_by_collection()` WP_Query, **breadcrumbs** (schema microdata), film grain, body classes (homepage-v2 on front, has-mobile-nav always), excerpt, reading time, post meta, `skyyrose_svg_icon()` (phone/email/location/clock/search/heart/cart/star), attr builders, **`skyyrose_get_social_links()` SoT** (instagram theskyyrosecollection, tiktok skyyrosecollection — NOTE: differs from seo.php Organization sameAs which uses skyyroseco), **`skyyrose_nav_fallback()`** (header.php fallback_cb).
- `collection-content.php` — per-collection page copy SoT (hero/story/quote/features/pin_beats founder narrative for all 4 collections; KC interim hero = SR monogram).
- `redirects.php` — 301s: /collections/{slug}/→/collection-{slug}/, /preorder/→/pre-order/, /shop-2/→/shop/.
- `maintenance.php` — `SKYYROSE_COMING_SOON_MODE` veil (503+Retry-After, editor/ajax/rest/login bypass, `?preview=coming-soon`, token bypass `SKYYROSE_COMING_SOON_BYPASS_TOKEN`).
- `menu-setup.php` — programmatic menus (option flag `skyyrose_menus_setup_v620`; defines 'mobile' menu but theme location not registered → orphan menu).
- `theme-activation-setup.php` — **`SKYYROSE_SETUP_VERSION = '4.2.0'`** (was 4.1.0 per old memory — bumped). One-shot page creation (init pri 30), 20 pages incl all collection/experience/landing pages, static front page, WC page assignment.
- `product-taxonomy.php` — programmatic product_cat (Shop parent + 4 collections) + product_tag creation; fallback `skyyrose_product_tag` taxonomy when WC absent.
- `facebook-sdk.php` — consent-gated Pixel (`SKYYROSE_FB_APP_ID` from option; loads only after `skyyrose:consent:accepted` / localStorage), ViewContent/InitiateCheckout/AddToCart/Lead events.
- `immersive-product-adapter.php` — `skyyrose_immersive_product($sku,$scene)` merges catalog + scene placement for hotspots (split from product-catalog v6.5.1).
- `patterns.php` — pattern category registration (skyyrose/collections/commerce) + `skyyrose_get_pattern()` loader (patterns/ auto-registered by WP core).

**Cross-file drifts/notes flagged this session:**
- Social handles inconsistent: template-functions `skyyrose_get_social_links()` uses `theskyyrosecollection`/`skyyrosecollection`/`skyyrosellc`; seo.php Organization sameAs hardcodes `skyyroseco`; customizer pulls user-set theme_mods. No single SoT for social URLs.
- 'mobile' nav theme_location referenced (accessibility-seo, menu-setup) but never `register_nav_menus`'d in theme-setup.
- Admin surfaces live in a "commercial cleanup" theme: experience-engine dashboard, accessibility-seo testing page, all the WC/preorder/KC/3D meta boxes, product-meta tab — all admin_menu/meta_box code is intact (DELETION_LOG only removed mascot/brand-ambassador + builders relocation).

### W2 builders + Elementor widgets (15 read, 2026-06-06) — synthesis

- `detection.php` — **`skyyrose_active_builder()`** (static-cached; order elementor > divi > beaver-builder > bricks > gutenberg via constant/class checks). **`skyyrose_builder_owns_template($post_id)`** (per-builder built-with check). Both consumed by root templates to defer content rendering.
- `shared.php` — **`skyyrose_register_builder_compat($slug, $config)`** scaffold. Config: `theme_support` (→after_setup_theme), `palette_hook`+`palette_callback` (callback receives `skyyrose_brand_colors()` prepended to hook args, priority configurable), `post_setup` (synchronous). Must load BEFORE divi/beaver/bricks files (they call it at include time). Elementor does NOT use this scaffold (richer inline integration).
- `elementor.php` — loaded on `elementor/loaded`. Registers 8 widgets via modern `elementor/widgets/register` + `register()` API (widget dir via **`SKYYROSE_THEME_DIR`** legacy alias). 2 categories: skyyrose, skyyrose-3d. Editor styles (elementor-editor.css) + frontend styles (product-card-holo.css + **elementor-widgets.css** which bridges `--col-accent`→`--skyyrose-accent`). Default schemes on activation (colors dark/rose_gold/white/gold; typography Playfair/Cormorant/Bebas). Breakpoints mobile 768/tablet 1024. `print_google_fonts` false. Canvas wrapper hooks.
- `elementor-compat.php` (elementor-compat, loaded on elementor/loaded too) — editorial helpers: 'skyyrose-editorial' category, expose utility classes to editor (lp-rv/rv/rv-clip/stagger-grid/magnetic/btn-sweep), **collection palette meta box** on pages (signature/black-rose/love-hurts ONLY — no kids-capsule) → `skyyrose_collection` post-meta. **4 shortcodes:** `[skyyrose_countdown hours=]`, `[skyyrose_scarcity sku=]`, `[skyyrose_press_bar]` (Maxim/CEO Weekly/SF Post/Best of Best, filterable), `[skyyrose_product_card sku=]` (delegates to template-parts/product-card-holo.php). Enqueues landing-pages.js on template-elementor-editorial.
- `divi.php` / `beaver-builder.php` / `bricks.php` — self-guarded stubs (return early if plugin constant/class absent). Each registers via `skyyrose_register_builder_compat()` with a 6-color palette callback (dark/rose_gold/#FFFFFF/gold/silver/crimson). Divi: et_default_color_palette + et_pb_allowed_tags model-viewer. Beaver: fl_builder_color_presets (hex sans #) + theme-builder header/footer locations. Bricks: bricks/setup/default_globals (id/name/raw shape; Bricks reads theme.json natively).
- **Elementor widgets (8, all `extends \Elementor\Widget_Base`, registered centrally):** three-viewer (skyyrose-3d cat, data-model GLB div), product-card (catalog SKU select, col-card markup), collection-hero (col-hero, 3 collections only), featured-product (col-featured, image-left/right), lookbook (Elementor GALLERY, masonry grid + inline hover JS), newsletter (col-nl, inline AJAX→skyyrose_newsletter_subscribe), testimonials (repeater + inline carousel JS), preorder-cta (inline countdown + skyyrosePulse keyframe).

**W2 drifts flagged:**
- **Widgets emit legacy `--col-accent`/`--col-accent-rgb` inline + use `.col-*` classes (col-card/col-hero/col-featured/col-nl) and `.col-rv` reveal class.** Cerebrum says `--col-*` aliases retired in U-4 (design-tokens now `--skyyrose-*`); elementor-widgets.css claims to bridge `--col-accent`→`--skyyrose-accent`. Verify in W7 that `.col-*` selectors + the bridge still exist in CSS, else Elementor widgets render unstyled. Also `.col-rv` ≠ unified reveal selector (`.col-reveal`/`.lp-rv` per premium-interactions `revealSelectors`) — `.col-rv` may never get `.is-visible` → widgets could stay hidden/unanimated. CONFIRM in W7/W8.
- Three collection-aware widgets (product-card/collection-hero/featured-product) hardcode only black-rose/love-hurts/signature accent maps — no kids-capsule (falls back to rose-gold #B76E79). collection-hero scene images point to `assets/scenes/<col>/scene-*.webp` (verify exist in W9/assets).

### W3 root chrome/templates (13 read, 2026-06-06) — synthesis

- **`front-page.php` is a self-contained homepage** — does NOT use header.php nav or footer.php; ships its OWN `.nav #mainNav`, `.mob-menu`, `.ft` footer, inline Org JSON-LD, and inlines homepage-v2.js via `file_get_contents` (bypasses page-optimize stripping). Uses `get_header()` (for `<head>`/wp_head + skip-link + #page open) but overrides nav with `.homepage-v2 .site-header{display:none}` (style.css). Sections: loader, hero (`<picture>` AVIF/WebP LCP via `skyyrose_avif_sibling_pair`), press strip, marquee, story, quote, collections grid (collection lockup `<picture>` images per slug, KC excluded via show_on_front), **kc-heir envelope reveal** (Kids Capsule, hover breaks wax seal), featured product-grid template-part, lookbook (srcset), craft, newsletter. Manually includes shared parts before `wp_footer()`: footer-cro, size-guide-modal, cookie-consent, mobile-bottom-nav (mascot commented). Collection data from `skyyrose_get_front_page_collections()`, counts from `skyyrose_get_collection_products()`.
- `header.php` — `<head>` + wp_head, `.skip-link → #content`, `.site-header #masthead` with `.navbar` (actions left: search/account/cart-badge; brand center: skyyrose-monogram-nav.webp 50×50; primary `wp_nav_menu` right, depth 2), mobile slide-in menu (2nd primary menu render, `inert`), search overlay (`inert`, dialog). Opens `#page > #content`.
- `footer.php` — closes `#content`, `.site-footer #colophon`: footer-cro part, newsletter bar (AJAX form → skyyrose_newsletter_subscribe), 5-col grid (brand+social via `skyyrose_get_social_links()`, Shop from catalog-derived collections, Help, Legal, Connect), copyright. Closes `#page`. Includes size-guide-modal, cookie-consent, mobile-bottom-nav, toast, scroll-progress, back-to-top, `wp_footer()`. **Footer copyright = "The Skyy Rose Collection LLC"** (3rd legal-name variant).
- `index.php` / `archive.php` / `single.php` / `page.php` — standard blog/page templates (`.skr-blog`/`.skr-single`/`.skr-page` → generic-pages.css per enqueue map; blog→generic). `archive.php` uses `get_template_part('template-parts/content/content', get_post_type())` + `content-none` and `get_sidebar()` — **verify template-parts/content/content*.php exist in W5**. single/page/page-wishlist also call `get_sidebar()` (sidebar-1 widget area); homepage/collection/landing/immersive do NOT.
- `404.php` — elaborate: collections-config cards, WC `total_sales` trending query w/ static-catalog fallback (first 4 published SKUs, no hardcoded list), **mascot image** (skyyrose-mascot-404.png → skyy-canonical.jpeg fallback), search, quick-links, newsletter. → 404.css.
- `search.php` — **renders only when Jetpack Instant Search disabled** (per cerebrum). Separate product WP_Query → holo cards (`product-card-holo` part), then content loop, branded empty state (suggestions + collection cards). → search-results.css.
- `searchform.php` / `comments.php` / `sidebar.php` — WP boilerplate (sidebar early-returns if sidebar-1 inactive).
- `page-wishlist.php` — Template Name "Wishlist", WC-gated (graceful "unavailable"), iterates `skyyrose_get_wishlist_items()`, uses **legacy `.product-card` markup (NOT holo)**, get_sidebar+get_footer.

**W3 drifts flagged:**
- **DUPLICATE Organization JSON-LD on homepage:** `seo.php` `skyyrose_organization_schema()` fires on `is_front_page()` (unless Yoast) AND `front-page.php:586` emits its own inline Org JSON-LD unconditionally. Two Organization blocks on `/`. The two also disagree: seo.php legalName "SkyyRose LLC" + sameAs instagram+tiktok; front-page sameAs instagram only + foundingDate 2020 + address. Pick one SoT.
- Legal name now has 3 variants: "SkyyRose LLC" (seo.php), "SkyyRose LLC" (front-page ft copy), "The Skyy Rose Collection LLC" (footer.php copyright). [[wordpress-site-title-canonical]] says site title = "SkyyRose".
- page-wishlist uses legacy `.product-card`, not the holo card system the rest of the theme standardized on.

### W4 page templates (21 read, 2026-06-06) — synthesis

**Thin delegating wrappers (collection + immersive):**
- `template-collection-{black-rose,love-hurts,signature}.php` (16L each) — `get_header()` → `get_template_part('template-parts/collection/page', null, ['slug'=>…])` → `get_footer()`. KC stub branches: launch mode → `template-parts/kids-capsule/teaser`, else collection/page. **All collection page chrome lives in `template-parts/collection/page.php` (W5).**
- `template-immersive-{black-rose,love-hurts,signature,kids-capsule}.php` — thin data-pass wrappers: build `$rooms` array (room name + scene .webp + product hotspots via `skyyrose_immersive_product($sku, ['left','top','prop','prop_label'])`), filter empties, → `template-parts/immersive/scene.php` (the 2D engine, W5). Accent per collection (BR #C0C0C0, LH #DC143C, SG #D4AF37, KC #B76E79). World names: BR "The Bay Bridge", LH "The Cathedral", SG "The Golden Gate Runway", KC "The Playroom". Scene images `assets/.../scene-<col>-<room>.webp` (verify W9). SKUs placed: BR(br-006/005/002/007/004/001/003), LH(lh-004/003/002/006), SG(sg-009/002/003/006/001/011/005/007/015), KC(kids-001/002). **2-product-per-room formula cap noted (overflows at 4+).**

**Elementor builder templates (3):**
- `template-elementor-fullwidth.php` — get_header/footer, `.skyyrose-full-width` the_content, no sidebar.
- `template-elementor-canvas.php` — bare `<!DOCTYPE>` + wp_head/wp_footer only (no header/footer), `body.skyyrose-canvas`, the_content. Design tokens still load via wp_head.
- `template-elementor-editorial.php` — get_header/footer, `.lp[data-collection]` wrapper (palette from `skyyrose_collection` meta, signature default, NO kids-capsule), the_content. enqueue slug 'elementor-editorial' → landing-pages.css.

**Content/conversion templates (full markup):**
- `template-preorder-gateway.php` — multi-collection: 4 hardcoded collection configs, products from `skyyrose_get_collection_products()`, hero-cinematic partial (video), showcase card grid (data-warp for immersive-core), tab bar (tablist), per-collection product grids (holo cards, `display:none` default, JS tabs), sticky cart-summary. `data-collection=""` (no palette override). GSAP+ScrollTrigger+Lenis+immersive-core per enqueue.
- `template-about.php` — editorial scroll-cinema, `.abt-page`. ALL chapter partials NOW wired (earlier "orphan" obs stale): about/featured-video (Blox YouTube `Ja11W-g34Zo`), about/chapter-origin, about/mission, about/collections-grid, about/timeline, about/community (Oakland manifesto places), about/press-section (4 press features). Hero portrait = homepage-story-founder.webp (user-locked). Content sourced from knowledge-base/seed/*.md.
- `template-contact.php` — `.contact-page`: 3 glass info cards, social via `skyyrose_get_social_links()`, contact/form partial (subject_options + referral_options from `skyyrose_get_referral_options()` SoT), response-promise, contact/faq-list partial (own 9-item $faq_items), map placeholder. contact_email default hello@skyyrose.co.
- `template-faq.php` — `.info-page--faq`, 5 categories of `<details>/<summary>` accordions, **emits FAQPage JSON-LD** (unless Yoast). info-pages.css.
- `template-shipping-returns.php` — `.info-page--shipping`, glass cards: shipping table ($17 std / $22 express / free $200+ via SKYYROSE_FREE_SHIPPING_THRESHOLD), returns (30-day), exchanges, pre-orders. Matches founder canon [cmem 12323]. info-pages.css.
- `template-coming-soon.php` — **fully self-contained** (own `<!DOCTYPE>`, inline `<head>`, inline CSS w/ own @font-face Cinzel/Cormorant/Bebas, inline JS, own `.cs-reveal` animation system — NOT premium reveals per cmem 7480). noindex. Newsletter AJAX (source=coming-soon). Loaded by inc/maintenance.php template_redirect when SKYYROSE_COMING_SOON_MODE. cmem 6289: live 503 once served homepage instead — watch deploy.
- **4 landing pages** (`template-landing-{black-rose,love-hurts,signature,kids-capsule}.php`) — `.lp[data-collection]` wrapper, identical section skeleton: hero partial (landing/hero, logo_image .png) → press bar → story → pin-narrative partial + lp-parallax → product-grid partial (landing/product-grid, hardcoded SKU array per collection: BR[br-004/005/006/010], LH[lh-004/002/003/005], SG[sg-001/009/015/006/002/011], KC[kids-001/002]) → editorial gallery (direct product webp imgs) → reviews (hardcoded) → craft → faq partial (landing/faq, per-collection Q&A) → email CTA. `.lp-rv` reveal. wear_count param (social proof). All converge on template-parts/landing/* (W5).

**W4 drifts flagged:**
- **THREE FAQ content sources, overlapping & driftable:** template-faq.php (5 cats, FAQPage schema), template-contact.php $faq_items (9), landing/faq partial (per-collection). No shared FAQ SoT.
- Social-handle inconsistency persists into templates: contact + coming-soon use `theskyyrosecollection`; front-page/footer Org use `skyyroseco`. (4th confirmation.)
- Landing/about/immersive emit collection accents inline; only 3 collections (no KC) in elementor-editorial palette + collection-hero widget.
- cmem 6796: landing hero images historically `loading="lazy"` (LCP-harmful) — verify current state of landing/hero.php in W5.
- Immersive double-asset risk (carry to W8): templates render 2D scene.php (immersive.js) but enqueue-experiences.php still loads Three.js+addons + experiences/*-experience.js on immersive slugs. Confirm whether experiences/*.js still referenced or dead ~607KB.

### W5 template-parts (40 read, 2026-06-06) — synthesis

**Canonical/shared engines:**
- `product-grid.php` — **THE canonical grid wrapper** (cmem 6262). 4 data sources in priority order: `products` (null-sentinel; empty array honored), `featured`, `collection` slug, `skus` list → all resolve via product-catalog.php helpers → delegate each card to `product-card-holo.php`. Handles section heading/aria, stagger-grid, limit cap, multi-token class sanitize, legacy `{token}__header` alias, kids-capsule `/pre-order/` permalink override.
- `product-card-holo.php` — **THE canonical card** (`.holo`/`.holo__*`). WC_Product OR static-card args; front=model image, back=techflat; falls back WC img → catalog front_model → placeholder. Uses `skyyrose_render_picture()` (AVIF/WebP). CTA `.holo__buy[data-product-id]` (real `<button>`, JS-disabled guard), `.holo__wishlist`. **Sizes are static S/M/L/XL pills (not product-derived).**
- `collection/page.php` — unified collection layout (all 4 collection stubs delegate here). Hard-fails on missing content config (logs + `data-skyyrose-error` beacon for verify_live deploy gate). Sections: col-hero (BR gets `rv-scroll-bloom`) → pin-narrative → product-grid → (BR only: founder-pullquote) → story → cta → crossnav → newsletter. Uses `.col-*` classes.
- `immersive/scene.php` — **2D engine, NO Three.js reference.** Composited scene `<img>` layers, hotspot beacon `<a>` (rich data-* for JS panel), room nav/indicators, per-collection lockup `<picture>`, glass product-panel dialog, → tab-bar + cinematic-toggle partials. Missing-scene-image emits `scene-layer--missing` + `data-missing-scene` marker. **Strengthens W8 flag: immersive renders pure 2D — Three.js from enqueue-experiences likely 100% dead here.**
- `hero-cinematic.php` — image-first LCP `<img fetchpriority=high>` + optional `<video>` progressive enhancement + lockup `<picture>`. Used by preorder-gateway.
- `pin-narrative.php` — scroll-pinned beats (`.pin-*`), JS in premium-interactions.js. Beats from arg or `skyyrose_get_collection_content()['pin_beats']`. No-op when no beats.
- `landing/{hero,product-grid,faq}.php` — hero uses `skyyrose_render_picture` for logo (.png→avif/webp), countdown block (landing templates pass `countdown=false` → skipped); product-grid is a **thin adapter → product-grid.php** (KC→/pre-order/); faq accordion (landing-pages.js).
- `product-detail-editorial.php` — 7-chapter scroll PDP (`.sr-ed__*`) driven by dossier data (`skyyrose_get_product_dossier`). BR-only numbered-edition badge. "Wears With" ch.6 retired. Uses `woocommerce_template_single_add_to_cart()`. **Caller unknown — likely woocommerce/single-product.php override (verify W6).**

**about/* (7):** all `.abt-*`, `rv` reveals, arg-driven. chapter-origin (dropcap+pullquote), collections-grid (4 collections, img from catalog first-product), community (Oakland manifesto), featured-video + press-section (Blox iframe — note: featured-video duplicates the `.abt-press__video` module; press-section now gets empty youtube id from caller so no double-embed), mission, timeline. All wired by template-about.php.

**components/* (10) — `sr-*` primitive library, slot pattern, `data-component`:** button, card-editorial, card-info, card-product, drawer, modal, form, input, figure, quote. **Grep-confirmed: only `button` has a real caller (content-none.php, 1 file); `figure` reached only via card-editorial which has 0 callers → transitively dead; other 8 have ZERO callers.** Refines cmem 5668 — primitive library exists but ~9/10 unwired; templates hardcode their own markup instead. `card-product` is a 3rd parallel product-card (`.sr-card-product`) — unused.

**Misc partials:** immersive/loader (SR monogram), tab-bar (4-world cross-nav), cinematic-toggle (inline JS, sessionStorage). contact/form (AJAX, honeypot, referral SoT), contact/faq-list (own accordion). kids-capsule/matching-set (→ holo card), kids-capsule/teaser (launch-mode full page, Klaviyo waitlist list=kids_capsule). cookie-consent (inline JS, localStorage, fires consent for facebook-sdk). footer-cro (reviews+valueprops+FAQ+scarcity, late-enqueues footer-cro.css, JS in footer-cro.js). mobile-bottom-nav (5-item, catalog-derived active state). size-guide-modal (3 tabbed tables tops/bottoms/kids + inline JS focus-trap). skyy-mascot (DISABLED — not included anywhere; gated on `skyyrose-skyy-3d` enqueue which never fires).

**W5 drifts flagged:**
- **archive.php calls `get_template_part('template-parts/content/content', get_post_type())` but only `content-none.php` exists** — no `content.php`/`content-post.php`. Posts in archives render nothing per-item (only the empty-state path works). Real bug for any non-empty post archive.
- **FOUR FAQ implementations now** (faq template, contact/faq-list, landing/faq, footer-cro) + the FAQPage schema only on faq template. Massive FAQ duplication.
- **THREE product-card systems:** `.holo` (canonical), Elementor `.col-card`, components `.sr-card-product` (orphan). Plus page-wishlist's `.product-card`. Consolidation debt.
- Component primitive library (`sr-*`, 10 files, ~1200L) is ~90% dead code — only button reached.
- skyy-mascot.php is fully dead (no includer; all call sites commented out in header/footer/front-page).
- product-detail-editorial.php caller needs W6 confirmation.

### W6 woocommerce/ overrides (5) + patterns/ (4) — synthesis (2026-06-06)

**woocommerce/ template overrides (5):**
- `single-product.php` — **CONFIRMED caller of product-detail-editorial.php.** Dual render path: resolves `$collection`/`$config`/`$meta`/`$sku` + `$catalog_entry` (skyyrose_get_product) + `$dossier` (skyyrose_get_product_dossier). If `$dossier['has_editorial_content']` → `get_template_part('template-parts/product-detail-editorial', null, [...])` (7-chapter editorial PDP). **Else → inline fallback PDP** `.sr-*` ("Technical Garment Lock + Techflat-First"): `.sr-hero` 2-col grid, gallery (techflat primary thumb + model thumb, `data-img` swap), info col (collection label, title, price, specs GARMENT TYPE/SKU/MATERIAL, `woocommerce_template_single_add_to_cart()`, in-stock vs Pre-Order). No tabs/related/upsell — matches founder "garment is protagonist" canon. `get_header('shop')`/`get_footer('shop')`. Resolves W5 carried-forward question. (cmem 7481 dual-render confirmed.)
- `archive-product.php` — **stock WC template, unmodified** (standard hook architecture: before_main_content, shop_loop_header, loop start/end, pagination, sidebar). Header v8.6.0. Real per-item card override happens in content-product.php. (cmem 6246.)
- `content-product.php` — **thin adapter: loop card → holo.** Guards `is_a(WC_Product)` + `is_visible()`, wraps `<li wc_product_class>` → `get_template_part('template-parts/product-card-holo', null, ['product'=>$product])`. This is how shop/archive loops render the canonical `.holo` card. v9.4.0.
- `cart/cart.php` — **full custom dark-luxury cart** (`.skyy-cart`), v10.1.0. WC()->cart null guard. Empty-state (SVG, CTA→shop). Free-shipping progress bar (reads `SKYYROSE_FREE_SHIPPING_THRESHOLD`=200, role=progressbar). 2-col layout: items list (catalog editorial labels via skyyrose_get_product — collection + garment_type_lock truncated 40ch; variation attrs; custom qty stepper `data-action` increase/decrease/`data-key`; remove `data-product_id`/`data-product_sku`) + sticky 400px summary (subtotal/coupons/shipping/fees/tax/total via WC core fns). Buffered express-pay hook `skyyrose_cart_express_pay_buttons` (only renders wrapper if a gateway outputs). Coupon + update-cart forms with nonces. Trust badges. **All WC hooks preserved** → cross-sell/notices still fire via hooks. ⚠ Reminder cmem 7804: cart only renders via `[woocommerce_cart]` shortcode path, NOT Elementor HTML widget.
- `checkout/form-checkout.php` — **custom 4-step checkout** (`.skyy-checkout`), v9.4.0. Cart null guard + empty-redirect. Progress bar (Cart/Information/Payment/Confirmation, JS-driven `data-skyy-progress-fill`). Single WC `<form name="checkout">` split into JS steps: STEP1 contact (billing_email + conditional phone) → STEP2 shipping (all billing_* fields via `woocommerce_form_field`, `woocommerce_checkout_shipping` hook, shipping methods) → STEP3 payment (manual gateway loop: radio per `get_available_payment_gateways()`, first checked, payment_box fields; fallback "support@skyyrose.co" message) → STEP4 review (JS-populated summary `data-skyy-review-*`, order notes, `terms.php`, place-order via `woocommerce_order_button_html` filter, process-checkout nonce). Sticky 420px sidebar (cart items 64px + totals). **All customer-details/order-review hooks preserved** → gateway JS + validation intact.

**patterns/ block patterns (4):** `collection-hero-{black-rose,love-hurts,signature,kids-capsule}.php` — **near-identical Gutenberg block patterns** (registered via inc/patterns.php, category `skyyrose-collections`, post_types page). Each pulls `skyyrose_get_collection($slug)` for label/tagline/desc (hardcoded fallbacks), emits `wp:group` alignfull bg `#0A0A0A` → h1 + tagline + optional desc + single CTA button. **Per-collection deltas = color token + font + button label only:** BR=metallic-silver/cinzel/"Shop Black Rose", LH=crimson/playfair-display/white-text/"Shop Love Hurts", SIG=gold/playfair-display/"Shop Signature", KC=pink/playfair-display/"Shop Kids Capsule". **Note: CTA buttons have NO href** (`<a class="wp-block-button__link">` empty) — editor placeholder patterns, link set on insert. These are the only block-editor (Gutenberg) surface in an otherwise classic-PHP theme.

**W6 drifts/notes:**
- single-product.php inline fallback uses `.sr-*` classes (W7 must confirm `single-product.css`/`sr-*` CSS exists for non-editorial PDPs).
- cart/checkout are large custom templates (~560L/~660L) reliant on JS (`data-skyy-cart`/`data-skyy-checkout`) — W8 must confirm `cart.js`/`checkout.js` (qty stepper, multi-step nav, review populate) exist and are enqueued.
- pattern CTA buttons hrefless by design (editor placeholders) — not a bug, but worth noting for any "broken link" audit.
- No `woocommerce/single-product/` partial overrides (tabs, related, etc.) — theme owns the whole PDP via single-product.php's two paths. Confirms minimal-WC-surface strategy.

### W7a CSS foundation/system (14 read, 2026-06-06) — synthesis

**Build model (CRITICAL — confirms .min.css are LIVE, not dead):** `inc/enqueue.php:48` `$use_min = ! defined('SCRIPT_DEBUG') || ! SCRIPT_DEBUG`. Every CSS load is `$use_min && file_exists(X.min.css) ? X.min.css : X.css`. **Production (no SCRIPT_DEBUG) serves the `.min.css`; unminified `.css` is the dev/fallback + source-of-truth for edits.** Deleting `.min.css` = prod falls back to larger unminified files (perf regression). Both must stay; edit `.css` then regenerate `.min.css`.

- `design-tokens.css` (319L) — **canonical token surface, enqueued globally** (enqueue.php:70). §0 global rules (view-transition, focus-visible, Jetpack overlay pointer-events:none fix). §1 `:root` full token scale (layout, z-index, radius, safe-area, brand color aliases→`var(--rose-gold)` etc, semantic surfaces, glass, depth shadows, gradients, typography scale/decorative/leading/tracking, motion durations + **cinematic easing library** `--ease-cinematic/magnetic/whip/smooth-out/dramatic`, reveal/stagger/split/parallax timing). §1.12 legacy font aliases (`--font-heading`,`--ff-*`) all → `--skyyrose-font-display` (single SoT). §2 **per-collection palette, 5 scopes** (`:root` rose-gold default + `[data-collection=black-rose|love-hurts|signature|kids-capsule]`) — sets accent/secondary/font-display + NEW per-collection display fonts. §3 reduced-motion token overrides.
- `fonts.css` (293L) — self-hosted @font-face, **zero Google CDN**. 8 base families (Inter/Playfair/Cinzel/Cormorant/Oswald/Barlow/Bebas Neue/Instrument Serif) + **4 collection-distinct added 2026-05-24** (Yellowtail→LH, Italiana→SIG+KC, Pinyon Script→SIG+KC accent, UnifrakturMaguntia→BR gothic). **Drift vs project CLAUDE.md** ("8 self-hosted families"/"9 in theme.json") — actually 12 families now. unicode-range subset latin/latin-ext, font-display:swap.
- `main.css` (125L) — global-grain (noise.png animated), global-vignette, + extensive `@media print` (hides chrome, shows link URLs, cart/checkout readable).
- `accessibility.css` (294L) — WCAG 2.2 AA: skip-link, screen-reader-text, focus-visible rings, immersive hotspot/room/size-btn focus, forced-colors, reduced-motion (hides film grain), aria helpers, **44×44 touch targets (2.5.8)** w/ inline-link exemption, prefers-contrast + prefers-reduced-transparency.
- `components.css` (505L) — badges/pills, **.skeleton (def #1)** + skeletonShimmer, spinner, toast (+success/error/info), btn-loading/success/error, breadcrumbs, pagination, divider, section-label, **.skyyrose-film-grain (inline SVG turbulence, site-wide)**, .sr-modal base, **.sr-btn system** (primary/ghost), no-results CTA group.
- `skeleton.css` (146L) — **.skeleton (def #2, CONFLICTS w/ components.css)** + skeleton-shimmer keyframe; skeleton variants (card/text/circle/hero); holo__gallery img-skeleton; **holo__scarcity edition-depletion bars**.
- `view-transitions.css` (73L) — cross-collection morph via View Transitions API (`view-transition-name` on `.col-hero__bg/__logo/__title/__rule`), reduced-motion off-switch. (cmem: F1 v1.5.4.)
- `brand-atmosphere.css` (45L) — particle canvas overlay (Experience Engine Phase 2), FPS-throttle + reduced-motion fade.
- `system/animations.css` (122L) — **.rv system (def #1), triggers `.vis`/`.visible`** + directional/scale variants + d1–d6 delays. Header says "single source of truth" but see drift below.
- `system/animations-premium.css` (475L) — **THE premium motion lib**, triggers `.is-visible`: rv-clip-{up,left,right,diagonal}, rv-blur/-down, split-char/word/line, stagger-grid, magnetic, btn-sweep/border-draw/press, gradient-shift, scroll-fade, parallax-{slow,med,fast,ken-burns}, ambient-glow, page-enter/exit, **safety-net (`srRevealSafety` 0.8s force-show)**, reduced-motion kill-all, **scroll-bloom (`animation-timeline:view()`)**. Consumes design-tokens easings.
- `system/immersive-core.css` (198L) — GSAP-driven (no CSS transitions): ic-overlay intro curtain, ic-dust, scene-lockup/hairline/tagline initial states, `.scene-layer--missing` token fallback, **[data-warp] SVG feDisplacementMap hover-warp** (hover+full-motion gated). Class contract must match immersive-core.js + scene.php.
- `commercial-polish.css` (383L) — **loaded LAST for override priority**. Own `--cp-*` token scale; broad selectors: typography rhythm (h1-h6, p/li max-width 68ch), button polish (`.btn/.button/.wp-block-button__link/.woocommerce .button` → pill radius), card polish (`.product-card/.product/.holo-card/.showcase-card`), hero, **section rhythm**, focus, **`.product-grid` grid-template at 3 breakpoints (1/2/3 col)**.
- `agency-tier-visuals.css` (194L) — double-bezel (`.col-card/.product-card/.holo/.craft-card` machined look), **island-button `!important` on `.hero-cta/.col-card-cta/.btn-primary/.holo__buy` (adds ↗ trailing icon)**, **.rv (def #2/#3) trigger `.is-visible` w/ `--ease-vanguard`**, nav-link mask hover, magnetic.
- `personalization.css` (211L) — `.skyy-curated` Curated-For-You grid (Experience Engine Phase 4), own **skyy-shimmer (skeleton def #3)** + own card markup (`.skyy-curated__card*`).

**W7a drifts flagged:**
- **`.min.css` ARE production assets** (not deletable) — see build model above. They are *output* for reading purposes only.
- **`.skeleton` defined 3×** with 3 different shimmer keyframes: components.css (`skeletonShimmer`, 200% bg-position), skeleton.css (`skeleton-shimmer`, translateX), personalization.css (`skyy-shimmer`, bg-position 800px). components.css + skeleton.css both target bare `.skeleton` → last-loaded wins, unpredictable. Consolidation debt.
- **`.rv` reveal defined in 3 files** with split trigger classes: animations.css uses `.vis`/`.visible`; animations-premium.css + agency-tier-visuals.css use `.is-visible`. Cerebrum says unified observer (premium-interactions.js) toggles `.is-visible` → **animations.css `.rv`+`.vis` path is likely only reached by homepage-v2.js (`.vis`)**; verify in W8. Potential dead `.rv` rules in animations.css for non-homepage pages.
- `.product-grid` grid-template owned by commercial-polish.css AND (W7c) product-grid.css — confirm no conflict.
- `.holo__buy` gets island-button `!important` from agency-tier-visuals — interacts w/ product-card-holo.css (W7c).
- fonts: 12 families now, project CLAUDE.md says 8/9 — doc drift.

### Cleanup Candidates (deletion deferred → ONE confirmed pass at end; policy chosen 2026-06-06)

**Methodology:** a file/rule lands here only when grep proves ZERO references (no enqueue, no get_template_part, no class usage). **SCOPE (user, 2026-06-06): "ONLY DELETE STALE, UNUSED AND DATED findings that DON'T MATCH CURRENT BUILD" — NOT a broad cleanup.** Permission granted "after full analysis." Each delete: grep-prove dead → `git rm`/edit on working tree → `php -l` (+ `npm run verify` before any deploy) → DO NOT commit/deploy without separate confirmation. Working tree already dirty (preorder-gateway.css/js, functions.php — unrelated in-flight) → keep deletes off those. ⚠ **Always quote `--include="*.php"` in grep** — bare `--include=*.php` triggers a zsh glob error that silently no-ops the whole command (caused the false mascot-dead finding).

**DONE — deleted 2026-06-06 (proven stale, matches scope):**
- ✅ `homepage-v7.css` + `homepage-v7.min.css` + enqueue block (enqueue.php ~554-565). Old "Concrete" homepage variant superseded by v2; enqueued but 0 v7 markup in any php/js, no v7 JS, no toggle. `git rm` + surgical enqueue edit (LCP preload kept) + `php -l` clean + zero dangling refs. NOT yet committed/deployed.

**KEEP (looked dead, are NOT):**
- 47 `.min.css` — production-served (build model). Never delete.
- `mascot.css/.min` — **NOT dead: styles the LIVE 404 mascot** (`404.php:190` emits `.error-404-mascot`; mascot.css:127-175 is its only stylesheet). Bug is the opposite — mascot.css isn't enqueued → 404 mascot unstyled. KEEP + needs enqueue fix, not deletion. (`skyy-mascot.php`, `skyy-walk.css/.min`, `mascot.js/.min`, `skyy-3d.js` = the living-widget half, parked intentionally per enqueue.php:207-208,363-364; user confirmed KEEP 2026-06-06.)
- `animations.css` `.rv`/`.vis` — NOT dead: backs homepage-v2.js `.vis` observer (W7d). KEEP.
- `landing-pages.css` `.lp-product-card*` — NOT dead: emitted by elementor-compat.php:394. KEEP.

**CANDIDATES (verify zero-refs in final pass before any rm; intra-file rules = higher risk, hold until W8 confirms no JS dep):**
- `template-parts/components/*.php` 9 of 10 sr-* primitives — W5 grep: only `button` reached (content-none.php); `card-editorial`,`card-info`,`card-product`,`drawer`,`modal`,`form`,`input`,`quote`,`figure` = 0 callers. ⚠ confirm CSS classes not hardcoded elsewhere before removing partials.
- `immersive.css` `.immersive-tab.active` + `.active::after` (L663) — orphaned (template emits BEM `--active`). Dated dup. Intra-file trim candidate.
- `immersive.css` 4 `.scene-viewport::after` env animations (L1013-1097) — overridden dead by immersive-scenes.css. **Design call (which animation set is wanted) — flag, don't auto-delete.**
- `preorder-gateway.css:207` duplicate `@keyframes skeletonShimmer` (also components.css:70) — redundant, dated. Trim candidate.
- `.collection-mascot` rules in mascot.css — orphaned (KC uses `.kc-teaser__silhouette`), but inside a KEEP file → leave unless surgically trimming mascot.css.
- `.skyy-product-card` (woocommerce.css) — possible orphan; W8 must find emitter (content-product.php delegates to `.holo`).
- `.product-card`/`.holo-card`/`.holo-card__media` Bento+polish selectors (product-grid.css, commercial-polish.css, agency-tier-visuals.css) — may match nothing (grid item is `.holo`). W8 confirm grid-item markup first.

**REFACTOR (merge, don't delete):**
- Triple `.skeleton`/shimmer (components.css + skeleton.css + personalization.css) → consolidate to one.
- Triple `.rv` (animations.css `.vis` vs premium/agency `.is-visible`) → unify trigger; confirm animations.css `.rv` still has a live consumer (W8) before trimming.

### W7b CSS chrome (9 read, 2026-06-06) — synthesis

- `header.css` (214L) — `.site-header` fixed + `.navbar` glass (blur 24px saturate 180%), `.scrolled` → rose-gold border. 3-col grid (`1fr auto 1fr`): left actions / center SR-monogram logo (44–52px) / right `.navbar__menu` (mono-font 11px tracked). ≤1024px → hamburger + `.mobile-menu` slide panel (85vw, translateX). `.search-overlay` (blur 32px). Legacy `.navbar__site-title` (Cinzel) fallback. Reduced-motion kills transitions.
- `footer.css` (322L) — `.footer-newsletter` bar (rose→gold gradient, 260px input), `.footer-grid` 5-col (2fr+4×1fr → 2-col ≤1024 → 1-col ≤480), `.footer-brand` (monogram+tagline+social 44px), `.footer-copyright` (safe-area bottom pad), `.toast-container` lives here (z3000). Uses `--skyyrose-accent` + legacy `--font-heading`. WCAG contrast lift (0.57 alpha). Print-clean.
- `footer-cro.css` (225L) — `.ft-cro-*`: scarcity banner, reviews 3-col, craft/value-props 4-col, **FAQ accordion (`.ft-cro-faq`)**. **Late-enqueued from inside footer-cro.php** (bytes only ship where rendered). Extracted from design-tokens v1.5.21. = the CSS for FAQ-impl #4 (W5).
- `mobile-bottom-nav.css` (90L) — `.mobile-nav` glass bar, `display:none` desktop → `flex` ≤768px, body padding-bottom offset, 5×`.mobile-nav__item` (20% each), rose-gold active, `.mobile-nav__badge` (cart count). Safe-area inset.
- `cookie-consent.css` (132L) — `.cookie-consent` glass bar (z9998), `--hidden` translateY, accept/decline btns, ≤599px stacks above mobile-nav (`bottom: calc(64px + safe-area)`).
- `size-guide.css` (113L) — `.sg-*` modal: overlay z10000, panel (blur 24px), tabs, measurement `.sg-table` (zebra rows), how-to-measure. Uses `--ff-brand`/`--ff-system`.
- `luxury-cursor.css` (71L) — `.cursor-dot`(8px rose)/`.cursor-ring`(36px), hover/down state scales, **hard-off on `(hover:none)`, ≤768px, reduced-motion** (restores native cursor). (cmem CURS-03: enqueue also gated off immersive slug.)
- `mascot.css` (573L) — **PARKED (CSS not enqueued).** TWO systems: (1) static `.collection-mascot` (Kids Capsule section, float anim) + `.error-404-mascot` (404 speech bubble, wiggle); (2) living `.skyyrose-mascot` widget (walk-on/off, breathe, speak-pulse, excited, glance, bubble, choice-chips, minimize, recall pill). Full reduced-motion coverage.
- `skyy-walk.css` (159L) — **PARKED.** Pixar walk-cycle layer over the living widget: walk-step/off-lean, breathe-enhanced, talk, jump, glance keyframes (all `!important` overrides of mascot.css). Image placeholder.

**W7b drifts flagged (RESOLVED in W7c):**
- **Mobile-nav print-hide = REAL BUG (confirmed):** `mobile-bottom-nav.php` emits `class="mobile-nav"`; `mobile-bottom-nav.css` correctly targets `.mobile-nav` ✓. BUT `main.css:65` + `footer.css` `@media print` hide `.mobile-bottom-nav` — **a class no PHP emits** → mobile bottom nav is NOT hidden when printing. Fix: change print selector `.mobile-bottom-nav` → `.mobile-nav` in main.css + footer.css. Low severity (print only).
- **⚠ CORRECTION (W7d, 2026-06-06): mascot.css is NOT dead — it styles a LIVE page.** Earlier W7c claim ("ZERO php emit `.error-404-mascot`/`.collection-mascot`") was based on a **broken grep** (`grep --include=*.php` threw a zsh "no matches found" glob error → command never ran, false-negative). Re-grep with `--include="*.php"` quoted: **`404.php:190,209,216,220` DOES emit `.error-404-mascot` / `__image` / `__placeholder` / `__speech`** (live 404 page). `mascot.css:127-175` is the only stylesheet for those classes. **mascot.css is enqueued NOWHERE** (commented at enqueue.php:207-208). → **REAL BUG: the 404 mascot renders UNSTYLED** (live markup, disabled stylesheet). mascot.css must be KEPT (it backs a live page) and arguably its 404-relevant half re-enqueued. Only `.collection-mascot` (Kids-Capsule) is genuinely orphaned — KC's teaser uses `.kc-teaser__silhouette*` instead (Agent B confirmed).

### W7c CSS product/woo (7 read, 2026-06-06) — synthesis

- `product-card-holo.css` (218L) — **THE canonical card `.holo`** (sharp border, front-model→back-techflat crossfade w/ grayscale, badges soldout/preorder, info, quick-add `.holo__drawer` w/ size pills + `.holo__buy`). **Touch (`hover:none`): drawer goes static/visible** (else CTA unreachable). Reduced-motion kills transitions. ⚠ agency-tier-visuals.css globally applies double-bezel + island-button `!important` to `.holo`/`.holo__buy`; this file doesn't know that.
- `product-grid.css` (55L) — `.product-grid__items` 4-col grid + **Bento (`nth-child(4n+1)` span 2×2)**. ⚠ Bento selectors target `.product-card` + `.holo-card__media` — **but canonical card class is `.holo`** (see drift). Uses `--ff-brand`/`--ff-editorial`/`--haze` (--haze not in design-tokens — undefined var).
- `single-product.css` (212L) — **RESOLVES W6 flag: `.sr-*` PDP CSS EXISTS** for the non-editorial fallback (sr-hero-grid 1.2fr/1fr, sr-gallery + thumbs, sr-info specs, `.single_add_to_cart_button` `!important` override) + **`.sr-ed__edition-badge`** for the BR editorial hand-numbered badge (F4 v1.5.4). Uses `--skyyrose-*` tokens.
- `woocommerce.css` (290L) — base, loaded ALL WC pages. Defines `--skyy-woo-*` var set. **`.skyy-product-card` = ANOTHER product-card system** (shop-loop, quick-view overlay, add-to-cart actions) — but W6 `content-product.php` delegates to `.holo`, so `.skyy-product-card` may be orphaned (verify what emits it). Collection accent variants (`.collection-black-rose`/`.collection-love-hurts` hover). WC notice overrides.
- `woocommerce-cart.css` (570L) — full `.skyy-cart__*` (empty state, header, 1fr/400px layout, items grid 150px, qty stepper, coupon, sticky summary, trust badges). Conditional-load. Requires woocommerce.css vars.
- `woocommerce-checkout.css` (624L) — full `.skyy-checkout__*` (progress bar is-active/is-complete, 1fr/420px layout, JS `.skyy-checkout__step` display toggle, glass panels, form fields, **`:has(input:checked)` payment-method highlight**, review, sticky sidebar). Requires woocommerce.css vars.
- `elementor-widgets.css` (485L) — **`--col-accent`→`--skyyrose-accent` token bridge** (cerebrum-confirmed, v1.5.0) for 5 widgets. `.col-rv` (`.is-visible`, unified observer). col-btn / col-feat / col-card / col-hero / col-nl families, all consuming `--skyyrose-*` w/ fallbacks. Reduced-motion.

**W7c drifts flagged:**
- **MISSING WC CSS vars (real bug):** `woocommerce-cart.css` uses `var(--skyy-woo-bg)` (L17) + `var(--skyy-woo-danger)` (L270, remove-btn hover); `woocommerce-checkout.css` uses `var(--skyy-woo-bg)`. **Neither `--skyy-woo-bg` nor `--skyy-woo-danger` is defined in woocommerce.css `:root`** (it has surface/glass/text/border/accent/gold/success/radius — no bg, no danger), and the usages have NO fallback. → cart/checkout bg resolves to nothing (rescued by body #0A0A0A); remove-btn hover bg = no-op. Add `--skyy-woo-bg:#0A0A0A; --skyy-woo-danger:#DC143C;` to woocommerce.css.
- **Product-card class fragmentation (5 systems):** `.holo` (canonical), `.skyy-product-card` (woocommerce.css, likely orphan), `.col-card` (elementor), `.skyy-curated__card` (personalization), + selectors `.product-card`/`.holo-card`/`.holo-card__media`/`.showcase-card` referenced by commercial-polish.css + agency-tier-visuals.css + product-grid.css Bento that may match nothing (canonical is `.holo`). Verify in W8 what wrapper class `product-grid.php` emits — if `.holo` not `.product-card`, the Bento span rules + commercial-polish `.holo-card` polish are dead. → Cleanup-candidates pending W8.
- Undefined vars: `--haze` (product-grid.css), confirm `--skyy-woo-*` completeness.

### W7d CSS pages (17 read via 3 parallel readers, 2026-06-06) — synthesis

- `homepage-v2.css` (1491L) — **THE live front-page** (`body.homepage-v2`). Loader, fixed nav, letter-stagger hero (dual-bg crossfade), press strip, marquee, story, quote, **bento collections (`.col-card` — homepage-LOCAL, distinct from elementor's `.col-card`)**, Kids-Capsule "Heir Apparent" wax-seal envelope (`.kc-heir__*`), lookbook, craft grid, newsletter, footer. **Owns a private `:root` token island** (`--void/--smoke/--glass/--ghost/--haze/--cloud/--rose/--silver/--crimson/--gold/--ff-brand|editorial|mono|cta/--ease-luxury/--space-sm…xl`) duplicating brand-palette hexes (2nd source of truth). Reveal = `.vis` via its OWN observer in homepage-v2.js (intentional exception).
- `homepage-v7.css` (644L) — **DEAD → DELETED 2026-06-06.** Was enqueued on front-page slug but front-page.php emits 0 v7 classes (61 v2 matches), no homepage-v7.js, no toggle. Old "Concrete" variant superseded by v2. Also referenced undefined `--bone`.
- `immersive.css` (1216L) — base immersive system: scene viewport/layers, vignette, grain, `.hotspot` beacons, glass `.product-panel` (AJAX), room-nav, tab-bar, per-collection beacon colors, env overlays, cart notification. **Undefined vars (all fallback-guarded, no breakage): `--rose-gold`/`--silver`/`--crimson`/`--gold`/`--tab-accent`/`--gradient-rose-gold` (canonical names are `--color-*`).** Dead rules: 4 `.scene-viewport::after` env animations (candleFlicker/lightShimmer/mistDrift/kidsGlow, L1013-1097) **overridden by immersive-scenes.css** (same selector, loads after); `.immersive-tab.active` + `::after` (L663) **orphaned** (template emits BEM `--active`).
- `immersive-scenes.css` (271L) — thin overlay on immersive.css (loads after): scene color-wash, particle drift, tab-bar restyle (BEM `--active`), cinematic-toggle. Redefines `beaconPulse`/`.scene-layer img`/`.immersive-tab-bar` (wins by load order). Undefined `--accent-color`/`--accent-r/g/b` (fallback).
- `hero-cinematic.css` (177L) — **CLEAN model file.** hero-cinematic.php (img/video hero, 12-col grid, pill CTA). Only canonical `--skyyrose-*`. front-page slug.
- `pin-narrative.css` (151L) — **CLEAN model file.** Scroll-pinned manifesto (`.pin-*`, `.is-active`), driver in premium-interactions.js. collection-standalone + landing slugs. Canonical tokens only.
- `collection-pages.css` (620L) — **CLEAN.** 4 collection pages (`.col-*` on `.col-page[data-collection]`): hero, marquee, story, bento, cross-nav, newsletter, BR-only founder pull-quote. Reveal `.col-reveal`→`.is-visible` (rule in animations-premium.css, not here).
- `landing-pages.css` (998L) — `.lp-*` on `.lp[data-collection]`, consumes `--skyyrose-*` directly (no aliases). **`.lp-product-card*` (L504-569) dead for NATIVE landing markup (delegates to `.holo`) BUT still emitted by `inc/builders/elementor-compat.php:394-402` (Elementor widget) → KEEP, retain-with-cause.** Reveal `.lp-rv`→`.is-visible`.
- `about.css` (1011L) — **CLEAN.** template-about (`.abt-*`). KITH/FOG editorial: lookbook hero, CH.01-04 chapters, dropcap, pullquotes, cream mission/community, timeline, press grid, featured-video. `.abt-page .rv`→`.is-visible` (rule lives here). Cinzel hardcoded ~14× (intentional, commented).
- `preorder-gateway.css` (457L) — `.po-*` on `.po-collection[data-collection]`, own `:root` `--po-*` palette. Self-hosts `.lp-faq*` scoped under `.preorder-gateway` (avoids enqueuing landing-pages.css). **DUP: `@keyframes skeletonShimmer` (L207) redefines components.css:70 — comment says "re-use global" but redefines (redundant).** Reveal `.po-rv`/`.preorder-gateway .lp-rv`→`--visible` suffix (NOT `.is-visible` — separate GSAP observer, intentional exception).
- `kids-capsule.css` (442L) — KC launch-mode teaser (slug `kc-launch`, only when launch mode on). `.kc-teaser__*`/`.kc-countdown*`/`.kc-waitlist-form*` + own `:root` `--kc-*`. **Undefined `--space-{sm…3xl}` + `--font-{display,body,ui}` (work by fallback only — those token names not in design-tokens; `--text-*` DO resolve).** `.kc-teaser__silhouette*` is KC's mascot-equivalent (NOT `.collection-mascot`).
- `404.css` (1218L) — `.error-404-*` (number gradient, sparkles, search, collection cards `[data-collection]`, trending, quick-nav, orbs, grain). All classes emitted by 404.php. **Note: 404.php ALSO emits `.error-404-mascot` → styled by mascot.css (NOT enqueued) → mascot unstyled (see correction above).** Hardcodes brand hex (stylistic).
- `contact.css` (1233L) — **all used** (template-contact + contact/form + contact/faq-list). `.contact-*`, AJAX form (`.contact-form__*`, honeypot, conditional order-number field via contact.js), CSS-drawn placeholder map (NOT a real embed), page-local `.contact-toast` (separate from global skyyToast). `.faq-item*` here = `<button>`+aria impl; differs from info-pages `<details>` impl — never co-load.
- `search-results.css` (682L) — search.php only (slug `search`). `.search-results__*` + `.product-grid__items` holo grid + branded empty state (mirrors 404 cards). Styles the **Jetpack-OFF server-rendered DOM**; ships regardless, inert when Instant Search active (cerebrum-consistent).
- `info-pages.css` (383L) — **shared by 2 templates**: faq (`.faq-*` `<details>`) + shipping-returns (`.ship-*` tables/steps) on a common `.info-page__*` shell. Deliberate 2-template file, no dead half.
- `generic-pages.css` (504L) — **most token-disciplined file.** WP fallbacks: page.php (`.skr-page__*`), single.php (`.skr-single__*`), index/blog (`.skr-blog__*`). Distinct from info-pages (different namespaces, both live).

**W7d cross-file confirmations:**
- **Reveal-trigger map (definitive):** homepage-v2 `.vis` (own JS observer — exception); collection `.col-reveal`→`.is-visible`; landing `.lp-rv`→`.is-visible`; about `.abt-page .rv`→`.is-visible`; preorder `.po-rv`→`--visible` (exception); pin/immersive `.is-active`. → **CORRECTS W7a flag: animations.css `.rv`/`.vis` is NOT dead — it's the CSS backing homepage-v2.js's `.vis` observer. KEEP.**
- **Product card emitters (definitive):** collection + landing native → `.holo` (via product-grid.php→product-card-holo.php). homepage bento tiles → `.col-card` (homepage-v2-local). `.lp-product-card` → Elementor widget only. → the `.product-card`/`.holo-card`/`.holo-card__media` Bento+polish selectors (product-grid.css, commercial-polish.css, agency-tier-visuals.css) target a class **no current template emits** as the grid item — product-grid.php wrapper item is the `.holo` card itself. **W8 must confirm exact grid-item markup before trimming Bento/`.holo-card` rules.**
- `--col-*` aliases: `--lp-*` fully gone; `--col-accent`/`--col-accent-rgb` survive ONLY as the inbound Elementor bridge (elementor-widgets.css consumes them). Cerebrum "fully retired U-4" is half-true.

### W8 JS (44 read via 2 parallel readers, 2026-06-06) — synthesis

- **`premium-interactions.js`** = THE unified motion engine (Motion One `inView()` + IntersectionObserver fallback, both toggle `.is-visible`). `revealSelectors`: rv-clip-*, rv-blur*, stagger-grid, rv-split-*, `.col-reveal`, `.lp-rv`, `.rv`. Sets `--stagger-index/--mag-x/y/--scroll-progress/--parallax-offset/--char|word|line-index/--glow-x/y`. Also owns pin-narrative scroll engine + a fade+full-reload page transition.
- **`woocommerce.js`** (528L) = drives BOTH cart (`[data-skyy-cart]`, qty stepper `data-action`) AND checkout (`[data-skyy-checkout]`, multi-step `data-skyy-next-step`, review `data-skyy-review-*`). **RESOLVES W6/W7c flag: cart.js/checkout.js need not exist — woocommerce.js is the single driver. Cart+checkout ARE functional.**
- `product-card-holo.js` — operates on `.holo`/`.holo__*` ONLY (tilt, IO stagger `.holo--visible`, localStorage wishlist, real `wc-ajax=add_to_cart`). **CONFIRMS: `.holo-card`/`.skyy-product-card`/`.product-card` have ZERO JS creating them → orphaned CSS** (woocommerce.css/product-grid.css/commercial-polish.css/preorder-gateway.css + page-wishlist.php). `.product-card` survives partly (WC-default markup + page-wishlist); `.holo-card` + `.skyy-product-card` are dead.
- `homepage-v2.js` — front-page only, OWN `.vis` observer (the intentional exception confirming animations.css `.rv`/`.vis` is live, not dead).
- `immersive.js` (v2.0) = current 2D immersive engine (hotspot→panel, room crossfade, parallax) — NOT Three.js. `system/immersive-core.js` (v7.0) = Lenis + GSAP intro + SVG warp. `immersive-wc-bridge.js` = immersive AJAX add-to-cart. All 3 active on immersive slug.
- **STALE cerebrum corrected:** smart-showcase.js + immersive-wc-bridge.js innerHTML **already fixed** (createElement/DOMParser+cloneNode; obs #8956, 2026-05-27). No XSS in any audited JS. The only `innerHTML` write = micro-interactions.js:127 (hardcoded SVG constant — rule-violation, not exploit).
- Minor JS bugs (fix, not delete): personalization.js:199 `&` should be `?` (malformed REST query); collection-pages.js newsletter shows success regardless of server result.
- Two parallel wishlist systems (server `wishlist.js` vs localStorage `product-card-holo.js`); two page-transition systems (page-transitions.js View-Transitions vs premium-interactions.js fade-reload). Duplication debt.

**W8 DELETION (dead Three.js 3D cluster — stale/doesn't-match-build):** the 4 immersive templates enqueued `inc/enqueue-experiences.php` (priority 65) → `three.min.js` (~593KB) + 9 addons + `experiences/{experience-base,luxury-animations,init-3d,4×collection}.js` targeting `.skyyrose-3d-container` — **a class NO php emits** (scene.php is pure 2D). init-3d.js silently bailed over an empty NodeList; ~700KB dead weight per immersive visit. Stale memory (#2348 feature-flag, #9740 never-enqueued) both wrong — it was live-but-DOM-orphaned. **DELETED:** `inc/enqueue-experiences.php`, functions.php:70 include, `assets/js/experiences/` (16), `assets/js/lib/three.min.js`, `assets/js/lib/three-examples/` (9) + cleaned stale comment/dead `threejs` defer branch in enqueue-performance.php + enqueue.php comment. **KEPT:** `skyy-3d.js` (mascot 3D, loads Three from jsDelivr CDN — independent of lib/three, parked) + gsap/lenis/motion/ScrollTrigger libs (live).

### W9 data canon (read via reader, 2026-06-06) — synthesis

- **`skyyrose-catalog.csv` = SoT, 33 products** (24 cols incl. `dossier_slug`, `garment_type_lock`, `is_preorder`, `render_*`). BR 14 / SG 12 / LH 5 / KC 2. **Project `wordpress-theme/CLAUDE.md` says "34" — stale, off by one** (br-013 retired as br-003 dup; lh-006 added from lh-002 split).
- **Clean 1:1:** catalog (33) ↔ dossiers (33) ↔ logo-registry `sku_logos` (33). Zero orphans. Retired SKUs (lh-001/sg-004/sg-008/br-d0x/sg-d0x/br-013) absent. ✓
- Dossiers = **3D-render briefs** (Gemini prompts: garment lock, branding spec, negative, scene direction) — NOT editorial prose. `has_editorial_content` is NOT in the .md frontmatter but **IS derived in `inc/product-catalog.php:323`** (`skyyrose_get_product_dossier()` sets it true under a condition; default false at :284) → **editorial PDP path IS reachable (not dead).**
- **STALE DATA (regenerate, don't delete — it's read by product-catalog.php:360):** `product-similarities.json` declares 33 but has 32 keys — **missing lh-006**, and lh-002 carries pre-split name. Primary stale-data hit.
- logo-registry.json vs brand-logos/*.md = two taxonomies, naming-inconsistent (only 2 shared IDs) but functional; all 9 dossier logo pointers resolve. Doc gap: 6 registry logos lack a brand-logo .md.
- Minor: br-014/br-015 `badge=Pre-Order` but `is_preorder=0` (flag oversight).

### W10 tests (read via reader, 2026-06-06) — synthesis

- Harness: `bootstrap.php` (loads 5 inc modules) + `wp-stubs.php` (pure-PHP WP shim, no WP_Mock). 4 unit tests cover ONLY pure/deterministic modules: BrandColors, CollectionsConfig, ProductCatalog(+display), Performance. ~4 of ~46 inc modules tested (by design — DB/HTTP/hook modules excluded).
- ProductCatalogTest reads the REAL csv; **no hard count assertion** (count drift won't break tests). CollectionsConfigTest asserts 4 collections + computes front-visible dynamically.
- Stale (cosmetic, non-failing): `ProductCatalogTest:42` normalizes `kc-001` (should be `kids-001` — `kc-` prefix retired); `bootstrap.php` `SKYYROSE_VERSION='1.1.1'` (test-only, not asserted).
- `tests/coverage/html/*` = generated PHPUnit report, **already gitignored** (theme `.gitignore:21`) — NOT committed, no action.

### FINAL DELETION SUMMARY (2026-06-06, after full W0–W10 analysis; scope = stale/unused/dated ≠ current build)

**Deleted (29 files + 4 edits), php -l clean, NOT committed/deployed:**
1. `homepage-v7.css` + `.min.css` + enqueue block — dead homepage variant (v2 is current).
2. Three.js 3D experiences cluster (27 files): `inc/enqueue-experiences.php`, `assets/js/experiences/*` (16), `assets/js/lib/three.min.js`, `assets/js/lib/three-examples/*` (9) + `functions.php` include + enqueue.php/enqueue-performance.php comment & dead `threejs` defer branch.

**Recoverable:** all via `git checkout`.

**RELOCATED 2026-06-06:** current branch `feat/legal-policies-shipping-sync` was 2 behind `origin/main` (+ behind newer `feat/gpt-image-2-and-wc-template-notice`; none touch homepage/3D). Per user, moved deletions onto fresh branch **`chore/wp-remove-dead-3d-homepage-v7`** off `origin/main` (worktree `/Users/theceo/DevSkyy-wp-deadcode`), commit **`4d6ab1abb`** (32 files, −11,116 lines, pre-commit php-lint passed, NOT pushed/deployed). Original branch restored to ONLY its pre-existing in-flight work (preorder-gateway ×4, functions.php version bump 1.5.27→1.5.28, readme.txt, style.css, untracked video/CLAUDE.md) — zero deletions there. homepage-v7 deletion (last turn) also folded into the new branch commit.

**NOT deleted (deliberately):** `.min.css` (prod), mascot.css/mascot.js/skyy-3d.js/skyy-walk.css (parked + mascot.css backs live 404), `.lp-product-card` (Elementor), `product-similarities.json` (used — regenerate instead), editorial PDP (reachable via derived flag), components/* primitives + orphaned card-CSS rules (intra-file, lower-confidence — flagged refactor).

**Real bugs to FIX later (not deletions):**
- 404 mascot unstyled — `mascot.css` styles live `.error-404-mascot` but is enqueued nowhere.
- Missing WC vars `--skyy-woo-bg`/`--skyy-woo-danger` (woocommerce.css :root) — no fallback.
- Print rule hides `.mobile-bottom-nav` but PHP emits `.mobile-nav`.
- product-similarities.json stale (missing lh-006).
- personalization.js:199 `&`→`?`; collection-pages newsletter false-success.
- Doc: wordpress-theme/CLAUDE.md "34 products" → 33. br-014/br-015 is_preorder flag.

**Refactor candidates (consolidation, not deletion):** triple `.skeleton`/shimmer; triple `.rv`; preorder dup `skeletonShimmer`; orphaned `.holo-card`/`.skyy-product-card` CSS; immersive.css dead `.immersive-tab.active` + 4 overridden `.scene-viewport::after`; 2 wishlist + 2 page-transition systems; homepage-v2 token island.
