After fixing the current issue, update CLAUDE.md with the learning so this mistake never repeats.

Steps:
1. Identify what went wrong
2. Fix it
3. Add a one-line entry to the Learnings section of /CLAUDE.md under the appropriate category
4. Commit the fix AND the CLAUDE.md update together

Format for learning entry:
`- Brief description of what to do/avoid (context: what file/system it applies to)`
