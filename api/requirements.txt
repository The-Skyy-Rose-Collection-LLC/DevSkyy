# DevSkyy API Requirements
# ========================
# Requirements for Vercel serverless Python functions

# Core Framework
fastapi>=0.104
mangum>=0.18.0
pydantic>=2.5
pydantic-settings>=2.1

# Security
PyJWT>=2.8
passlib[bcrypt]>=1.7
argon2-cffi>=23.1
cryptography>=41

# HTTP Client
httpx>=0.25

# Async Database (optional - uses in-memory fallback if not available)
# asyncpg>=0.29
