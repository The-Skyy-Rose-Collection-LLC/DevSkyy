# ============================================================================
# DevSkyy Vercel - Serverless Deployment Isolated Dependencies
# Minimal dependencies for serverless deployment with latest security updates
# ============================================================================

# Build Dependencies
setuptools>=78.1.1

# Core FastAPI Framework (Latest Secure Versions)
fastapi==0.119.0
uvicorn[standard]==0.34.0
starlette==0.48.0

# Essential Dependencies with Email Support
pydantic==2.7.4
email-validator==2.1.0  # Explicit email validator for Pydantic
python-multipart==0.0.18
PyJWT==2.10.1  # Replaces python-jose for better security
passlib[bcrypt]==1.7.4

# Database (Lightweight)
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0

# HTTP Client
httpx==0.28.1
requests==2.32.4
aiofiles==24.1.0

# Logging and Monitoring
structlog==24.4.0
prometheus-client==0.22.0

# Security (Latest)
cryptography==46.0.2
bcrypt==4.2.1

# Utilities
python-dotenv==1.0.1
click==8.1.8

# Redis (Optional - for caching)
redis==5.2.1
aioredis==2.0.1

# AI Integration (Lightweight - SDKs only, no heavy models)
anthropic==0.69.0
openai==2.3.0

# Template Engine
jinja2==3.1.4

# Vercel-specific optimizations
dnspython==2.7.0  # Required for some networking operations
typing-extensions==4.12.2  # Ensure compatibility

# Security Middleware
slowapi==0.1.9
secure==0.3.0

# Note: Heavy dependencies like TensorFlow, PyTorch, transformers are excluded
# for optimal serverless performance and faster cold starts
# Security updates applied: 2024-10-24
# Email validation fix applied: 2024-10-24
