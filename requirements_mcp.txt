# DevSkyy MCP Server Dependencies - Enhanced Edition v2.0.0
# Install with: pip install -r requirements_mcp.txt
# All versions pinned for reproducibility (per DevSkyy standards)

# MCP Framework (pinned versions)
fastmcp==0.1.0        # FastMCP framework for building MCP servers
mcp==1.21.0           # Model Context Protocol core (updated to match requirements.txt)

# HTTP Client (pinned versions)
httpx==0.28.1         # Async HTTP client for API calls (updated to match requirements.txt)

# Data Validation (pinned versions)
pydantic==2.10.4      # Data validation and settings management (updated to match requirements.txt)

# Async Support (pinned versions)
asyncio-throttle==1.0.0  # Rate limiting for async operations

# Security & Analytics (pinned versions)
PyJWT==2.10.1                      # JWT handling for secure API calls (updated, replaces python-jose)
python-dotenv==1.0.1               # Environment variable management
rich==13.9.4                       # Enhanced console output (updated to match requirements.txt)

# Development & Testing (Optional - pinned versions)
pytest==8.4.2                      # Testing framework (updated to match requirements.txt)
pytest-asyncio==0.24.0             # Async testing support (updated to match requirements.txt)
