# DevSkyy Vercel-Optimized Requirements v3.0
# Minimal dependencies for serverless deployment with latest security updates
# Uses inheritance from base requirements for consistency

# Note: Vercel has specific requirements for serverless deployment
# Heavy dependencies like TensorFlow, PyTorch are excluded for optimal performance

# Build Dependencies (Security-Critical)
setuptools>=78.1.1  # Fix CVE-2025-47273, CVE-2024-6345

# Core FastAPI Framework (Latest Secure Versions)
fastapi==0.119.0
uvicorn[standard]==0.38.0  # Updated to match requirements.txt
starlette==0.48.0

# Essential Dependencies with Email Support
pydantic==2.10.4  # Updated to match requirements.txt
email-validator==2.1.0  # Explicit email validator for Pydantic
python-multipart==0.0.18
PyJWT==2.10.1
passlib[bcrypt]==1.7.4

# Database (Lightweight)
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0

# HTTP Client (Updated)
httpx==0.28.1
requests==2.32.4
aiofiles==24.1.0

# Logging and Monitoring (Updated)
structlog==24.4.0
prometheus-client==0.22.0

# Security (Latest)
cryptography==46.0.3  # Fix CVE-2024-26130, CVE-2023-50782, CVE-2024-0727
bcrypt==4.2.1

# Utilities
python-dotenv==1.0.1
click==8.1.8

# Redis (Optional - for caching)
redis==5.2.1
aioredis==2.0.1

# AI Integration (Lightweight)
anthropic==0.69.0
openai==2.7.2  # Updated to match requirements.txt

# Template Engine
jinja2==3.1.4

# Vercel-specific optimizations
dnspython==2.7.0  # Required for some networking operations
typing-extensions==4.12.2  # Ensure compatibility

# Security updates applied: 2025-11-12
# Synchronized with main requirements.txt versions
